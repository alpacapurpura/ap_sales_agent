- [x] `facebook-business` package is installed in backend environment.
- [x] `META_APP_ID` and `META_APP_SECRET` are set in environment.
- [x] `MetaAdapter` uses `FacebookAdsApi` for initialization.
- [x] `GET /api/v1/connections/meta/auth-url` returns a valid Facebook Login URL.
- [x] `POST /api/v1/connections/meta/callback` successfully exchanges code for token.
- [x] `POST /api/v1/connections/meta/test` returns user profile data using the SDK.
- [x] Frontend displays "Connect Meta" button.
- [x] Connection status updates to "Connected" after successful flow.
- [x] "Test Connection" button appears for connected accounts.
- [x] Clicking "Test Connection" shows a success toast with user profile name.
