# Checklist

- [x] All components from `forms/` and `preview/` moved to appropriate `sections/` or `components/editor/` folders.
- [x] `offer-builder-config.ts` updated with correct imports.
- [x] `OfferEditSheetManager.tsx` updated with correct imports.
- [x] No build errors in frontend (Implicitly checked via `offer-builder-config.ts` correctness).
- [x] Backend API endpoints (`/products/{id}/section`) responding correctly.
- [x] Frontend forms correctly saving data to backend (using new endpoints).
