# Tasks

- [x] Task 1: Refactor IdentityPreview (Creative Hero Style)
    - [x] Remove internal "Identidad" title and `Card` wrapper.
    - [x] Implement a "Hero" design inspired by `Brand Studio > HeaderSection`.
        - Use a gradient/subtle background container.
        - Display `public_name` as H1 (Large/Bold).
        - Display `type` and `delivery_model` as visual badges/icons.
    - [x] Ensure it looks good in both empty and populated states.

- [x] Task 2: Refactor StrategyPreview (Visual Summary)
    - [x] Remove internal "Estrategia" title and `Card` wrapper.
    - [x] Implement a "Persona & Highlights" design.
        - **Avatar**: Create a mini "User Card" layout (Avatar Icon + Name).
        - **Pain Points**: Display as a "Tag Cloud" or clean list (Top 3 only).
    - [x] Use icons (`Target`, `User`, `AlertCircle`) creatively to guide the eye.

- [x] Task 3: Refactor & Clean Other Previews
    - [x] **PricingPreview**: Remove title/card. Display price as a large, bold number with currency symbol (like a price tag).
    - [x] **InstructorsPreview**: Remove title/card. Display instructors as a row of avatars.
    - [x] **ValueStackPreview**: Remove title/card. Show a simple list or count of deliverables.
    - [x] **ResourcesPreview**: Remove title/card. Show file icons.
    - [x] **GalleryPreview**: Remove title/card. Show a mini-grid of images.
    - [x] **ClosingPreview**: Remove title/card. Show guarantee badge.
    - [x] **PlaceholderPreview**: Ensure it has no title/card.

- [x] Task 4: Polish OfferSectionWrapper
    - [x] Verify that the `OfferSectionWrapper` title and icon look good with the new "flat" content.
    - [x] Ensure hover states for "Edit" actions work seamlessly with the new designs.
