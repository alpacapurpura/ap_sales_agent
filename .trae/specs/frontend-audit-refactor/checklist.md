- [x] `src/app` contiene solo rutas y layouts, sin lógica de negocio inline.
- [x] `src/lib` contiene solo utilidades globales y configuraciones.
- [x] Todas las features en `src/features` siguen la estructura: `components/`, `hooks/`, `types/`, `index.ts`.
- [x] No hay lógica de negocio en `src/components/ui`.
- [x] `src/components/shared` contiene solo componentes reutilizables globalmente.
- [x] `src/hooks` contiene solo hooks genéricos, no de negocio.
- [x] No se detecta duplicidad significativa de código (> 10 líneas idénticas).
- [x] Nombres de funciones y variables son descriptivos y siguen convenciones (camelCase para funciones, PascalCase para componentes).
- [x] Imports son relativos o absolutos de manera consistente (preferiblemente alias `@/`).
- [x] El código compila sin errores de TypeScript (verificado con lint y tsc parcial).
- [x] No hay "any" implícitos o explícitos innecesarios.
