Voy a reestructurar el contenedor de horarios en la página de reserva para que ocupe todo el espacio vertical disponible, solucionando el problema visual de "corte".

### Pasos de Implementación
1.  **Editar archivo**: `src/app/book/[tenant_slug]/[event_slug]/page.tsx`
2.  **Modificar contenedor padre (Columna Derecha)**:
    -   Convertirlo en un contenedor Flex (`flex flex-col`).
    -   Eliminar `space-y-4` para evitar márgenes duplicados (ya que usaremos `mb-4` en el título y Flexbox no colapsa márgenes).
3.  **Modificar lista de horarios**:
    -   Eliminar la restricción `max-h-[400px]`.
    -   Agregar `flex-1` para que la lista se expanda y ocupe todo el espacio restante hasta el fondo.
    -   Asegurar que el scroll se mantenga con `overflow-y-auto`.

Esto permitirá que la lista de horarios se estire dinámicamente según la altura del calendario o el contenedor, llegando visualmente hasta el final como se solicita.