Voy a realizar los siguientes cambios para ajustar el flujo de UX según tus indicaciones:

1. **Backend (`products.py`)**:

   * Actualizaré el modelo `ProductResponse` para que devuelva el campo `landing_page_config`. Esto permitirá al frontend saber si una oferta ya tiene landing page y si está publicada sin necesidad de hacer una petición extra.

2. **Frontend (`offer.ts`)**:

   * Actualizaré la interfaz `Offer` para incluir `landing_page_config`, asegurando que TypeScript reconozca estos datos.

3. **Offer Editor (`offer-editor.tsx`)**:

   * Añadiré un botón **"Landing Page"** en la barra superior del editor de ofertas. Este será el **único punto de entrada** para crear o editar la landing, centralizando la gestión.

4. **Tarjetas del Dashboard (`offer-card.tsx`** **y** **`lead-magnet-stream-card.tsx`)**:

   * **Eliminaré** la opción "Landing Page" / "Generar" del menú de 3 puntos (ya que movimos la creación al editor).

   * **Añadiré** una opción condicional **"Ver Landing Page"** (con icono de enlace externo) que **solo aparecerá** si la landing page

