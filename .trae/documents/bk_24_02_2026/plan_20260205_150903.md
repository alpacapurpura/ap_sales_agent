Entendido. Procederé con la implementación enfocada en la **extensibilidad y documentación para IA**.

**Plan Refinado:**

1.  **Crear Documentación Técnica (`docs/other/offer_polimorfism.md`)**:
    *   Redactaré una guía exhaustiva titulada "Arquitectura Polimórfica de Ofertas".
    *   Explicará: El patrón `Union`, el mapa de tipos (`TYPE_TO_DETAIL_MAP`), cómo funcionan los validadores cruzados y una guía paso a paso para "Cómo agregar un nuevo Tipo de Oferta".
    *   Este documento servirá como "Contexto Maestro" para futuras iteraciones.

2.  **Implementar Código con "AI-Comments" (`backend/src/core/domain/offer/schema.py`)**:
    *   **Mapeo Explícito**: Definiré `OFFER_TYPE_TO_DETAILS_MAPPING` al inicio del archivo.
    *   **Validación Estricta**: Implementaré el validador `validate_consistency` que forzará la coherencia entre `type` <-> `specific_details` y `type` <-> `value_level`.
    *   **Comentarios Guía**: Insertaré bloques de comentarios dirigidos a LLMs (ej: `### AI INSTRUCTION: Add new models here...`) explicando dónde y cómo inyectar nuevos campos sin romper la lógica existente.

3.  **Verificación**:
    *   Me aseguraré de que el código sea válido (python check) y que la documentación sea clara y completa.

¿Procedo con la creación del documento y la refactorización del código?