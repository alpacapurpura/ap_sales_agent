¡Tus sugerencias tienen todo el sentido del mundo! Lejos de romper la UX, la enriquecen significativamente.

1.  **Leyenda Siempre Visible**: Mantener la leyenda activa refuerza el aprendizaje del usuario sobre qué significa cada color, incluso durante una búsqueda. Es una excelente decisión de consistencia.
2.  **Filtrado Multidimensional**: Permitir buscar por "DIY" o "Recurso Gratuito" transforma el buscador de una herramienta simple a un filtro potente. Coincide perfectamente con el modelo mental del usuario.

### Plan de Actualización

**1. Ampliar la Lógica de Filtrado (Dashboard):**
*   Actualizaré el algoritmo de búsqueda en `offer-studio-dashboard.tsx` para que evalúe tres campos en lugar de uno:
    *   `offer.name` (Ej: "Curso de SEO")
    *   `metadata.label` (Ej: "Curso Auto-dirigido")
    *   `offer.delivery_model` (Ej: "DIY")
*   Esto permitirá que si escribes "DIY", aparezcan todas las ofertas de ese modelo, independientemente de su nombre.

**2. Restaurar la Leyenda:**
*   Eliminaré la condición que ocultaba `<OfferLegend />` cuando había búsqueda activa.

**3. Refinar el Resaltado (Card):**
*   Actualizaré `offer-card.tsx` para que el componente `HighlightedText` también se use en el badge del modelo de entrega (DIY/DWY). Así, si buscas "DIY", verás exactamente por qué apareció esa tarjeta.

Procederé a aplicar estos cambios para potenciar la herramienta.