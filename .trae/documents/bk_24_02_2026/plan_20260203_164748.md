Voy a proceder a actualizar tu "base de conocimiento" en el código para integrar las 5 dimensiones que has solicitado.

### 1. Actualización de `offer_enums.py`
Añadiré los nuevos Enums que definen la lógica de negocio extendida:
- **`PaymentPlanType`**: Para manejar estrategias de pago (Contado, Cuotas, Financiación).
- **`AccessDuration`**: Para distinguir entre acceso al contenido y tiempo de soporte.
- **`PrerequisiteType`**: Para tipificar los requisitos (Facturación, Técnico, Nicho).
- **`OnboardingMechanism`**: Para estandarizar qué sucede post-compra.

### 2. Actualización de `offer/schema.py` (Tu clase `Offer`)
Actualizaré la clase `Offer` (y `PricingStructure`) para usar estos nuevos Enums, tal como solicitaste en tu pseudocódigo:
- **En `PricingStructure`**: Añadiré el campo `plan_type` usando el nuevo Enum.
- **En `Offer`**:
    - **Finanzas**: Se mantiene `pricing_options` pero ahora enriquecido.
    - **Acceso**: Añadiré `access_duration` y `support_duration_days`.
    - **Cualificación**: Añadiré `anti_avatar_keywords` y tipificaré mejor `prerequisites`.
    - **Logística**: Añadiré `onboarding_action` y `onboarding_url`.

Esto dejará tu código listo para que el Agente pueda razonar sobre "¿Le ofrezco cuotas?", "¿Le rechazo por falta de nivel?" o "¿Qué paso sigue tras el pago?".

¿Procedo con estos cambios?