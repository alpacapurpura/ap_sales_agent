I will create a Python script `scripts/seed_offers_visionarias.py` to populate the database with realistic offers based on the `visionarias.lat` tenant.

### 1. Script Location & Setup
- Create file: `scripts/seed_offers_visionarias.py`
- Imports:
  - `src.services.database` for `SessionLocal`.
  - `src.services.db.models.business` for `Product`.
  - `src.services.db.models.tenant` for `Tenant`.
  - `src.core.domain.offer_enums` for all Enums (`OfferType`, `OfferValueLevel`, etc.).
  - `src.core.domain.offer.schema` for Pydantic models (used for validation/structuring `specific_details` JSON).

### 2. Data Generation Strategy
I will map the Visionarias website content to the 6 Offer Value Levels:

| Level | Type | Name | Delivery | Details |
|-------|------|------|----------|---------|
| **N0** | `FREE_RESOURCE` | **"Guía: Liberar la Mente"** | `DIY` | PDF Guide, Lead Magnet. |
| **N1** | `TRIPWIRE_OFFER` | **"Pack Meditaciones: Abundancia"** | `DIY` | Low ticket audio pack ($27). |
| **N2** | `GROUP_COACHING` | **"Programa: De Propósito a Prosperidad"** | `DWY` | 8-week cohort, live calls ($997). |
| **N3** | `1ON1_MENTORING` | **"Mentoría Privada: Visión Clara"** | `DWY` | 1:1 Strategy sessions ($2500). |
| **N4** | `PRODUCTIZED_SERVICE` | **"Agencia Visionarias DFY"** | `DFY` | "Done For You" Funnel/Marketing service ($5k/mo). |
| **N6** | `CORPORATE_TRAINING` | **"Visionarias Corporate Leadership"** | `B2B` | Training for companies. |

### 3. Implementation Logic
The script will:
1. **Initialize Session**: Connect to the DB using `SessionLocal`.
2. **Get/Create Tenant**: Fetch the "Visionarias" tenant (slug: `visionarias`) or create a dummy one if missing (to ensure `tenant_id` linkage).
3. **Clean Slate (Optional)**: Delete existing products for this tenant to avoid duplicates (I will ask for confirmation or just check existence). *Decision: I will use `get_or_create` logic based on `internal_sku`.*
4. **Insert Products**:
   - Construct `Product` SQL Alchemy objects.
   - Populate `specific_details` using the Pydantic schemas (dumped to dict).
   - Populate `pricing` JSON.
   - Populate `offer_value_level` and `delivery_model` explicitly.
5. **Commit**: Save changes to the database.

### 4. Verification
- Run the script: `python -m scripts.seed_offers_visionarias`.
- Verify via SQL or by letting you check the Offer Studio frontend.
