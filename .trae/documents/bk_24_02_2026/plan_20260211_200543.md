Voy a corregir la visualización de las imágenes de perfil en la sección "Equipo & Figuras Clave" para asegurar que se muestren correctamente incluso si las URLs son relativas.

**Pasos de implementación:**

1. **Actualizar Componente de Visualización (`TeamSection`)**:

   * Importar y configurar `NEXT_PUBLIC_API_URL`.

   * Modificar la lógica de renderizado del `Avatar` para verificar si la `headshot_url` es relativa (comienza con `/`) o absoluta.

   * Si es relativa, anteponer la URL de la API para que el navegador pueda

