Voy a proceder con la refactorización completa del backend "Offer Studio" siguiendo tu arquitectura polimórfica y requisitos de terminología.

### 1. Refactorización de Enums (Domain Core)

**Archivo:** `backend/src/core/domain/offer_enums.py`
Implementaré los Enums detallados con descripciones ricas para la IA y el Frontend.

* **Nuevos Enums:** `OfferValueLevel` (N0-N6), `OfferDeliveryModel`, `OfferType` (Taxonomía completa), `GuaranteeType`, `OfferStatus`, `DeliverableFormat`, `EventLocationType`, `PaymentPlanType`, `AccessDuration`, `PrerequisiteType`, `OnboardingMechanism`.

* **Metadatos Ricos:** Implementaré un diccionario de mapeo (`METADATA_REGISTRY`) junto a los Enums que vincule cada opción con su `description`, `hint` (para UI), y reglas de negocio (ej. `FREE_RESOURCE` implica `N0` y `DIY`).

### 2. Actualización de Schemas (Pydantic)

**Archivo:** `backend/src/core/domain/offer/schema.py`
Actualizaré el modelo `Offer` para soportar la nueva estructura:

* Integración de `OfferValueLevel` como campo explícito (validado contra `OfferType`).

* Actualización de `PricingStructure` para usar `PaymentPlanType`.

* Inclusión de nuevos campos de "Gatekeeping" y "Onboarding" (`PrerequisiteType`, `OnboardingMechanism`).

* Validación de consistencia (ej. si es `N0`, el precio debe ser 0).

### 3. Persistencia de Datos (SQLAlchemy)

**Archivo:** `backend/src/services/db/models/business.py`
Actualizaré la tabla `products` para reflejar los cambios críticos que requieren indexación o consultas directas:

* **Nueva Columna:** `offer_value_level` (String).

* **Mapeo:** Asegurar que los nuevos campos complejos se guarden correctamente en las columnas `JSONB` existentes (`pricing`, `specific_details`, `prerequisites`).

### 4. Migración de Base de Datos (Alembic)

Crearé una nueva versión de migración para añadir la columna `offer_value_level` a la tabla `products`.

### 5. API de Definiciones (Opcional/Proactivo)

**Archivo:** `backend/src/api/routers/products.py`
Añadiré un endpoint auxiliar (o lógica interna) que permita al Frontend obtener las "Definiciones del Studio" (la lista de tipos, niveles, garantías con sus descripciones y hints) para construir el formulario dinámicamente.

### Pasos de Ejecución:

1. Reescribir `offer_enums.py`.
2. Actualizar `schema.py`.
3. Modificar modelo `Product` en `business.py`.
4. Generar y aplicar migración Alembic.
5. Verificar que el backend levanta correctamente.

