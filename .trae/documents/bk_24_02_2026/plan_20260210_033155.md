Voy a reestructurar la sección de Avatares para que tenga su propio espacio en el Índice y cuente para la salud de la marca ("Brand Health").

### 1. Actualizar el Sidebar de Salud de Marca (`BrandHealthSidebar.tsx`)
*   **Implementar Fetching:** Agregaré una consulta (`useQuery`) para obtener la lista de avatares directamente en este componente, ya que no forman parte del objeto principal `BrandSettings`.
*   **Nueva Métrica:** Añadiré "Avatares" a la lista de verificaciones (`checks`). Si existe al menos un avatar, marcará como completado.
*   **Índice:** Esto hará que "Avatares" aparezca automáticamente en la lista visual del índice del Brand Book.

### 2. Crear Nueva Sección de Visualización (`AvatarsSection.tsx`)
*   Crearé un nuevo componente `AvatarsSection` en `src/features/brand/components/live-preview/`.
*   Este componente mostrará un resumen visual de los avatares configurados (similar a como se muestra el "Equipo").
*   Incluirá un botón "Gestionar Avatares" que abrirá el panel de edición existente.

### 3. Integrar en el Brand Book (`BrandBookPreview.tsx`)
*   **Insertar Sección:** Colocaré la nueva `AvatarsSection` justo después de la sección de "Identidad Visual", dándole la prominencia que solicitas.
*   **Limpieza:** Eliminaré el botón "Avatares" que actualmente está escondido dentro de la sección "Héroe" (Identity), ya que ahora tendrá su propia sección principal.

### 4. Limpieza de Código (`BrandHeroSection.tsx`)
*   Removeré la lógica y botones antiguos de avatares de la sección de identidad para evitar duplicidad.

Esto cumplirá con tus dos requerimientos: visibilidad en el índice y contribución al puntaje de salud de la marca.