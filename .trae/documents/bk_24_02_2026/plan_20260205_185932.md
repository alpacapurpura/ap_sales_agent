# Plan de Corrección de Navegación

He detectado que la ruta de edición está mal configurada en el frontend. Los componentes intentan navegar a `/offer-studio/offer/[id]/edit`, pero la página real está en `/offer-studio/offer/[id]`.

## Pasos para la Solución

1.  **Corregir `offer-card.tsx`**:
    *   Modificar `handleDoubleClick`: cambiar ruta a `/offer-studio/offer/${offer.id}`.
    *   Modificar opción del menú "Editar": cambiar ruta a `/offer-studio/offer/${offer.id}`.

2.  **Corregir `offer-studio-dashboard.tsx`**:
    *   Modificar `handleCreateOffer`: al crear una oferta exitosamente, redirigir a `/offer-studio/offer/${newOffer.id}` (sin `/edit`).

Esto alineará la navegación con la estructura de archivos existente y permitirá acceder correctamente al editor polimórfico que acabamos de implementar.