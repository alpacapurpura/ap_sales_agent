Entendido. Tienes razón, la estructura de mapeo actual es rígida y no escala. Necesitamos implementar tu matriz estratégica completa de **Arquetipos vs Productos** y hacerla flexible para soportar múltiples opciones por producto en el futuro.

### 1. Refactorización del Mapeo de Arquetipos (Backend)
Voy a extraer la lógica de selección de arquetipo del servicio principal y crear un **"Router de Arquetipos"** dedicado (`archetype_router.py`).
- Implementaré exactamente tu matriz de 6 Arquetipos (Squeeze, Event, Flash Offer, Transformer, Velvet Rope, Brochure) mapeada a los `OfferType` específicos que definiste.
- Esto permitirá que `FREE_RESOURCE` use `THE_SQUEEZE` y `HYBRID_MENTORSHIP` use `THE_TRANSFORMER` sin ambigüedades.

### 2. Soporte para Selección Múltiple (Future-Proofing)
Aunque ahora implementaré la opción "por defecto" para cada producto según tu lista, dejaré la estructura lista para soportar `options = get_available_archetypes(offer_type)`.
- Así, en el futuro, cuando tengas 3 variantes de landing para un "Webinar", el sistema podrá devolverlas todas y dejar que el usuario elija.

### 3. Explicación Técnica del Flujo "IA Magic"
Responderé a tu duda sobre el proceso paso a paso:
1.  **Click en "IA Magic"**: Frontend llama a `POST /landing/generate`.
2.  **Router de Arquetipos**: Backend recibe el `OfferType` (ej: `HYBRID_MENTORSHIP`) y consulta el nuevo mapa. -> Decide: `THE_TRANSFORMER`.
3.  **Selector de Prompt**: Backend selecciona el template Jinja2 específico para ese arquetipo (`landing_transformer.j2`).
4.  **Inyección de Contexto**: Se inyectan datos de Brand Studio + Offer Studio en el prompt.
5.  **Generación LLM**: GPT-4o genera el XML con la estructura psicológica correcta (Gancho, Promesa, Bullets, etc.).
6.  **Parseo y Renderizado**: El backend convierte el XML a JSON y el frontend lo pinta en el editor visual.

### Archivos a modificar:
- `backend/src/core/domain/landing_page/archetype_map.py`: Nuevo archivo para tu matriz de lógica.
- `backend/src/services/landing_generator_service.py`: Usar el nuevo mapa y limpiar la lógica hardcodeada.
