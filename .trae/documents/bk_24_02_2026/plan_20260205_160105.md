Voy a implementar la lógica de negocio para `ProgramDetails`, convirtiéndolo en un modelo robusto para gestión de programas educativos (Cohortes y Mentorías).

**Plan de Implementación:**

1.  **Enums de Estructura Educativa (`backend/src/core/domain/offer_enums.py`)**:
    *   `ProgramStructure`: Para definir la rigidez temporal (Fixed Cohort vs Rolling vs Challenge).
    *   `LiveInteractionType`: Para definir el nivel de acceso al experto (Q&A, Workshop, Híbrido).
    *   `CommunityPlatform`: Para saber dónde ocurre la interacción.

2.  **Schema Principal (`backend/src/core/domain/offer/schema.py`)**:
    *   Actualizaré `ProgramDetails` con los campos estratégicos: `structure_type`, fechas (`start_date`, `end_date`), capacidad (`cohort_limit`, `current_enrollment_count`) y lógica de entrega.
    *   Implementaré el validador `@model_validator` para asegurar consistencia temporal (Start < End) y reglas de negocio.

3.  **Documentación (`backend/docs/other/program_details_logic.md`)**:
    *   Crearé la "Guía de Interpretación para Agente IA - ProgramDetails", detallando cómo el agente debe actuar como "Coordinador de Admisiones" (manejo de FOMO, escasez y expectativas).

4.  **Verificación**:
    *   Script de prueba para validar reglas de fechas, límites de cupos y consistencia de estructura.

¿Procedemos?