Voy a crear un documento extenso de arquitectura técnica en `docs/architecture/technical_architecture.md` que servirá como la "biblia" técnica del proyecto.

El documento incluirá:

1.  **Visión General**: Definición del "Monolito Modular" y diagrama de alto nivel.
2.  **Backend (Hexagonal & Clean Architecture)**:
    *   Explicación detallada de las capas: Dominio (`src/core/domain`), Aplicación (`src/services`), y Adaptadores (`src/api`, `src/services/db`).
    *   Arquitectura Agéntica: Desglose del grafo de LangGraph (`src/core/agents`), orquestadores y sub-agentes.
3.  **Frontend (Feature-Sliced Design)**:
    *   Estructura de carpetas mapeada a conceptos FSD: `app` (Routing), `features` (Slices de negocio), `shared` (UI Kit).
    *   Estrategia de gestión de estado (React Query vs Local State).
4.  **Infraestructura (Docker & DevOps)**:
    *   Tabla de servicios (API, Client, Admin, Qdrant, Postgres, Evolution API).
    *   Flujo de datos entre contenedores.
5.  **Patrones de Diseño Identificados**: Repository, Factory, Strategy y Dependency Injection con ejemplos de código del proyecto.

Este documento consolidará toda la información que hemos analizado para que sirva de referencia futura.