Voy a implementar la refactorización profunda de `ProductDetails` siguiendo tu especificación exacta, transformándolo en un esquema inteligente para logística y producto.

**Plan de Implementación:**

1. **Enums Auxiliares (`backend/src/core/domain/offer_enums.py`)**:

   * Agregaré `FulfillmentType` (Direct, External, Physical).

   * Agregaré `DigitalFormat` (PDF, Video, Notion, Physical, etc.).

2. **Schema Principal (`backend/src/core/domain/offer/schema.py`)**:

   * Reemplazaré la clase `ProductDetails` actual con tu nueva definición "Logística y Estratégica".

   * Implementaré los campos de lógica de entrega (`fulfillment_type`, `access_url`), atributos (`estimated_consumption_time_minutes`) y logística física (`sku`, `stock`).

   * Integraré el validador `@model_validator` para imponer las reglas de negocio (ej: Físico requiere shipping=True).

3. **Documentación (`docs/other/product_details_logic.md`)**:

   * Crearé el documento "Guía de Interpretación para Agente IA - ProductDetails" con las instrucciones de cómo razonar sobre el fulfillment y la venta del beneficio.

4. **Verificación**:

   * Ejecutaré un script de prueba para validar que las reglas de negocio (ej: error

