I will implement the requested changes to the Offer Editor to improve the UX and align with the business logic.

### 1. Header Refactoring (`offer-editor.tsx`)
- **Title Change**: Replace the static "Editar Oferta" fallback with the dynamic "Nombre Público" (`public_name`) of the offer.
- **Subtitle Change**: Replace the Offer Type ID with the **Business Name**. I will use the `useOrganization` hook from Clerk to retrieve the current organization's name.

### 2. Identity Step Overhaul (`steps/identity-step.tsx`)
- **Remove SKU**: Delete the "SKU Interno" field as requested.
- **Read-Only Offer Type & Delivery Model**:
  - Replace the editable `Select` inputs with **informative read-only cards**.
  - **Design**: Use a `Card` layout with a `Badge` for the type/model code and a descriptive text explaining its meaning and utility.
  - **Content**:
    - **Delivery Models**: Add clear definitions for DIY (Do It Yourself), DWY (Done With You), DFY (Done For You), and B2B.
    - **Offer Types**: Implement a mapping of descriptions for key offer types (e.g., Tripwire, Mastermind, 1-on-1) to educate the user on their strategic value.

### 3. Technical Implementation
- **Dependencies**: Import `Card`, `Badge` components in `IdentityStep`.
- **Data Fetching**: Use `useOrganization` in `OfferEditor` for the business name context.
- **Clean Up**: Remove unused imports and form fields.

This approach ensures the editor focuses on the "Public Name" and provides helpful context about the structural choices (Type/Model) without risking accidental changes to fixed architectural decisions.