I will restructure the "Vehicle Configuration" section to use independent, stacked cards instead of a nested layout, matching the style of the "Deal" section.

**Steps:**

1. **Unwrap the Container in** **`offer-editor.tsx`**: Remove the outer `<Card>` wrapper around the "Configuración del Vehículo" section. This allows the inner components to render their own independent cards directly in the main layout stack.
2. **Update Other Forms**: Since I'm removing the parent card, I need to ensure the other form types (Product, Service, Subscription, Event) don't lose their card styling. I will wrap each of these forms in their own self-contained `<Card>` component.

   * `product-form.tsx`: Wrap content in `<Card>`.

   * `service-form.tsx`: Wrap content in `<Card>`.

   * `subscription-form.tsx`: Wrap content in `<Card>`.

   * `event-form.tsx`: Wrap content in `<Card>`.
3. **Result**:

   * **Cohorts/Challenges (Program)**: Will now display 3 distinct, vertically stacked cards ("Estructura", "Calendario", "Dinámica"), solving the "heavy" nested look.

   * **Other Offers**: Will maintain their current single-card look, but now structurally independent.

