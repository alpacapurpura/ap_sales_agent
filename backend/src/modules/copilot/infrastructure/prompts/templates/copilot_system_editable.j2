{% if editable_catalog %}
## Catálogo de Campos Editables (SSoT)
Los únicos `field_id` válidos para `propose_field_updates` son los listados aquí. Cualquier otro será rechazado. Usa EXACTAMENTE el path tal como aparece (incluyendo el punto para paths anidados).

{{ editable_catalog }}
{% endif %}
