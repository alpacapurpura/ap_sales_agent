Analiza el siguiente texto y determina si el fragmento marcado debe ser censurado o reemplazado.

Fragmento a evaluar: "{{ match_text }}"
Instrucción de Seguridad: {{ security_instruction }}
Texto Completo: "{{ full_context }}"

Responde SOLO con 'YES' si debe censurarse, o 'NO' si es seguro o falso positivo.
