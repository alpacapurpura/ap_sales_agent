"""Recording layer — payload sanitization + abstract callback handler base.

* :mod:`sanitization` — pure PII redaction + truncation for trace rows.
* :mod:`base_callback_handler` — Template Method abstract for
  agent-specific subclasses (S1+).
"""
