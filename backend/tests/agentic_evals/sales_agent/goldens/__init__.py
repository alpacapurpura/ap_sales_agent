"""Golden scenario dataset package — Story D (synthetic-first eval ground truth).

Path layout:
    goldens/{tenant_slug}/{persona_kind}/{golden_id}.yaml

Schema: `_schema.GoldenScenarioModel` v1 cement.
Migrations: `_schema_migrations.GOLDEN_SCHEMA_MIGRATIONS` parallel registry.
"""
