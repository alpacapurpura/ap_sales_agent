-r requirements-runtime.txt
pytest>=8.0.0
pytest-asyncio>=0.23.5
pytest-cov>=6.0.0
pytest-randomly>=3.15.0
pytest-timeout>=2.3.1
ruff>=0.3.0
mypy>=1.20
factory-boy>=3.3.0
faker>=30.0.0
pip-audit>=2.7.0
# Eval-as-code framework for copilot quality testing (testing-2026-04 plan).
# Dev-only: pyproject sin runtime usage. Stage `dev_builder` del Dockerfile
# lo incluye, stage `final` (prod) NO. Ver docs/domains/copilot/testing-2026-04/01-tooling.md.
deepeval==3.9.7
# PI-12 S1 sales-agent-eval-runner-foundation T-2 (Story B): language
# detection for Capa 3 (output) of the agentic eval harness. Pure-Python
# port of Google's Java lib (MIT, ~1MB, mature). Imported lazily inside
# tests/agentic_evals/sales_agent/runner/assertions.py (T-4) so absence
# does not break default test collection. Decision B5 ratified Chris
# 2026-05-04: keyword count alone too fragile for Spanish regression
# detection if voice profile bug regresses agent to English.
langdetect>=1.0.9

