# --- Global / Root ---
# Quality reports
.jscpd-report/
report/

# Environments (Secrets)
.env
.env.prod
.env.dev
.env.example
*.env
*.env.prod
*.env.dev
*.env.example

# OS & System
.DS_Store
Thumbs.db
.Desktop
.VolumeIcon.icns
._*

# IDEs & Editors
.idea/
.vscode/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Backend (Python/FastAPI) ---
**/__pycache__/
*.py[cod]
*$py.class
backend/.Python
backend/build/
backend/dist/
backend/*.egg-info/
backend/.installed.cfg
backend/*.egg

# Virtual Environments
backend/venv/
backend/.venv/
backend/env/
venv/
.venv/

# Testing & Coverage
.coverage
.coverage.*
coverage.json
htmlcov/
.pytest_cache/
backend/.pytest_cache/

# AI Models & Cache (Large Files)
backend/model_cache/
# Ignore everything under backend/data/ EXCEPT the curated marketing KB
# corpus which is versioned in repo (F10 RAG seeder reads it).
backend/data/*
!backend/data/marketing_kb/
model_cache/

# --- Frontend (Next.js/React) ---
frontend/node_modules/
node_modules/
frontend/.next/
frontend/out/
frontend/build/
frontend/dist/
frontend/.turbo/
frontend/*.tsbuildinfo
frontend/.eslintcache
frontend/storybook-static/

# --- Infrastructure & Data ---
# Docker Volumes & Persistence
data/
# Curated marketing KB corpus is versioned (F10 RAG) — re-include after
# the wildcard data/ rule that ignores Docker volume mounts.
!backend/data/
!backend/data/marketing_kb/
!backend/data/marketing_kb/**
postgres_data/
tmp/
temp/

# Cloudflare
cloudflared.deb
backend/cloudflare/cloudflared*


# # Deployment (Local) - Ignore everything except READMEs and scripts
# despliegue/
# !despliegue/local/
# !despliegue/github_actions/
# !despliegue/**/README.md
# !despliegue/**/*.sh

# --- IDE & AI Tool Caches (not project-specific) ---
.agents/
.trae/
skills-lock.json
.superpowers/

# --- GSD / Planning State (ephemeral, not application code) ---
.planning/

# --- Claude Code worktrees (auto-generated, never commit) ---
.claude/worktrees/

# --- Claude Code session lock (per-session, churns on every start) ---
.claude/scheduled_tasks.lock

# --- Local overrides (personal, not shared) ---
CLAUDE.local.md

# --- Tessl (managed tiles cache) ---
.tessl/

# --- Playwright E2E ---
frontend/test-results/
frontend/playwright-report/
frontend/playwright/.clerk/
frontend/blob-report/

# --- Documentation artifacts (temp only) ---
docs/temp/
# Verification snapshots (contain real metric values, never commit)
backend/scripts/verify/snapshots/
.worktrees/
REVIEW.md
