[project]
name = "nicolify-client-simulator"
version = "0.1.0"
description = "Client simulator for Nicolify sales agent testing"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100
