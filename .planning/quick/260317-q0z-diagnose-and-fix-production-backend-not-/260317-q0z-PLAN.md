---
phase: quick
plan: 260317-q0z
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements-runtime.txt
  - docker-compose.prod.yml
  - docs/ops/production-deployment-checklist.md
autonomous: true
requirements: [HOTFIX-PROD-BOOT]
must_haves:
  truths:
    - "Backend container starts successfully on QEMU Virtual CPU (no X86_V2 requirement)"
    - "Worker and scheduler services run in production alongside the API"
    - "Deployment checklist exists to prevent similar prod-only failures"
  artifacts:
    - path: "backend/requirements-runtime.txt"
      provides: "numpy<2.0 pin before fastembed"
      contains: "numpy<2.0"
    - path: "docker-compose.prod.yml"
      provides: "worker and scheduler service definitions"
      contains: "visionarias_worker"
    - path: "docs/ops/production-deployment-checklist.md"
      provides: "Lessons learned and pre-deploy verification steps"
  key_links:
    - from: "backend/requirements-runtime.txt"
      to: "fastembed"
      via: "numpy<2.0 pin must appear BEFORE fastembed line"
      pattern: "numpy<2\\.0[\\s\\S]*fastembed"
    - from: "docker-compose.prod.yml"
      to: "docker-compose.yml"
      via: "worker/scheduler services must mirror dev compose"
      pattern: "arq src\\.modules\\.analytics\\.workers"
---

<objective>
Document the production backend boot failure diagnosis and fixes, and create a deployment checklist to prevent recurrence.

Purpose: The backend failed to start in production due to two issues: (1) NumPy 2.x requiring X86_V2 CPU instructions not available on the QEMU production server, and (2) worker/scheduler services missing from docker-compose.prod.yml. Both fixes are already applied. This plan documents the incident and creates operational guardrails.

Output: Production deployment checklist document.
</objective>

<execution_context>
@/home/chris/AISALESHT/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/AISALESHT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/requirements-runtime.txt
@docker-compose.prod.yml
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify applied fixes are correct</name>
  <files>backend/requirements-runtime.txt, docker-compose.prod.yml</files>
  <action>
    Verify the two fixes already applied:

    1. **requirements-runtime.txt**: Confirm `numpy<2.0` appears BEFORE `fastembed>=0.2.0` so pip resolves the constraint before fastembed pulls numpy. NumPy 2.x uses X86_V2 CPU instructions (AVX, SSE4.2) that QEMU Virtual CPU 2.5+ does not support (only SSE/SSE2).

    2. **docker-compose.prod.yml**: Confirm `worker` and `scheduler` services exist with:
       - Same image as backend (`ghcr.io/alpacapurpura/visionarias-backend:latest`)
       - Worker command: `arq src.modules.analytics.workers.settings.WorkerSettings`
       - Scheduler command: `arq src.modules.analytics.workers.settings.SchedulerSettings`
       - Both depend on redis + postgres, use internal_net network, env_file .env.prod

    No code changes needed -- just verify correctness of existing changes.
  </action>
  <verify>
    <automated>grep -n "numpy" /home/chris/AISALESHT/backend/requirements-runtime.txt && grep -c "worker\|scheduler" /home/chris/AISALESHT/docker-compose.prod.yml</automated>
  </verify>
  <done>numpy<2.0 pin confirmed before fastembed; worker and scheduler services confirmed in prod compose</done>
</task>

<task type="auto">
  <name>Task 2: Create production deployment checklist</name>
  <files>docs/ops/production-deployment-checklist.md</files>
  <action>
    Create `docs/ops/production-deployment-checklist.md` with:

    **Section 1: Pre-Deploy Checks**
    - Compare docker-compose.yml vs docker-compose.prod.yml services (diff service names)
    - Check for new pip dependencies that may have CPU-specific builds (numpy, scipy, torch, etc.)
    - Verify .env.prod has all vars that .env has
    - Run `docker compose -f docker-compose.prod.yml config` to validate syntax

    **Section 2: Known Production Constraints**
    - Server CPU: QEMU Virtual CPU version 2.5+ (supports SSE/SSE2 only, NO AVX/SSE4.2/X86_V2)
    - numpy MUST stay pinned below 2.0 (2.0+ requires X86_V2 instructions)
    - Any ML/scientific library that ships native wheels should be tested on prod CPU arch

    **Section 3: Post-Deploy Verification**
    - `docker compose -f docker-compose.prod.yml ps` -- all services running
    - `curl -f https://$API_DOMAIN/health` -- backend healthy
    - Check worker/scheduler logs: `docker logs visionarias_worker --tail 20`
    - Verify ETL runs: check Redis for queued/completed jobs

    **Section 4: Incident Log**
    - 2026-03-17: Backend failed to start. Root cause: numpy 2.x pulled by fastembed requires X86_V2 CPU instructions. Fix: pin numpy<2.0 in requirements-runtime.txt.
    - 2026-03-17: Worker/scheduler not running in prod. Root cause: services existed in docker-compose.yml but were never added to docker-compose.prod.yml. Fix: added both services.

    Keep the document concise and actionable (not a wall of text).
  </action>
  <verify>
    <automated>test -f /home/chris/AISALESHT/docs/ops/production-deployment-checklist.md && wc -l /home/chris/AISALESHT/docs/ops/production-deployment-checklist.md</automated>
  </verify>
  <done>Deployment checklist exists with pre-deploy checks, known constraints, post-deploy verification, and incident log</done>
</task>

</tasks>

<verification>
- numpy<2.0 pin present and correctly positioned in requirements-runtime.txt
- worker and scheduler services present in docker-compose.prod.yml
- Production deployment checklist document created at docs/ops/
</verification>

<success_criteria>
- All production fixes verified as correctly applied
- Deployment checklist document exists with actionable pre/post deploy steps
- Incident documented for future reference
</success_criteria>

<output>
After completion, create `.planning/quick/260317-q0z-diagnose-and-fix-production-backend-not-/260317-q0z-SUMMARY.md`
</output>
