---
phase: quick
plan: 260317-i9e
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/offer-studio/components/editor/sections/program-details/__tests__/program-form.test.tsx
autonomous: true
requirements: [QUICK-CI-CLEAN]

must_haves:
  truths:
    - "npm run lint exits 0 inside frontend container (no ESLint errors or warnings)"
    - "npm run test exits 0 inside frontend container (all vitest tests pass)"
    - "No uncommitted frontend changes remain"
  artifacts: []
  key_links: []
---

<objective>
Commit the remaining uncommitted test fix (program-form.test.tsx) from the prior CI verification task and confirm the frontend CI quality gates (lint + test) pass cleanly.

Purpose: Prior quick task 260317-h91 fixed all 13 ESLint issues (2 errors, 11 warnings). Verification task 260317-hrh confirmed lint and tests pass but left one test file uncommitted. This plan commits that file and does a final CI gate check.
Output: Clean CI quality gates with all changes committed.
</objective>

<execution_context>
@/home/chris/AISALESHT/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/AISALESHT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260317-h91-fix-frontend-eslint-errors-blocking-gith/260317-h91-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit remaining test fix and verify CI gates pass</name>
  <files>frontend/src/features/offer-studio/components/editor/sections/program-details/__tests__/program-form.test.tsx</files>
  <action>
1. Commit the uncommitted program-form.test.tsx file. It has two changes from the verification task:
   - Updated `defaultValues` to include `specific_details` with `structure_type` and `schedule` (matches current ProgramDetailsForm contract)
   - Changed expected header text from "Estructura y Admision" to "Diseno del Programa" (matches current component)

2. Run both CI quality gate commands inside the frontend Docker container to confirm clean:
   ```
   docker exec -t visionarias_client_dev npm run lint
   docker exec -t visionarias_client_dev npm run test
   ```

3. Both must exit 0. If either fails, diagnose and fix before committing.
  </action>
  <verify>
    <automated>docker exec -t visionarias_client_dev npm run lint && docker exec -t visionarias_client_dev npm run test</automated>
  </verify>
  <done>program-form.test.tsx committed, lint exits 0 with no errors/warnings, all vitest tests pass (60/60)</done>
</task>

</tasks>

<verification>
Both CI quality-gate commands exit 0:
1. `docker exec -t visionarias_client_dev npm run lint` -- 0 errors, 0 warnings
2. `docker exec -t visionarias_client_dev npm run test` -- 12/12 test files, 60/60 tests pass
3. `git status` shows no uncommitted frontend changes
</verification>

<success_criteria>
- All ESLint errors and warnings from CI are resolved (already done in 260317-h91)
- Remaining test fix is committed
- Frontend lint and test pass cleanly in Docker container
- No uncommitted frontend changes remain
</success_criteria>

<output>
After completion, create `.planning/quick/260317-i9e-fix-frontend-eslint-errors-blocking-gith/260317-i9e-SUMMARY.md`
</output>
