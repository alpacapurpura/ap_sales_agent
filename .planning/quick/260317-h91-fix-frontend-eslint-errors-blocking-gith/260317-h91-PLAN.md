---
phase: quick
plan: 260317-h91
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/not-found.tsx
  - frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx
  - frontend/src/features/brand/sections/authority/authority-list.tsx
  - frontend/src/features/brand/sections/authority/authority-preview.tsx
  - frontend/src/features/brand/sections/gallery/gallery-manager.tsx
  - frontend/src/features/connections/components/meta-view.tsx
  - frontend/src/features/offer-studio/components/editor/components/offer-gallery-section.tsx
  - frontend/src/features/offer-studio/components/editor/sections/visuals/gallery-preview.tsx
  - frontend/src/features/offer-studio/components/landing/components/blocks/AuthoritySection.tsx
  - frontend/src/features/offer-studio/components/landing/templates/server/SqueezeServerTpl.tsx
  - frontend/src/features/offer-studio/components/landing/templates/server/TransformerServerTpl.tsx
  - frontend/src/features/offer-studio/components/editor/ui/offers-multi-select.tsx
autonomous: true
must_haves:
  truths:
    - "GitHub Actions ESLint step passes with zero errors"
    - "No <a> tags used where Next.js Link should be used"
    - "No <img> tags used where Next.js Image should be used"
  artifacts:
    - path: "frontend/src/app/not-found.tsx"
      provides: "Link import replacing <a>"
    - path: "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx"
      provides: "Link import replacing <a>"
  key_links: []
---

<objective>
Fix all ESLint errors and warnings in the frontend that are blocking GitHub Actions CI.

Purpose: Unblock CI/CD pipeline -- two ESLint errors fail the build, plus 11 warnings that should be cleaned up.
Output: All affected files updated, ESLint passes cleanly.
</objective>

<execution_context>
@/home/chris/AISALESHT/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/AISALESHT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ESLint errors -- replace HTML anchor tags with Next.js Link</name>
  <files>
    frontend/src/app/not-found.tsx,
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx
  </files>
  <action>
These two files use raw `<a href="...">` for internal navigation, triggering @next/next/no-html-link-for-pages errors that BLOCK CI.

1. **not-found.tsx** (line 5): Replace `<a href="/">` with `<Link href="/">` from `next/link`. Add `import Link from "next/link"` at top.

2. **SalesDetail.tsx** (line 156): Replace `<a href="/offer-studio">` with `<Link href="/offer-studio">`. Add `import Link from "next/link"` at top (check if already imported first -- if so, just replace the tag).

Preserve all existing className and children. Link accepts className directly, no wrapper needed.
  </action>
  <verify>
    <automated>docker exec -t visionarias_client_dev npx next lint --file src/app/not-found.tsx --file src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx 2>&1 | grep -E "error|Error" | grep -v "^$" || echo "PASS: no errors"</automated>
  </verify>
  <done>Both files use Next.js Link component. Zero @next/next/no-html-link-for-pages errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ESLint warnings -- replace img tags with Next.js Image and fix a11y</name>
  <files>
    frontend/src/features/brand/sections/authority/authority-list.tsx,
    frontend/src/features/brand/sections/authority/authority-preview.tsx,
    frontend/src/features/brand/sections/gallery/gallery-manager.tsx,
    frontend/src/features/connections/components/meta-view.tsx,
    frontend/src/features/offer-studio/components/editor/components/offer-gallery-section.tsx,
    frontend/src/features/offer-studio/components/editor/sections/visuals/gallery-preview.tsx,
    frontend/src/features/offer-studio/components/landing/components/blocks/AuthoritySection.tsx,
    frontend/src/features/offer-studio/components/landing/templates/server/SqueezeServerTpl.tsx,
    frontend/src/features/offer-studio/components/landing/templates/server/TransformerServerTpl.tsx,
    frontend/src/features/offer-studio/components/editor/ui/offers-multi-select.tsx
  </files>
  <action>
For each file with `<img>` tags, replace with `<Image>` from `next/image`:

1. Add `import Image from "next/image"` (if not already imported).
2. Replace `<img src={...} ... />` with `<Image src={...} ... />`.
3. For each Image: add `width` and `height` props if not present. Since these are dynamic user-uploaded images (brand galleries, offer galleries, authority logos), use `width={400} height={300}` as reasonable defaults with `className="object-cover"` (most already have object-cover).
4. If the image src is a dynamic external URL (user uploads, CDN), also add `unoptimized` prop to avoid Next.js image optimization domain config issues. Check if `next.config.js` has image domains configured -- if not, use `unoptimized`.
5. Preserve all existing className, alt, style, and event handler props.

**Special cases to watch for:**
- `meta-view.tsx` has TWO img tags (lines 474 and 541) -- fix both.
- Server template files (SqueezeServerTpl.tsx, TransformerServerTpl.tsx) -- these are server components so Image works fine.

**Accessibility fix (offers-multi-select.tsx line 68):**
- The `<div role="combobox">` already has `aria-expanded={open}` (line 69).
- Add `aria-controls="offers-listbox"` to the combobox div.
- Find the associated PopoverContent/CommandList and add `id="offers-listbox"` to the listbox element (the CommandList or first child with role="listbox").
  </action>
  <verify>
    <automated>docker exec -t visionarias_client_dev npx next lint 2>&1 | tail -30</automated>
  </verify>
  <done>All @next/next/no-img-element warnings eliminated. Combobox has proper aria-controls/aria-expanded. Full ESLint run shows zero errors and zero warnings from these files.</done>
</task>

</tasks>

<verification>
Run full frontend ESLint check inside Docker:
```bash
docker exec -t visionarias_client_dev npx next lint
```
Expected: Clean output with no errors and no warnings related to the fixed files.
</verification>

<success_criteria>
- GitHub Actions ESLint step would pass (zero errors)
- All 11 img-element warnings resolved
- Accessibility warning on offers-multi-select resolved
- No regressions (existing functionality preserved -- these are purely tag replacements)
</success_criteria>

<output>
After completion, create `.planning/quick/260317-h91-fix-frontend-eslint-errors-blocking-gith/260317-h91-SUMMARY.md`
</output>
