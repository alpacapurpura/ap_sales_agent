---
quick_id: 260318-e07
description: Fix GitHub Actions CI checks — run all jobs locally, fix failures, push clean
date: 2026-03-18
status: completed
commit: efc3fe7, 275641d
---

# Summary: Fix GitHub Actions CI

## Root Causes (2 issues)

### Issue 1: ESLint rules-of-hooks (frontend lint)
GH Actions run `23250404139` failed at **frontend lint**. ESLint reported `react-hooks/rules-of-hooks` in `Calendar.stories.tsx:16` — `React.useState` called inside an anonymous `render` function.

**Fix:** Extracted to a named `CalendarDemo` component.

### Issue 2: TypeScript build failure (security scan / frontend build)
Run `23251736244` passed lint but failed at **security scan** step during frontend Docker build. Storybook stories used discriminated union types (`Accordion`, `Sheet`) incompatible with Next.js production TypeScript checking.

**Fix:** Added `src/stories` and `.storybook` to `tsconfig.json` `exclude` array. Stories are only consumed by Storybook, never by the Next.js app — they should never be type-checked during `next build`.

## Local CI Verification (all checks passed)

| Check | Result | Details |
|-------|--------|---------|
| Backend lint (ruff) | PASS | 0 errors |
| Backend tests (pytest) | PASS | 278 passed, 59 skipped |
| Frontend lint (eslint) | PASS | 0 errors, 0 warnings |
| Frontend tests (vitest) | PASS | 60 passed across 12 test files |
| Frontend build (next build) | PASS | Full runner image built successfully |

## GitHub Actions Pipeline — FULL GREEN

Run `23252628908` — all 5 jobs passed:

| Job | Status |
|-----|--------|
| Docker Lint & Test Gates | SUCCESS |
| Vulnerability Scan | SUCCESS |
| Build & Push Images | SUCCESS |
| Deploy to VPS | SUCCESS |
| Health Check | SUCCESS |

## Commits
1. `efc3fe7`: fix(frontend): extract CalendarDemo component to fix ESLint rules-of-hooks error
2. `275641d`: fix(frontend): exclude Storybook stories from Next.js production build
