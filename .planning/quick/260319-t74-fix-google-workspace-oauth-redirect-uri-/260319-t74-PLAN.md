---
phase: quick-260319-t74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".env.prod", ".env"]
autonomous: false
requirements: [QUICK-FIX]
must_haves:
  truths:
    - "Google Workspace OAuth flow completes without redirect_uri_mismatch error in production"
    - "GOOGLE_REDIRECT_URI in .env.prod points to /connections (registered in Google Cloud Console)"
    - "Production backend container uses the updated redirect URI"
  artifacts:
    - path: ".env.prod"
      provides: "Corrected GOOGLE_REDIRECT_URI for production"
      contains: "GOOGLE_REDIRECT_URI=https://app.nicolify.com/connections"
    - path: ".env"
      provides: "Corrected GOOGLE_REDIRECT_URI for dev"
      contains: "GOOGLE_REDIRECT_URI=https://dev-app.nicolify.com/connections"
  key_links:
    - from: ".env.prod"
      to: "backend/src/modules/connections/api/google_workspace.py"
      via: "settings.GOOGLE_REDIRECT_URI consumed by google OAuth Flow"
      pattern: "flow\\.redirect_uri = settings\\.GOOGLE_REDIRECT_URI"
---

<objective>
Fix Google Workspace OAuth redirect_uri_mismatch error (HTTP 400) in production by updating
GOOGLE_REDIRECT_URI from `/connections/brand-settings` to `/connections` in both .env.prod and .env,
then deploying the change to the production server.

Purpose: Google OAuth fails in production because the redirect URI in .env.prod is not registered
in Google Cloud Console. The `/connections` page already has a working OAuth callback handler.

Output: Updated env files, restarted production container, documented Google Console step for user.
</objective>

<execution_context>
@/home/chris/AISALESHT/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/AISALESHT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.env.prod (line 95: GOOGLE_REDIRECT_URI)
@.env (line 78: GOOGLE_REDIRECT_URI)
@backend/src/modules/connections/api/google_workspace.py (uses settings.GOOGLE_REDIRECT_URI at lines 79, 101)
@frontend/src/app/connections/page.tsx (OAuth callback handler — handles GOOGLE_OAUTH_SUCCESS postMessage)
@frontend/src/app/connections/brand-settings/page.tsx (legacy callback — same logic but less complete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GOOGLE_REDIRECT_URI in env files</name>
  <files>.env.prod, .env</files>
  <action>
    Update GOOGLE_REDIRECT_URI in both environment files:

    1. In `.env.prod` (line 95): Change `GOOGLE_REDIRECT_URI=https://app.nicolify.com/connections/brand-settings` to `GOOGLE_REDIRECT_URI=https://app.nicolify.com/connections`
    2. In `.env` (line 78): Change `GOOGLE_REDIRECT_URI=https://dev-app.nicolify.com/connections/brand-settings` to `GOOGLE_REDIRECT_URI=https://dev-app.nicolify.com/connections`

    Both the `/connections` and `/connections/brand-settings` pages have OAuth callback handlers, but `/connections` is the standard callback page (handles both Google and Meta). The `/connections/brand-settings` is the legacy one.

    Do NOT change any other env vars. Only touch the GOOGLE_REDIRECT_URI line in each file.
  </action>
  <verify>
    <automated>grep "GOOGLE_REDIRECT_URI" .env.prod .env | grep -v brand-settings</automated>
  </verify>
  <done>Both .env.prod and .env have GOOGLE_REDIRECT_URI pointing to /connections (not /connections/brand-settings). grep confirms no "brand-settings" in GOOGLE_REDIRECT_URI lines.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy updated env to production server</name>
  <files></files>
  <action>
    SSH into the production server and update the running environment:

    1. SSH: `ssh -i /home/chris/.ssh/id_rsa -p 22022 root@161.132.41.191`
    2. Navigate to project directory (likely `/root/ap_sales_agent` or similar — check with `ls /root/`)
    3. Update `.env.prod` on the server: change GOOGLE_REDIRECT_URI to `https://app.nicolify.com/connections`
    4. Restart the backend container: `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d visionarias_brain`
    5. Verify the container is running: `docker ps | grep visionarias_brain`
    6. Verify the env var inside the container: `docker exec visionarias_brain env | grep GOOGLE_REDIRECT_URI`

    The frontend does not need restart — the callback page at `/connections` already exists and handles Google OAuth.
  </action>
  <verify>
    <automated>ssh -i /home/chris/.ssh/id_rsa -p 22022 root@161.132.41.191 "docker exec visionarias_brain env | grep GOOGLE_REDIRECT_URI"</automated>
  </verify>
  <done>Production backend container shows GOOGLE_REDIRECT_URI=https://app.nicolify.com/connections. Container is running and healthy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Updated GOOGLE_REDIRECT_URI to https://app.nicolify.com/connections in both local env files and production server.</what-built>
  <how-to-verify>
    The user MUST add the redirect URI in Google Cloud Console:

    1. Go to https://console.cloud.google.com/apis/credentials
    2. Select the OAuth 2.0 Client ID used by Nicolify
    3. Under "Authorized redirect URIs", add: `https://app.nicolify.com/connections`
    4. (Optional) Remove the old URI `https://app.nicolify.com/connections/brand-settings` if it was there
    5. Click Save

    After adding the URI, test the Google Workspace connection:
    1. Go to https://app.nicolify.com
    2. Navigate to Connections settings
    3. Click "Connect Google Workspace" (or similar)
    4. Complete the OAuth flow — should succeed without redirect_uri_mismatch error
  </how-to-verify>
  <resume-signal>Type "verified" after adding the redirect URI to Google Cloud Console and testing the OAuth flow</resume-signal>
</task>

</tasks>

<verification>
- `grep GOOGLE_REDIRECT_URI .env.prod` shows `https://app.nicolify.com/connections`
- `grep GOOGLE_REDIRECT_URI .env` shows `https://dev-app.nicolify.com/connections`
- Production container env var matches the updated URI
- Google OAuth flow completes without redirect_uri_mismatch (after user adds URI to Google Console)
</verification>

<success_criteria>
Google Workspace OAuth redirect_uri_mismatch error is resolved. Users can connect Google Workspace services (Gmail, Calendar, Analytics) through the production app without HTTP 400 errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260319-t74-fix-google-workspace-oauth-redirect-uri-/260319-t74-SUMMARY.md`
</output>
