---
phase: quick-260319-m95
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/brand/components/navigation/brand-nav-rail.tsx
autonomous: true
requirements: [quick-260319-m95]
must_haves:
  truths:
    - "Brand Studio nav rail no longer shows Roman numeral group titles"
    - "All 12 navigation items remain accessible and functional"
    - "Nav rail is visually shorter and cleaner"
  artifacts:
    - path: "frontend/src/features/brand/components/navigation/brand-nav-rail.tsx"
      provides: "Cleaned up nav rail without group title headers"
  key_links: []
---

<objective>
Remove the Roman numeral group titles ("I. ADN de Marca", "II. Voz", etc.) from the Brand Studio nav rail sidebar to reduce vertical space. The menu has become too long and looks bad with these section headers.

Purpose: The 6 group titles with Roman numerals add unnecessary vertical height to the nav rail, making it overflow and look cluttered. The nav items themselves are self-explanatory with their icons and labels.
Output: Cleaner, shorter nav rail with just the navigation items and subtle visual separators between groups.
</objective>

<execution_context>
@/home/chris/AISALESHT/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/AISALESHT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/features/brand/components/navigation/brand-nav-rail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Roman numeral group titles from nav rail</name>
  <files>frontend/src/features/brand/components/navigation/brand-nav-rail.tsx</files>
  <action>
In `brand-nav-rail.tsx`, remove the group title `div` element that renders `group.title` (lines 135-140). This is the div with classes "px-4 py-1 text-xs font-semibold text-muted-foreground/50 uppercase tracking-wider" that shows "I. ADN de Marca", "II. Voz", etc.

Keep the `navGroups` array structure intact (groups still separate items logically), but remove the title rendering. The groups should still be visually separated by the existing `gap-6` on the parent `nav` flex container, which provides enough spacing between groups without the title text.

Optionally, reduce the gap from `gap-6` to `gap-3` since the titles no longer consume space, and add a subtle 1px separator between groups using `border-t border-muted/20` on groups after the first one (groupIdx > 0) to maintain visual grouping without text labels.

Also remove the `title` property from the `navGroups` array items since it is no longer rendered (clean up dead code).
  </action>
  <verify>
    <automated>cd /home/chris/AISALESHT && grep -c "ADN de Marca\|Voz\|Publico\|Universo Visual\|Validacion Social" frontend/src/features/brand/components/navigation/brand-nav-rail.tsx | grep "^0$"</automated>
  </verify>
  <done>Nav rail renders all 12 navigation items without any Roman numeral group titles. Groups are visually separated by subtle borders or spacing only. The menu is shorter and cleaner.</done>
</task>

</tasks>

<verification>
- `grep -c "I\.\|II\.\|III\.\|IV\.\|V\." frontend/src/features/brand/components/navigation/brand-nav-rail.tsx` returns 0
- All 12 nav item IDs still present: identity, strategy, story, voice, avatars, visuals, gallery, team, testimonials, authority, contact, legal
- No TypeScript errors: `cd frontend && npx tsc --noEmit --pretty 2>&1 | grep brand-nav-rail` returns nothing
</verification>

<success_criteria>
Brand Studio nav rail no longer displays Roman numeral section titles. All navigation items remain functional. The sidebar is visually shorter and cleaner.
</success_criteria>

<output>
After completion, create `.planning/quick/260319-m95-elimina-los-t-tulos-en-n-meros-romanos-e/260319-m95-SUMMARY.md`
</output>
