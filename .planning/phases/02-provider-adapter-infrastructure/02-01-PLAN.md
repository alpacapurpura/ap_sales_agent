---
phase: 02-provider-adapter-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/analytics/domain/enums.py
  - backend/src/modules/analytics/domain/ports.py
  - backend/src/modules/analytics/domain/exceptions.py
  - backend/src/modules/analytics/domain/models.py
  - backend/src/modules/analytics/infrastructure/providers/base.py
  - backend/src/modules/analytics/infrastructure/models/staging_metrics_model.py
  - backend/src/modules/analytics/infrastructure/models/official_metrics_model.py
  - backend/src/modules/analytics/infrastructure/models/extraction_run_model.py
  - backend/src/modules/analytics/infrastructure/models/metric_aggregation_model.py
  - backend/src/modules/analytics/infrastructure/models/__init__.py
  - backend/src/modules/analytics/application/dto/channel_metric_dto.py
  - backend/src/modules/analytics/application/cost_type_mapping.py
  - backend/src/modules/iam/infrastructure/models/tenant_model.py
  - backend/alembic/versions/xxxx_add_etl_tables.py
  - backend/tests/modules/analytics/__init__.py
  - backend/tests/modules/analytics/conftest.py
  - backend/tests/modules/analytics/test_cost_type.py
  - backend/tests/modules/analytics/test_provider_adapter.py
  - backend/tests/modules/analytics/test_connection_port.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04

must_haves:
  truths:
    - "A new provider adapter can be added by implementing one ABC (BaseMetricsProvider) without modifying service or API layers"
    - "Every channel metric DTO carries cost_type, unit, and currency fields"
    - "ConnectionPort ABC is defined in analytics domain with get_credentials() and list_active_connections() methods"
    - "ETL tables exist in PostgreSQL for staging, official, extraction_runs, and aggregations"
    - "TenantModel has an extraction_priority integer field for scheduler ordering"
  artifacts:
    - path: "backend/src/modules/analytics/domain/enums.py"
      provides: "CostType, MetricUnit, ExtractionStatus enums"
      contains: "class CostType"
    - path: "backend/src/modules/analytics/domain/ports.py"
      provides: "ConnectionPort ABC and ConnectionCredentials model"
      exports: ["ConnectionPort", "ConnectionCredentials"]
    - path: "backend/src/modules/analytics/domain/exceptions.py"
      provides: "ConnectionRevokedException, TokenRefreshFailed"
      exports: ["ConnectionRevokedException", "TokenRefreshFailed"]
    - path: "backend/src/modules/analytics/infrastructure/providers/base.py"
      provides: "BaseMetricsProvider ABC and ExtractedMetric model"
      exports: ["BaseMetricsProvider", "ExtractedMetric"]
    - path: "backend/src/modules/analytics/infrastructure/models/staging_metrics_model.py"
      provides: "StagingMetricModel table"
      contains: "staging_metrics"
    - path: "backend/src/modules/analytics/infrastructure/models/official_metrics_model.py"
      provides: "OfficialMetricModel table"
      contains: "official_metrics"
    - path: "backend/src/modules/analytics/infrastructure/models/extraction_run_model.py"
      provides: "ExtractionRunModel table"
      contains: "extraction_runs"
    - path: "backend/src/modules/analytics/application/cost_type_mapping.py"
      provides: "COST_TYPE_MAP dict and get_cost_type() function"
      exports: ["COST_TYPE_MAP", "get_cost_type"]
  key_links:
    - from: "backend/src/modules/analytics/application/dto/channel_metric_dto.py"
      to: "backend/src/modules/analytics/domain/enums.py"
      via: "imports CostType and MetricUnit"
      pattern: "from.*enums import.*CostType"
    - from: "backend/src/modules/analytics/infrastructure/providers/base.py"
      to: "backend/src/modules/analytics/domain/enums.py"
      via: "adapter uses MetricUnit from domain enums for unit validation"
      pattern: "from.*enums import"
---

<objective>
Define all domain contracts, enums, ABC interfaces, database models, and DTOs that the entire ETL infrastructure depends on. This is the foundation layer -- every subsequent plan imports from these files. Also adds extraction_priority field to TenantModel for scheduler ordering.

Purpose: Establish the type system, database schema, and interface contracts so Plans 02 and 03 can implement against stable contracts without guessing shapes.
Output: Domain enums, ConnectionPort ABC, BaseMetricsProvider ABC, 4 SQLAlchemy ETL table models, extended ChannelMetricDTO, cost type mapping, tenant priority field, Alembic migration, and test scaffolding.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-adapter-infrastructure/02-CONTEXT.md
@.planning/phases/02-provider-adapter-infrastructure/02-RESEARCH.md
@.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md
@.planning/phases/01-critical-bug-fixes/01-02-SUMMARY.md

<interfaces>
<!-- Existing codebase interfaces the executor needs -->

From backend/src/modules/connections/domain/enums.py:
```python
class ChannelType(str, Enum):
    TELEGRAM = "telegram"
    WHATSAPP = "whatsapp"
    WHATSAPP_CLOUD = "whatsapp_cloud"
    MANYCHAT = "manychat"
    SHOPIFY = "shopify"
    MAILERLITE = "mailerlite"
    GOOGLE_ANALYTICS = "google_analytics"
    META = "meta"
    FACEBOOK_PAGE = "facebook_page"
    INSTAGRAM_ACCOUNT = "instagram_account"
    META_ADS_ACCOUNT = "meta_ads_account"
    YOUTUBE = "youtube"
    YOUTUBE_ANALYTICS = "youtube_analytics"
    GOOGLE_CALENDAR = "google_calendar"
    GMAIL = "gmail"
```

From backend/src/modules/analytics/application/dto/attraction_dto.py:
```python
class ChannelMetricDTO(BaseModel):
    slug: str
    name: str
    channel_type: str  # ORGANIC_SOCIAL, ORGANIC_SEARCH, PAID_MEDIA
    value: int
    cost: Optional[int] = None
    source_label: str
    connected: bool
```

From backend/src/modules/iam/infrastructure/models/tenant_model.py:
```python
class TenantModel(Base):
    __tablename__ = "tenants"
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name = Column(String, nullable=False)
    slug = Column(String, unique=True, nullable=False)
    timezone = Column(String, server_default="UTC")
    # ... other fields
```

From backend/src/core/database.py:
```python
engine = create_engine(settings.DATABASE_URL)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
redis_client = redis.from_url(settings.REDIS_URL, decode_responses=True)
```

From backend/src/shared/domain/base_entity.py (Base for all models):
- The project uses `Base` from this module for SQLAlchemy declarative base
- All models use UUID primary keys via `Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)`
- Soft deletes use `deleted_at` or `is_active` pattern

From backend/src/shared/infrastructure/database/types.py:
- `EncryptedJSON` custom TypeDecorator exists for transparent encrypt/decrypt of JSONB columns
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain contracts — enums, ports, exceptions, provider ABC</name>
  <files>
    backend/src/modules/analytics/domain/enums.py
    backend/src/modules/analytics/domain/ports.py
    backend/src/modules/analytics/domain/exceptions.py
    backend/src/modules/analytics/domain/models.py
    backend/src/modules/analytics/infrastructure/providers/__init__.py
    backend/src/modules/analytics/infrastructure/providers/base.py
    backend/tests/modules/analytics/__init__.py
    backend/tests/modules/analytics/conftest.py
    backend/tests/modules/analytics/test_cost_type.py
    backend/tests/modules/analytics/test_provider_adapter.py
    backend/tests/modules/analytics/test_connection_port.py
  </files>
  <behavior>
    - CostType enum has 4 values: NEUTRAL, EXPENSE, INVESTMENT, REVENUE (all str enum)
    - MetricUnit enum has 4 values: COUNT, CURRENCY, PERCENTAGE, RATIO
    - ExtractionStatus enum has 5 values: PENDING, RUNNING, SUCCESS, FAILED, RETRYING
    - ConnectionPort is ABC with two abstract methods: get_credentials(tenant_id, channel_type) and list_active_connections(tenant_id)
    - ConnectionCredentials is a Pydantic BaseModel with channel_type (str), credentials (dict), config (dict)
    - ConnectionRevokedException and TokenRefreshFailed are custom exceptions inheriting from Exception
    - BaseMetricsProvider is ABC with three abstract methods: extract_metrics(), provider_name(), rate_limit_config()
    - ExtractedMetric is a Pydantic BaseModel with provider, channel_slug, metric_name, value, unit, currency, date, campaign_id, ad_set_id, ad_id, extra
    - A concrete DummyProvider implementing BaseMetricsProvider can be instantiated and its extract_metrics returns List[ExtractedMetric]
  </behavior>
  <action>
    **First write tests (RED), then implement (GREEN).**

    1. Create `backend/tests/modules/analytics/__init__.py` (empty).
    2. Create `backend/tests/modules/analytics/conftest.py` with shared fixtures:
       - `test_tenant_id` (UUID fixture)
       - `mock_credentials` (dict with access_token, refresh_token)
       - `mock_connection_credentials` (ConnectionCredentials instance)
    3. Create `backend/tests/modules/analytics/test_cost_type.py`:
       - Test CostType has exactly 4 members
       - Test MetricUnit has exactly 4 members
       - Test ExtractionStatus has exactly 5 members
       - Test all are str enums (serializable)
    4. Create `backend/tests/modules/analytics/test_provider_adapter.py`:
       - Test BaseMetricsProvider cannot be instantiated (ABC)
       - Test a concrete DummyProvider subclass works
       - Test ExtractedMetric validates required fields
       - Test ExtractedMetric optional fields default correctly (campaign_id=None, extra={})
    5. Create `backend/tests/modules/analytics/test_connection_port.py`:
       - Test ConnectionPort cannot be instantiated (ABC)
       - Test ConnectionCredentials model validates
       - Test ConnectionRevokedException and TokenRefreshFailed are Exception subclasses

    **Then implement:**

    6. Rewrite `backend/src/modules/analytics/domain/enums.py` (currently has only `event.py` placeholder):
       - CostType(str, Enum): NEUTRAL, EXPENSE, INVESTMENT, REVENUE
       - MetricUnit(str, Enum): COUNT, CURRENCY, PERCENTAGE, RATIO
       - ExtractionStatus(str, Enum): PENDING, RUNNING, SUCCESS, FAILED, RETRYING
    7. Create `backend/src/modules/analytics/domain/exceptions.py`:
       - ConnectionRevokedException(Exception) with message and optional channel_type
       - TokenRefreshFailed(Exception) with message and optional provider
    8. Create `backend/src/modules/analytics/domain/ports.py`:
       - ConnectionCredentials(BaseModel) with channel_type: str, credentials: dict, config: dict
       - ConnectionPort(ABC) with abstract async get_credentials(tenant_id: UUID, channel_type: str) -> ConnectionCredentials and abstract async list_active_connections(tenant_id: UUID) -> List[ConnectionCredentials]
    9. Create `backend/src/modules/analytics/domain/models.py`:
       - Keep existing event.py as-is; this file is for domain value objects if needed (can be minimal)
    10. Create `backend/src/modules/analytics/infrastructure/providers/__init__.py` (empty).
    11. Create `backend/src/modules/analytics/infrastructure/providers/base.py`:
        - ExtractedMetric(BaseModel) with all fields per research (provider, channel_slug, metric_name, value: float, unit: str, currency: Optional[str], date: date, campaign_id: Optional[str], ad_set_id: Optional[str], ad_id: Optional[str], extra: dict = {})
        - BaseMetricsProvider(ABC) with abstract async extract_metrics(tenant_id: UUID, credentials: dict, start_date: date, end_date: date) -> List[ExtractedMetric], abstract provider_name() -> str, abstract rate_limit_config() -> dict

    Note: The existing `domain/event.py` should be preserved as-is. The new `enums.py` file replaces the empty/placeholder domain init.
  </action>
  <verify>
    <automated>docker exec -t visionarias_brain_dev pytest tests/modules/analytics/test_cost_type.py tests/modules/analytics/test_provider_adapter.py tests/modules/analytics/test_connection_port.py -x -v</automated>
  </verify>
  <done>
    All 3 test files pass. CostType/MetricUnit/ExtractionStatus enums exist with correct members. ConnectionPort ABC has 2 abstract methods. BaseMetricsProvider ABC has 3 abstract methods. ExtractedMetric validates all fields. Exceptions are importable.
  </done>
</task>

<task type="auto">
  <name>Task 2: ETL database models, tenant priority field, ChannelMetricDTO extension, cost mapping, and Alembic migration</name>
  <files>
    backend/src/modules/analytics/infrastructure/models/__init__.py
    backend/src/modules/analytics/infrastructure/models/staging_metrics_model.py
    backend/src/modules/analytics/infrastructure/models/official_metrics_model.py
    backend/src/modules/analytics/infrastructure/models/extraction_run_model.py
    backend/src/modules/analytics/infrastructure/models/metric_aggregation_model.py
    backend/src/modules/analytics/application/dto/channel_metric_dto.py
    backend/src/modules/analytics/application/cost_type_mapping.py
    backend/src/modules/iam/infrastructure/models/tenant_model.py
    backend/alembic/versions/xxxx_add_etl_tables.py
  </files>
  <action>
    1. Create `backend/src/modules/analytics/infrastructure/models/__init__.py` that imports all 4 models (so Alembic auto-detects them).

    2. Create `staging_metrics_model.py` — StagingMetricModel:
       - Table name: `staging_metrics`
       - Columns: id (UUID PK), tenant_id (UUID, index, NOT NULL), provider (String, NOT NULL, index), channel_slug (String, NOT NULL), metric_name (String, NOT NULL), value (Float, NOT NULL), unit (String, NOT NULL), currency (String, nullable), metric_date (Date, NOT NULL, index), spend (EncryptedJSON, nullable — for monetary encryption), revenue (EncryptedJSON, nullable), campaign_id (String, nullable), ad_set_id (String, nullable), ad_id (String, nullable), extra (JSONB, default={}), extraction_run_id (UUID, nullable, FK to extraction_runs.id), created_at (DateTime with timezone, server_default=func.now())
       - Composite index on (tenant_id, provider, metric_date) for ETL queries
       - Import EncryptedJSON from `src.shared.infrastructure.database.types`
       - Import Base from the project's declarative base (check `src.shared.domain.base_entity` or whichever the project uses)

    3. Create `official_metrics_model.py` — OfficialMetricModel:
       - Table name: `official_metrics`
       - Columns: id (UUID PK), tenant_id (UUID, index, NOT NULL), provider (String, NOT NULL), channel_slug (String, NOT NULL), metric_name (String, NOT NULL), value (Float, NOT NULL), unit (String, NOT NULL), currency (String, nullable), metric_date (Date, NOT NULL, index), spend (EncryptedJSON, nullable), revenue (EncryptedJSON, nullable), campaign_id (String, nullable), ad_set_id (String, nullable), ad_id (String, nullable), cost_type (String, nullable — stores CostType enum value), extra (JSONB, default={}), source_extraction_run_id (UUID, nullable), created_at (DateTime TZ), updated_at (DateTime TZ, onupdate=func.now())
       - Composite index on (tenant_id, channel_slug, metric_date) for dashboard queries

    4. Create `extraction_run_model.py` — ExtractionRunModel:
       - Table name: `extraction_runs`
       - Columns per RESEARCH: id, tenant_id (index), provider, status (default="pending"), started_at, completed_at, error (Text, nullable), metrics_count (Integer, default=0), duration_seconds (Float, nullable), rows_extracted (Integer, default=0), rate_limit_headroom (Float, nullable), created_at
       - Composite index on (tenant_id, provider, status)

    5. Create `metric_aggregation_model.py` — MetricAggregationModel:
       - Table name: `metric_aggregations`
       - Columns: id (UUID PK), tenant_id (UUID, index, NOT NULL), channel_slug (String, NOT NULL), metric_name (String, NOT NULL), period_type (String, NOT NULL — "daily", "weekly", "monthly", "last_30_days"), period_start (Date, NOT NULL), period_end (Date, NOT NULL), value (Float, NOT NULL), unit (String, NOT NULL), currency (String, nullable), cost_type (String, nullable), computed_at (DateTime TZ), extraction_run_id (UUID, nullable)
       - Composite index on (tenant_id, channel_slug, period_type, period_start)

    6. **Add extraction_priority field to TenantModel** (per user decision: "Priority by plan: Infrastructure for tenant priority. Field on tenant model even if plans don't exist yet"):
       - Read `backend/src/modules/iam/infrastructure/models/tenant_model.py` first
       - Add `extraction_priority = Column(Integer, server_default="0", nullable=False)` after the `timezone` field
       - This field will be used by the scheduler (02-03) to order tenant extraction (higher priority = extracted first)
       - Comment: `# ETL extraction priority: higher value = extracted first (premium tenants)`

    7. Extend `channel_metric_dto.py` — Keep existing ChannelMetricDTO fields but ADD:
       - cost_type: Optional[str] = None (maps to CostType enum value)
       - unit: str = "count" (maps to MetricUnit enum value)
       - currency: Optional[str] = None
       - value field: change type from int to float for consistency with ETL precision
       - cost field: change type from Optional[int] to Optional[float]
       - Add new field: last_updated: Optional[str] = None (ISO timestamp of last ETL extraction)
       - Keep backward compatibility — all new fields have defaults

    8. Create `cost_type_mapping.py`:
       - Import CostType from domain enums
       - COST_TYPE_MAP: dict[tuple[str, str], CostType] mapping (channel_slug, stage_slug) pairs
       - Populate with initial mappings from RESEARCH: meta-ads/attraction=INVESTMENT, google-ads/attraction=INVESTMENT, ig-organic/attraction=NEUTRAL, tiktok-ads/attraction=INVESTMENT, manychat/capture=EXPENSE, shopify/sales=REVENUE, etc.
       - get_cost_type(channel_slug: str, stage_slug: str) -> CostType | None function that logs warning for unmapped pairs

    9. Generate Alembic migration:
       - Run `docker exec -t visionarias_brain_dev alembic revision --autogenerate -m "add_etl_infrastructure_tables_and_tenant_priority"`
       - Verify the migration includes all 4 ETL tables AND the `extraction_priority` column on `tenants`
       - If autogenerate doesn't detect (models not imported by Alembic env), manually register models in `alembic/env.py` target_metadata
       - Run `docker exec -t visionarias_brain_dev alembic upgrade head` to apply

    IMPORTANT: Check what Base class the project uses for SQLAlchemy models before writing. Look at existing models like `ChannelConnectionModel` to follow the exact same import pattern. The project uses SQLAlchemy 2.0 syntax.
  </action>
  <verify>
    <automated>docker exec -t visionarias_brain_dev alembic upgrade head && docker exec -t visionarias_brain_dev python -c "from src.modules.analytics.infrastructure.models import StagingMetricModel, OfficialMetricModel, ExtractionRunModel, MetricAggregationModel; print('All 4 ETL models importable')" && docker exec -t visionarias_brain_dev python -c "from src.modules.analytics.application.cost_type_mapping import get_cost_type, COST_TYPE_MAP; print(f'Cost map has {len(COST_TYPE_MAP)} entries'); assert get_cost_type('meta-ads', 'attraction') is not None" && docker exec -t visionarias_brain_dev python -c "from src.modules.iam.infrastructure.models.tenant_model import TenantModel; assert hasattr(TenantModel, 'extraction_priority'); print('TenantModel has extraction_priority field')"</automated>
  </verify>
  <done>
    4 ETL tables created in PostgreSQL via Alembic migration. All models importable. ChannelMetricDTO has cost_type, unit, currency, last_updated fields. COST_TYPE_MAP has initial entries and get_cost_type returns correct values for known pairs and None+warning for unknown pairs. TenantModel has extraction_priority integer field (default 0).
  </done>
</task>

</tasks>

<verification>
- All domain contracts (enums, ABCs, exceptions) importable from their module paths
- All 4 SQLAlchemy models importable and registered with Alembic
- Alembic migration applies cleanly: `docker exec -t visionarias_brain_dev alembic upgrade head`
- Test suite passes: `docker exec -t visionarias_brain_dev pytest tests/modules/analytics/ -x -v`
- ChannelMetricDTO backward compatible (existing code still works since new fields have defaults)
- TenantModel.extraction_priority exists and defaults to 0
</verification>

<success_criteria>
- CostType, MetricUnit, ExtractionStatus enums exist with correct members
- ConnectionPort ABC defines get_credentials() and list_active_connections()
- BaseMetricsProvider ABC defines extract_metrics(), provider_name(), rate_limit_config()
- 4 ETL tables exist in PostgreSQL: staging_metrics, official_metrics, extraction_runs, metric_aggregations
- ChannelMetricDTO carries cost_type, unit, currency fields
- COST_TYPE_MAP maps (channel, stage) -> CostType for all initial channels
- TenantModel has extraction_priority field for scheduler ordering
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-adapter-infrastructure/02-01-SUMMARY.md`
</output>
