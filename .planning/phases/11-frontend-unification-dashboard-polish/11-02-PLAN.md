---
phase: 11-frontend-unification-dashboard-polish
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: [
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/CaptureDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/NurtureDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/OpportunityDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AdoptionDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/ExpansionDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/EvangelizationDetail.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx",
  "frontend/src/features/marketing-studio/components/metrics-dashboard/sidebar/SidebarContent.tsx",
  "frontend/src/features/marketing-studio/lib/channelIcons.ts"
]
autonomous: true
requirements: [UI-01, UI-04]
must_haves:
  truths:
    - "All 8 detail panels follow identical wrapper structure (header KPIs, MiniFunnel, content, bottleneck banner)"
    - "Channel icons are professional brand icons (not emoji placeholders)"
    - "Clicking any metric opens sidebar with campaign-level detail"
    - "ChannelRow components consistently styled across all 8 panels"
    - "Responsive layouts work on mobile, tablet, and desktop breakpoints"
    - "Micro-interactions: hover scales, count-up animations, fade-ins, skeleton shimmer"
  artifacts:
    - path: "frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/*.tsx"
      provides: "8 detail panels with consistent wrapper, real data integration, onClick handlers"
      min_lines: "150 per panel"
    - path: "frontend/src/features/marketing-studio/components/metrics-dashboard/sidebar/SidebarContent.tsx"
      provides: "Polymorphic sidebar content adapter for per-metric drill-down"
      min_lines: 120
    - path: "frontend/src/features/marketing-studio/lib/channelIcons.ts"
      provides: "Channel slug to icon component mapping (Meta, Google, TikTok, YouTube, Shopify, Mailerlite, WhatsApp, Manychat, etc.)"
      min_lines: 50
    - path: "frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx"
      provides: "Updated ChannelRow with brand icons, onClick handler for sidebar, responsive styling"
      min_lines: 120
  key_links:
    - from: "All 8 detail-panels/*.tsx"
      to: "DetailSkeleton, DetailEmpty, DetailError (Plan 11-01)"
      via: "Wrapper component pattern using isLoading/error states"
      pattern: "if (isLoading) return <DetailSkeleton>; if (error) return <DetailError>; if (!data) return <DetailEmpty>;"
    - from: "ChannelRow.tsx"
      to: "channelIcons.ts"
      via: "Icon lookup by channelSlug"
      pattern: "const Icon = getChannelIcon(channelSlug); <Icon className='...' />"
    - from: "Detail panels"
      to: "MetricSidebar + SidebarContent (Plan 11-01)"
      via: "onClick on ChannelRow/OfferCard calls onMetricClick(metricData)"
      pattern: "onClick={() => onMetricClick({ stageId, channelSlug, metricName, currentValue })} in ChannelRow"
    - from: "SidebarContent.tsx"
      to: "Each detail panel type (pre-venta, adopcion, expansion, evangelizacion)"
      via: "Polymorphic rendering based on stageId"
      pattern: "switch (stageId) { case 'capture': return <CaptureChannelDetail />; ... }"
---

<objective>
Polish all 8 detail panels with consistent UX, professional channel icons, and action sidebar integration. Add micro-interactions (hover effects, count-up animations, fade-ins). Implement responsive design across mobile/tablet/desktop. Ensure every metric is clickable and opens sidebar with campaign-level detail (per-metric drill-down).

Purpose: Phase 11 must deliver a unified, polished command-and-control dashboard. Each panel must look and feel identical structurally while being specialized per stage type. Every metric is a potential action entry point.

Output: 8 updated detail panels with consistent styling, sidebar integration points, and professional channel icons. SidebarContent adapter for polymorphic metric drill-down. channelIcons library for instant brand recognition.
</objective>

<execution_context>
@/home/chris/AISALESHT/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/AISALESHT/.planning/phases/11-frontend-unification-dashboard-polish/11-CONTEXT.md
@/home/chris/AISALESHT/.planning/phases/11-frontend-unification-dashboard-polish/11-UI-SPEC.md
</execution_context>

<context>
@/home/chris/AISALESHT/.planning/PROJECT.md
@/home/chris/AISALESHT/.planning/ROADMAP.md
@/home/chris/AISALESHT/.planning/phases/11-frontend-unification-dashboard-polish/11-01-PLAN.md

## Panel Consistency Template (from UI-SPEC.md)

All 8 panels follow this structure:
```
DetailPanel
├── CardHeader (pb-3)
│   ├── CardTitle "Detalle: {stageName}" (text-lg)
│   └── Timestamp "Actualizado: {lastUpdated}" (text-xs muted italic)
├── CardContent (space-y-4)
│   ├── 3-column Header KPIs grid (lg:grid-cols-3, md:grid-cols-2, sm:flex gap-4)
│   │   └── KpiCard × 3 (with KpiTooltip for hints)
│   ├── MiniFunnel (stage N-1 → stage N = X%)
│   ├── Content area (specialized per stage)
│   │   ├── Pre-venta (0-4): ChannelGroup + ChannelRow ×N
│   │   ├── Adopcion (5): HealthBar + OfferHealthCard ×N
│   │   ├── Expansion (6): ExpansionGroup + ExpansionOfferRow ×N
│   │   └── Evangelizacion (7): NpsSummaryCard + EvangelistCard + CandidatosBanner
│   ├── BottleneckBanner (if applicable)
│   └── Available/unconnected channels (if applicable)
```

## Micro-Interactions (from UI-SPEC.md)

| Interaction | Trigger | Animation | Duration | Easing |
|-------------|---------|-----------|----------|--------|
| Stage card hover | mouseenter | scale(1.02) + shadow | 150ms | ease-out |
| Skeleton shimmer | load state | gradient sweep left-to-right | 1.5s | linear, infinite |
| KPI count-up | data arrival | number 0 → value | 600ms | ease-out |
| Panel fade-in | activeStage change | opacity 0 → 1 | 200ms | ease-in |
| ChannelRow hover | mouseenter | bg-primary/5 + subtle lift | 100ms | ease-out |
| Sidebar slide-in | metric click | translateX -100% → 0% | 250ms | ease-in-out |
| Bottleneck banner pulse | render (critical) | border-red glow pulse every 2s | infinite | ease-in-out |

## Channel Icons (from UI-SPEC.md & UI-04)

Platform Icons mapping (channelIcons.ts):
- Instagram: lucide-react `Instagram` or custom SVG (purple #E4405F)
- Facebook: lucide-react `Facebook` or custom SVG (blue #1877F2)
- YouTube: lucide-react `Youtube` or custom SVG (red #FF0000)
- TikTok: Custom SVG (not in lucide) — use platform-icons or manual SVG (black #000000)
- Meta Ads: Custom SVG (Meta logo) or lucide-react `Zap` fallback
- Google (GA4, Ads, Search): lucide-react `Search`, `Zap`, or custom Google logo
- Shopify: Custom SVG or lucide-react `ShoppingCart` fallback
- Mailerlite: Custom SVG or lucide-react `Mail` fallback
- WhatsApp: lucide-react `MessageCircle` + green accent (#25D366)
- Manychat: Custom SVG or lucide-react `MessageSquare` fallback

Icon sizing:
- Row icons (ChannelRow): 20×20px, inline with channel name
- Badge icons: 14×14px
- Alert icons: 16×16px
- Metric breakdown icons: 16×16px, muted color

## Existing Stage Color Palette (from UI-SPEC.md Research)

Optional left-border accent on stage cards (if approved):
- Atracción: #3b82f6 (Blue)
- Captura: #8b5cf6 (Purple)
- Nutrición: #ec4899 (Pink)
- Oportunidad: #f97316 (Orange)
- Ventas: #22c55e (Green)
- Adopción: #06b6d4 (Cyan)
- Expansion: #eab308 (Amber)
- Evangelización: #d946ef (Magenta)

Use as optional border-l-4 on panel headers if implemented. If deferred, use primary/secondary only.

## Responsive Breakpoints (from UI-SPEC.md)

- Mobile (<640px): Single-column layout, MetricsDashboard space-y-4
- Tablet (640-1024px): 2-column header KPIs, wrapped ChannelGroup
- Desktop (>1024px): 3-column header KPIs, full-width panels, 400px sidebar

## Existing Components (Reference)

- SalesDetail.tsx: "most comprehensive, good reference" for pattern
- AttractionDetail.tsx: "se ve horrible", needs most polish (use as baseline to improve)
- ChannelGroup, ChannelRow, ConnectionBadge: existing pre-venta components
- HealthBar, OfferHealthCard: adoption-specific components
- ExpansionGroup, ExpansionOfferRow: expansion-specific components
- NpsSummaryCard, EvangelistCard, CandidatosBanner: evangelization-specific components
- KpiTooltip: reusable tooltip component for KPI hints (Spanish)
- BottleneckBanner: shared severity-colored alert
- MiniFunnel: stage N-1 → stage N conversion visual
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create channelIcons library mapping channel slugs to brand icon components</name>
  <files>frontend/src/features/marketing-studio/lib/channelIcons.ts</files>
  <read_first>
    - frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx (how channels are currently displayed; look for CHANNEL_ICONS or similar)
    - frontend/src/features/marketing-studio/types/metrics.ts (channel slug type/enum if exists)
    - package.json (verify lucide-react is installed)
  </read_first>
  <action>
Create a channelIcons.ts library that maps channel slugs to professional brand icons:

1. Import lucide-react icons: `import { Instagram, Facebook, Youtube, Mail, MessageCircle, MessageSquare, ShoppingCart, Search, Zap } from 'lucide-react'`

2. Define getChannelIcon() function:
```typescript
export function getChannelIcon(channelSlug: string): React.ComponentType<any> {
  switch (channelSlug) {
    case 'instagram-organic':
    case 'instagram':
      return Instagram;  // or custom SVG component
    case 'facebook-organic':
    case 'facebook':
      return Facebook;
    case 'youtube-organic':
    case 'youtube':
      return Youtube;
    case 'tiktok-organic':
    case 'tiktok':
      return TikTokIcon;  // custom SVG component (TikTok not in lucide)
    case 'meta-ads':
    case 'meta':
    case 'facebook-ads':
      return Zap;  // or custom Meta logo SVG
    case 'google-ads':
    case 'google-search':
    case 'ga4-search':
      return Search;  // or custom Google logo
    case 'shopify':
    case 'shopify-checkout':
      return ShoppingCart;  // or custom Shopify logo
    case 'mailerlite':
    case 'mailerlite-newsletter':
      return Mail;  // or custom Mailerlite logo
    case 'whatsapp':
    case 'whatsapp-inbound':
      return MessageCircle;  // with green color accent
    case 'manychat':
    case 'manychat-messenger':
      return MessageSquare;  // or custom Manychat logo
    case 'cold-contact':
    case 'outbound':
      return Search;  // or custom icon
    default:
      return Search;  // generic fallback
  }
}
```

3. Define getChannelColor() function to return brand color hex:
```typescript
export function getChannelColor(channelSlug: string): string {
  switch (channelSlug) {
    case 'instagram-organic':
    case 'instagram':
      return '#E4405F';
    case 'facebook-organic':
    case 'facebook':
      return '#1877F2';
    // ... etc for each channel
    default:
      return 'hsl(210 40% 96.1%)';  // muted-foreground from slate
  }
}
```

4. For TikTok and other custom icons not in lucide, create inline SVG components or import from an SVG library (defer if not available; use Zap as fallback)

5. Min 50 lines. Export both getChannelIcon and getChannelColor.

6. Use for ChannelRow icon display in Task 2.
  </action>
  <verify>
    <automated>grep -n "export function getChannelIcon" frontend/src/features/marketing-studio/lib/channelIcons.ts && grep -n "Instagram\|Facebook\|Youtube\|MessageCircle" frontend/src/features/marketing-studio/lib/channelIcons.ts && grep -n "default:" frontend/src/features/marketing-studio/lib/channelIcons.ts</automated>
  </verify>
  <done>channelIcons.ts exports getChannelIcon() and getChannelColor() functions; maps all channel slugs to lucide-react icons with fallbacks; includes brand color hex values</done>
</task>

<task type="auto">
  <name>Task 2: Update ChannelRow component with brand icons, onClick handlers, and responsive styling</name>
  <files>frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx</files>
  <read_first>
    - frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx (current implementation)
    - frontend/src/features/marketing-studio/lib/channelIcons.ts (from Task 1)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ConnectionBadge.tsx (existing pattern)
  </read_first>
  <action>
Update ChannelRow to use brand icons, add metric click handlers, and polish styling:

Props (add to existing):
- `onMetricClick?: (metric: MetricClickData) => void` — callback when user clicks metric value
- Existing props: channel, metrics, connectionState, etc.

Changes:
1. Replace emoji/placeholder icons with getChannelIcon(channel.slug):
```typescript
import { getChannelIcon, getChannelColor } from '@/features/marketing-studio/lib/channelIcons';

const Icon = getChannelIcon(channel.slug);
const iconColor = getChannelColor(channel.slug);

<Icon className="w-5 h-5" style={{ color: iconColor }} />
```

2. Add onClick to metric value elements:
```typescript
<button
  onClick={() => onMetricClick?.({
    stageId,
    channelSlug: channel.slug,
    metricName: 'visitors',  // or whatever metric
    currentValue: metrics.visitors,
  })}
  className="cursor-pointer hover:bg-primary/5 px-1 rounded transition-colors duration-100"
>
  {formatNumber(metrics.visitors)}
</button>
```

3. Add hover micro-interaction:
   - Use Tailwind `hover:bg-primary/5` for subtle highlight
   - Use `transition-all duration-100 ease-out` for smooth effect
   - Add shadow/lift on hover (optional: shadow-sm)

4. Responsive styling:
   - Ensure metrics stack correctly on mobile (use flex flex-col gap-2 on mobile, flex gap-4 on tablet+)
   - Icon size: 20×20 px (w-5 h-5)
   - Font sizes: 14px body, 10px secondary (already established)
   - Metric badges responsive: hide on mobile if needed, show on tablet+

5. ConnectionBadge styling (if inline):
   - Gray badge for "Configurar" / "datos simulados"
   - Position: inline with channel name or right-side

6. Min 120 lines. Update prop types in interface to include onMetricClick.

Styling consistency:
- Use gap-4 for metrics grouping
- Use muted-foreground for secondary text (10px)
- Use primary/5 for hover background (subtle, from slate color system)
- Ensure ChannelRow re-exports all metric display logic (no changes to internal metric math)
  </action>
  <verify>
    <automated>grep -n "getChannelIcon\|getChannelColor" frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx && grep -n "onMetricClick" frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx && grep -n "cursor-pointer\|hover:bg-primary" frontend/src/features/marketing-studio/components/metrics-dashboard/channel-widgets/ChannelRow.tsx</automated>
  </verify>
  <done>ChannelRow uses brand icons from getChannelIcon(), metric values are clickable with onMetricClick callback, hover styling with transition-all, responsive layout on mobile/tablet/desktop</done>
</task>

<task type="auto">
  <name>Task 3: Create SidebarContent polymorphic component for per-metric drill-down detail</name>
  <files>frontend/src/features/marketing-studio/components/metrics-dashboard/sidebar/SidebarContent.tsx</files>
  <read_first>
    - frontend/src/features/marketing-studio/types/metrics.ts (MetricClickData type from Plan 11-01)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/*.tsx (each panel's data structure)
    - frontend/src/features/marketing-studio/api/metrics-api.ts (what data is available per stage)
  </read_first>
  <action>
Create SidebarContent component that renders different detail views based on clicked metric:

Props: `{ metric: MetricClickData | null; stageId: StageId }`

Implementation (polymorphic rendering per stage):
```typescript
export function SidebarContent({ metric, stageId }: Props) {
  if (!metric) return <div className="text-sm text-muted-foreground">Selecciona una métrica</div>;

  switch (stageId) {
    case 'attraction':
      return <AttractionMetricDetail metric={metric} />;
    case 'capture':
      return <CaptureMetricDetail metric={metric} />;
    case 'nurturing':
      return <NurtureMetricDetail metric={metric} />;
    // ... continue for all 8 stages
    default:
      return <DefaultMetricDetail metric={metric} />;
  }
}
```

Each stage-specific component (AttractionMetricDetail, etc.) should render:
1. Metric name + current value (large, bold)
2. Channel info (channel name, status: Connected/Configurar/Crear)
3. Campaign-level detail (if available from API):
   - List of campaigns/offers/evangelists per stage
   - Each with sub-metrics (e.g., campaign name, spend, conversions, ROI)
   - Use shadcn Table or Card grid for layout
4. Placeholder action buttons: "Próximamente: Crear campaña", "Próximamente: Editar config"
5. "Conectar" CTA if channel not connected
6. "Crear" CTA if channel type not created yet

For now (MVP):
- Show metric name + current value
- Show channel connection state (Connected / Conectar / Crear)
- Placeholder for campaign detail (div with "Detalles de campaña proximamente")
- Placeholder action buttons

Min 120 lines total. Future phases will add actual campaign-level APIs and sparklines. Use shadcn components (Card, Button, Badge) for consistent styling.

This is the framework; actual per-metric content (campaign lists, sparklines, etc.) can be added incrementally.
  </action>
  <verify>
    <automated>grep -n "export function SidebarContent" frontend/src/features/marketing-studio/components/metrics-dashboard/sidebar/SidebarContent.tsx && grep -n "switch.*stageId" frontend/src/features/marketing-studio/components/metrics-dashboard/sidebar/SidebarContent.tsx && grep -n "Próximamente\|Conectar" frontend/src/features/marketing-studio/components/metrics-dashboard/sidebar/SidebarContent.tsx</automated>
  </verify>
  <done>SidebarContent.tsx renders polymorphic content per stageId, shows metric name + value + channel connection state, includes placeholder action buttons and "Próximamente" CTAs</done>
</task>

<task type="auto">
  <name>Task 4: Update all 8 detail panels (AttractionDetail through EvangelizationDetail) with consistent wrapper, responsive styling, and onClick handlers</name>
  <files>
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/CaptureDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/NurtureDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/OpportunityDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AdoptionDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/ExpansionDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/EvangelizationDetail.tsx
  </files>
  <read_first>
    - frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx (most comprehensive reference)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx (baseline for polish)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AdoptionDetail.tsx (health bar reference)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/EvangelizationDetail.tsx (evangelist cards reference)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/ui/DetailSkeleton.tsx (from Plan 11-01)
  </read_first>
  <action>
Update each detail panel (8 total) to follow consistent pattern while respecting specialized content:

**Consistent Wrapper Template** (apply to all 8):
```typescript
export function XxxDetail({
  data,
  isLoading,
  error,
  onMetricClick,  // NEW: callback for sidebar
}: {
  data: XxxDetailData | null;
  isLoading: boolean;
  error: Error | null;
  onMetricClick?: (metric: MetricClickData) => void;  // NEW
}) {
  if (isLoading) return <DetailSkeleton isLoading>{/* placeholder */}</DetailSkeleton>;
  if (error) return <DetailError error={error} onRetry={() => {}} lastData={data} />;
  if (!data) return <DetailEmpty stage={{ name: 'Xxx' }} />;

  return (
    <Card className="w-full">
      <CardHeader className="pb-3">
        <CardTitle className="text-lg">Detalle: {data.stageName}</CardTitle>
        <p className="text-xs text-muted-foreground italic">
          Actualizado: {new Date(data.lastUpdated).toLocaleString('es-MX')}
        </p>
      </CardHeader>
      <CardContent className="space-y-4">
        {/* 3-column header KPIs */}
        <div className="grid lg:grid-cols-3 md:grid-cols-2 sm:grid-cols-1 gap-4">
          {data.headerKpis?.map((kpi, i) => (
            <KpiCard key={i} kpi={kpi} />
          ))}
        </div>

        {/* MiniFunnel */}
        {data.miniFunnel && (
          <MiniFunnel from={data.miniFunnel.from} to={data.miniFunnel.to} rate={data.miniFunnel.conversionRate} />
        )}

        {/* Content area (stage-specific) */}
        {/* Use existing components: ChannelGroup + ChannelRow, HealthBar + OfferHealthCard, etc. */}
        {/* Pass onMetricClick to ChannelRow and OfferCard components */}

        {/* BottleneckBanner (if applicable) */}
        {data.bottleneck && <BottleneckBanner bottleneck={data.bottleneck} />}

        {/* Available/unconnected channels (if applicable) */}
      </CardContent>
    </Card>
  );
}
```

**Per-panel changes:**

1. **AttractionDetail**: Add onMetricClick to ChannelRow; use for all visible metrics (visitors, impressions, reach, clicks, engagement). Ensure responsive: mobile single column, tablet 2-col KPIs, desktop 3-col.

2. **CaptureDetail**: Add onMetricClick to ChannelRow; update styling to match AttractionDetail. Polish spacing and typography.

3. **NurtureDetail**: Add onMetricClick to ChannelGroup; ensure group headers and row metrics are consistent. Update icon styling.

4. **OpportunityDetail**: Add onMetricClick; update bottleneck banner colors and animation if critical severity. Ensure responsive.

5. **SalesDetail**: Add onMetricClick to OfferCard (offer-level click). Use as reference for pattern; ensure all 8 follow similar structure.

6. **AdoptionDetail**: Add onMetricClick to OfferHealthCard; update HealthBar styling. Ensure KPI layout responsive.

7. **ExpansionDetail**: Add onMetricClick to ExpansionOfferRow; update styling consistency. Ensure responsive grid.

8. **EvangelizationDetail**: Add onMetricClick to EvangelistCard and NpsSummaryCard. Update evangelist list styling.

**Responsive styling** (all 8 panels):
- Header KPIs: `lg:grid-cols-3 md:grid-cols-2 sm:flex flex-col` with gap-4
- Metric rows: stack on mobile, side-by-side on tablet+
- Sidebar trigger: maintain full-width sheet on mobile, 400px drawer on desktop
- Use Tailwind responsive prefixes (sm:, md:, lg:)

**Micro-interactions** (all 8 panels):
- Skeleton shimmer: use DetailSkeleton from Plan 11-01
- Panel fade-in: add `className="fade-in"` with CSS animation (opacity 0→1, 200ms ease-in)
- ChannelRow hover: `hover:bg-primary/5` + `transition-all duration-100`
- Count-up animation: optional, use if headerKpis values need animated increment (defer if too complex; use stable values for now)

**Error/fallback handling** (all 8 panels):
- Use DetailEmpty with Spanish "Sin datos para este período"
- Use DetailError with retry button
- Show "datos simulados" badge if fallback data used

Min 150 lines per panel (8 files total). Update all ChannelRow/OfferCard/health component props to accept onMetricClick and call it appropriately.
  </action>
  <verify>
    <automated>grep -n "onMetricClick" frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx && grep -n "DetailSkeleton\|DetailEmpty\|DetailError" frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx && grep -n "CardHeader\|CardContent" frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx</automated>
  </verify>
  <verify>
    <automated>for f in frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/{Capture,Nurture,Opportunity,Sales,Adoption,Expansion,Evangelization}Detail.tsx; do echo "=== $f ==="; grep -c "onMetricClick" "$f" 2>/dev/null || echo "0"; done</automated>
  </verify>
  <done>All 8 detail panels updated with consistent CardHeader/CardContent wrapper, use DetailSkeleton/DetailEmpty/DetailError, have onMetricClick prop passed to metric elements, responsive KPI grid (3-col lg, 2-col md, 1-col sm), styled with shadcn components</done>
</task>

<task type="auto">
  <name>Task 5: Wire MetricSidebar and SidebarContent into MetricsDashboard and detail panels</name>
  <files>
    frontend/src/features/marketing-studio/components/metrics-dashboard/MetricsDashboard.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx
  </files>
  <read_first>
    - frontend/src/features/marketing-studio/components/metrics-dashboard/MetricsDashboard.tsx (from Plan 11-01)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/MetricSidebar.tsx (from Plan 11-01)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/sidebar/SidebarContent.tsx (from Task 3)
  </read_first>
  <action>
Wire the MetricSidebar and SidebarContent components into the dashboard orchestrator:

**MetricsDashboard.tsx changes:**
1. Import MetricSidebar and SidebarContent:
```typescript
import { MetricSidebar } from './MetricSidebar';
import { SidebarContent } from './sidebar/SidebarContent';
```

2. Update the sidebar state (already defined in Plan 11-01):
```typescript
const [sidebarMetric, setSidebarMetric] = useState<MetricClickData | null>(null);
const [sidebarOpen, setSidebarOpen] = useState(false);

const handleMetricClick = (metric: MetricClickData) => {
  setSidebarMetric(metric);
  setSidebarOpen(true);
};
```

3. Render MetricSidebar at bottom of MetricsDashboard (after detail panel):
```typescript
<MetricSidebar
  isOpen={sidebarOpen}
  onClose={() => setSidebarOpen(false)}
  metric={sidebarMetric}
  stageId={activeStageId}
>
  <SidebarContent metric={sidebarMetric} stageId={activeStageId} />
</MetricSidebar>
```

4. Pass handleMetricClick to the active detail panel:
```typescript
{activeStageId === 'attraction' && (
  <AttractionDetail data={attractionData} isLoading={attractionLoading} error={attractionError} onMetricClick={handleMetricClick} />
)}
// ... repeat for all 8 stages
```

**Detail Panel integration** (update one example; pattern applies to all 8):
1. Each detail panel already receives onMetricClick prop (Task 4)
2. Pass it down to ChannelRow/OfferCard components:
```typescript
<ChannelRow
  channel={channel}
  metrics={metrics}
  onMetricClick={onMetricClick}
/>
```

3. ChannelRow calls onMetricClick when user clicks metric value (Task 2 already did this)

**Sidebar rendering location:**
- Render MetricSidebar as a sibling to the detail panel container (outside any overflow/scroll container)
- Use portal or CSS z-index to ensure it appears above other content
- On mobile: full-screen sheet overlay
- On desktop: 400px right drawer (shadcn Sheet handles this)

**Test the flow:**
1. Click metric value in detail panel → MetricSidebar opens from right
2. Sidebar shows metric name + current value + "Próximamente" buttons
3. Click close (X button) → sidebar closes

Min changes: Update MetricsDashboard render tree + pass prop through detail panels (mostly already done in Tasks 4). No new files, just integration wiring.
  </action>
  <verify>
    <automated>grep -n "handleMetricClick\|setSidebarMetric" frontend/src/features/marketing-studio/components/metrics-dashboard/MetricsDashboard.tsx && grep -n "MetricSidebar" frontend/src/features/marketing-studio/components/metrics-dashboard/MetricsDashboard.tsx && grep -n "onMetricClick={handleMetricClick}" frontend/src/features/marketing-studio/components/metrics-dashboard/MetricsDashboard.tsx</automated>
  </verify>
  <done>MetricSidebar rendered in MetricsDashboard with SidebarContent; sidebar state (sidebarMetric, sidebarOpen) wired; handleMetricClick passed to all 8 detail panels; sidebar opens/closes on metric click</done>
</task>

<task type="auto">
  <name>Task 6: Add micro-interactions (hover scales, count-up animations, fade-ins, skeleton shimmer) and responsive CSS</name>
  <files>
    frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/AttractionDetail.tsx
    frontend/src/features/marketing-studio/components/metrics-dashboard/StageCard.tsx
  </files>
  <read_first>
    - frontend/src/features/marketing-studio/components/metrics-dashboard/detail-panels/SalesDetail.tsx (existing animation patterns if any)
    - frontend/src/features/marketing-studio/components/metrics-dashboard/StageCard.tsx (current hover state)
    - globals.css (Tailwind config, animation definitions)
  </read_first>
  <action>
Add micro-interactions and responsive polish to stage cards and detail panels:

**Skeleton Shimmer Animation:**
Add to globals.css if not present:
```css
@keyframes shimmer {
  0% { background-position: -1000px 0; }
  100% { background-position: 1000px 0; }
}

.skeleton-loading {
  background: linear-gradient(
    to right,
    hsl(210 40% 96.1%) 0%,
    hsl(210 40% 90%) 50%,
    hsl(210 40% 96.1%) 100%
  );
  background-size: 1000px 100%;
  animation: shimmer 1.5s linear infinite;
}
```

**StageCard Hover Animation:**
Update StageCard to use:
```typescript
className={cn(
  "transition-all duration-150 ease-out",
  isActive ? "ring-2 ring-primary border-primary" : "",
  "hover:scale-105 hover:shadow-md"
)}
```

Or if prefer subtle scale:
```typescript
className={cn(
  "transform transition-all duration-150 ease-out",
  "hover:scale-102 hover:shadow-sm"
)}
```

**ChannelRow Hover:**
Add to ChannelRow (already partially done in Task 2):
```typescript
<div className="hover:bg-primary/5 transition-colors duration-100 px-2 py-1 rounded">
  {/* metric value */}
</div>
```

**Panel Fade-in:**
Add to detail panels after CardContent loads:
```typescript
<CardContent className={cn("space-y-4", "animate-fade-in")}>
  {/* content */}
</CardContent>
```

Add CSS (globals.css):
```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.animate-fade-in {
  animation: fadeIn 200ms ease-in forwards;
}
```

**Count-up Numbers** (optional, defer if complex):
If headerKpis values are large numbers, add count-up animation (use Framer Motion or React Number Format library). For MVP, static values are acceptable.

**Responsive Typography and Spacing:**
- Header KPI label: `text-xs sm:text-sm` (12px on mobile, 14px+ on tablet)
- Metric value: `text-xl sm:text-2xl` (20px on mobile, 28px on tablet+)
- ChannelRow spacing: `gap-2 sm:gap-4` (tighter on mobile, normal on desktop)
- Panel padding: `px-4 sm:px-6 py-4 sm:py-6` (responsive padding)

**Bottleneck Banner Pulse** (if critical severity):
Add to globals.css:
```css
@keyframes pulse-border {
  0%, 100% { border-color: hsl(0 84.2% 60.2%); }
  50% { border-color: hsl(0 84.2% 40%); }
}

.bottleneck-critical {
  animation: pulse-border 2s ease-in-out infinite;
}
```

Apply to BottleneckBanner when severity === 'critical'.

**Test animations:**
1. Load dashboard → DetailSkeleton shimmer on card load
2. Data arrives → fade-in effect
3. Hover stage card → scale + shadow
4. Hover metric value → bg-primary/5 highlight
5. Click metric → sidebar slide-in from right (shadcn Sheet handles this)

Min changes: Add CSS animations to globals.css, update Tailwind classes in components to use transition/hover/animate utilities. Most animations are Tailwind-native or CSS-only (no JS required).
  </action>
  <verify>
    <automated>grep -n "@keyframes\|animation:" frontend/src/styles/globals.css && grep -n "hover:scale\|transition-all\|animate-" frontend/src/features/marketing-studio/components/metrics-dashboard/StageCard.tsx</automated>
  </verify>
  <done>Skeleton shimmer animation defined in globals.css, StageCard has hover:scale + transition, ChannelRow has hover:bg-primary/5, detail panels fade-in, bottleneck banner pulses on critical, responsive typography (sm: prefixes) on all components</done>
</task>

</tasks>

<verification>
Wave 2 verification (depends on Wave 1 completion):

1. **UI-01 Panel Consistency:**
   - All 8 detail panels have CardHeader (with stage name + timestamp)
   - All 8 have 3-column KPI grid (responsive)
   - All 8 have MiniFunnel + content area + BottleneckBanner
   - All 8 use DetailSkeleton, DetailEmpty, DetailError

2. **UI-04 Channel Icons:**
   - getChannelIcon() returns lucide-react or custom SVG component
   - All channel slugs mapped (Instagram, Facebook, YouTube, TikTok, Google, Meta, Shopify, Mailerlite, WhatsApp, Manychat)
   - Icons render 20×20 px in ChannelRow
   - Brand colors applied (Instagram purple, Facebook blue, TikTok black, etc.)

3. **Action Sidebar:**
   - MetricSidebar imports and renders in MetricsDashboard
   - SidebarContent polymorphic rendering per stageId
   - Clicking metric value opens sidebar
   - Sidebar shows metric name + current value + "Próximamente" buttons
   - Sidebar closes on close button

4. **Micro-interactions:**
   - Skeleton shimmer animates (1.5s gradient sweep)
   - StageCard scales on hover
   - ChannelRow highlights on hover (bg-primary/5)
   - Panel fade-in on data load
   - Sidebar slide-in from right on metric click

5. **Responsive Design:**
   - Mobile: single-column layout, MetricsDashboard space-y-4
   - Tablet: 2-column KPIs, wrapped ChannelGroup
   - Desktop: 3-column KPIs, full-width panels, 400px sidebar

Automated test run:
```bash
npm run test -- marketing-studio --run 2>&1 | tail -20
```

Component render check (no errors on screen):
```bash
npm run dev &
# Open http://localhost:3000/growth/metrics/attraction
# Verify: no console errors, stage cards visible, detail panel loads, click metric opens sidebar
```
</verification>

<success_criteria>
- All 8 detail panels follow identical wrapper template (CardHeader/CardContent structure)
- Channel icons are professional brand icons (not emoji)
- Clicking any metric opens MetricSidebar with drill-down detail
- ChannelRow styled consistently across all panels with hover effects
- Responsive layouts work on mobile (single column), tablet (2-col KPIs), desktop (3-col KPIs + sidebar)
- Micro-interactions present: skeleton shimmer, hover scales, fade-in, sidebar slide-in
- All 8 panels use real API data (not hardcoded mock)
- Sidebar renders per-metric detail with "Próximamente" action buttons
- No TypeScript errors or missing imports
- All components use shadcn design system (colors, spacing, typography from UI-SPEC)
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-unification-dashboard-polish/11-02-SUMMARY.md` with:
- Artifact list (all 11 created/modified files)
- UI-SPEC implementation checklist (all design decisions materialized)
- Micro-interaction catalog (animations defined and tested)
- Responsive breakpoint validation (mobile/tablet/desktop tested)
- Sidebar content mapping (which metrics open which sidebar views)
</output>
