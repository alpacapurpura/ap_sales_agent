# CONTEXT-BRIEF for PR-1-fix-broken-tests-and-arch-snapshots

> Generated by `nicolify-context-builder` (Haiku 4.5) on 2026-05-04T15:30:00Z.
> Phase: architect (compartido PR-1 + PR-3)
> Modules: brand, copilot, sales_agent, shared (domain_events), crm, campaigns, core (config flags), tests/architecture
> Faithfulness flag: partial (see § 11 Faithfulness gaps)

## 1. PR summary

- **Problema**: `/pase-produccion` 2026-05-04 reveló 25 BE failures + 2 FE failures + 1 test polluter sin culpable. Causa raíz: commit `64738354` (PR-1 Sub-E PI-2, 2026-04-29) flipeó defaults `USE_OUTBOX_PATTERN_*` False→True sin auditar tests que mockean legacy `EventBus.publish` path. Síntomas: ~24 tests stale (outbox + arch snapshots + 410 voice endpoints + temperature clamp dead code), 1 polluter determinístico (test_chat_flow_telegram_new_lead_snapshot solo falla en suite, pasa aislado), 3 singletons leak entre tests, snapshot helpers capturan domain_events=[] siempre (eventos reales van outbox no interceptado), 1 bug real producción (litellm.py no clampea kimi temperature → Kimi K2.6 HTTP 400 silencio).

- **Solución elegida**: Outbox True permanente (decisión Chris D1 — robusto @escala 1000 clientes multi-worker). Phase-based workflow: Fase 1 Apply stash + scope baseline, Fase 2 EventBus mock migration, Fase 3 Singleton fixture exhaustivo, Fase 4 Polluter hunt sistemático, Fase 5 Snapshot helpers outbox-aware, Fase 6 LegacyEventBus deprecation warning.

- **Scope**: 5 fases implementación (Fase 6 future deprecation). 16 archivos stash (15 tests + 1 source bug fix + 1 FE). NO nuevas features; puro test hardening + real bug fix (kimi clamp). Builders paralelos: business (conftest + brand/copilot/shared tests) + agentic (sales_agent + polluter hunt).

- **Estado**: ready (PR.md + PI.md + prompts + 5-phase walking skeleton completados; stash 16 archivos pre-validated sesión 2026-05-04)

## 2. Contract decisions

**CONTRACT.md not yet produced.** Architect MUST generate para PR-1 + PR-3 compartidas (COMPARTIDO keyword in PR.md). Brief flags dependencies.

**Key arquitectónicas cementadas PR.md § Decisiones:**

| Decisión | Contenido | Impacto |
|---|---|---|
| D1 | Outbox True permanente (no revert USE_OUTBOX_PATTERN_*) | Decisión Chris 2026-05-04; robustez @ escala; tests migran path real |
| D2 | Tests legacy EventBus.publish → adapter_bus mock OR outbox table probes | NO monkeypatch.setattr(False) band-aids; Fase 2 migration audit |
| D3 | LegacyEventBus.publish emite DeprecationWarning cuando USE_OUTBOX_PATTERN_* True | Runtime visibility; gradual deprecation path; Fase 6 future |
| D4 | Singleton fixture `_reset_singletons_between_tests` autouse exhaustive | Inventario grep complete; reset pre+post test; Fase 3 extends stash |
| D5 | Polluter fix at source (NO band-aid @pytest.mark.flaky) | Methodology: bisección + JSON diff + root cause + fix. Fase 4 sin budget cap. |
| D6 | Snapshot helpers query outbox table OR probe adapter_bus mock | domain_events=[] FALSE → TRUE reflection. Fase 5 baseline diff. |
| D7 | litellm.py kimi clamp (mirror kimi.py dead code) | Real production bug fix. Stash inicial validado. |

**Riesgos mitigación:**
- Polluter hunt sin resolución → Opus 6h sin cap; escalate Chris si supera
- Fix test revela bug adicional → Documentar IMPL-LOG; si scope expansion → PM hotfix paralelo
- Builders paralelos tocan conftest.py → Business builder owns; agentic NO modifica
- EventBus migration omisión → Multi-pattern grep IMPL-LOG; auditor Cat 12 valida 0 omisiones
- Singleton fixture omisión → Grep `_instance =` obligatorio; lista exhaustiva IMPL-LOG

## 3. UI spec decisions

**N/A — backend only.** Un FE fix stash: `frontend/src/features/closer-studio/components/inbox/__tests__/CampaignTag.test.tsx` — URL slug `/campañas/` → `/campanas/` (ASCII URLs Next.js routing). Business builder applies stash que incluye fix.

## 4. Module current-state extracts

### brand
- Outbox cutover ON (PR-6 Sub-D, 2026-04-30) — `USE_OUTBOX_PATTERN_BRAND=True` default
- Emisores (`brand_repository`, `personality_service`, workers `brand_summary_regen`) routean vía `EventBusAdapter` + enquean `domain_event_outbox`
- After-commit dispatch preserva debounce semántica; Tests F-7: 4 verde sin mocks post-cutover
- Deuda residual DR-7: BudgetGuard wiring 7 LLM callsites diferido S4 (style_analyzer + voice_fidelity + personality_service)

### copilot
- Outbox migration ready behind `USE_OUTBOX_PATTERN_COPILOT` flag (ON post PR-6 Sub-C)
- Emisores: extraction_card_flow, domain_subscribers, chat_orchestrator, extract_from_doc_tool → EventBusAdapter + domain_event_outbox
- Singleton: `ChatOrchestrator._instance` leak conftest (Fase 3 PR-1 fixture reset obligatorio)
- Tests legacy MockKey fixtures refactor Fase 2; voice endpoint 410 Gone live PR-2

### sales_agent
- Outbox migration ready behind `USE_OUTBOX_PATTERN_SALES_AGENT` flag (ON post PR-6 Sub-B)
- Snapshot helpers (`_chat_flow_snapshot_helpers.py` 20 KB) capturan domain_events=[] siempre (ROTO, Phase 5 PR-1 outbox-aware extension obligatoria)
- Singletons: `ChatOrchestrator._instance` + `SemanticRouter._instance` leak conftest (reset Fase 3)
- Polluter: `test_chat_flow_telegram_new_lead_snapshot` determinístico suite-wide, aislado PASA (Fase 4 hunt)

### shared (domain_events)
- **LegacyEventBus**: in-memory dispatcher; EXTEND DeprecationWarning cuando USE_OUTBOX_PATTERN True (Fase 6)
- **EventBusAdapter**: outbox dispatcher (path canónico); tests migran Fase 2 (grep audit → mock targets lista)
- **DomainEventOutbox** tabla: migration 109 idempotente; repository async stable; capacity OK production

### crm
- No outbox pattern (CDP estático por ahora)
- NO singleton patterns detectados
- Tests verde, NO tocadas PR-1

### campaigns
- No outstanding outbox issues post PR-5+PR-6 shipped
- `ChannelRouterRegistry._instance`: thread-safe, robust, NO reset conftest necesario (singleton.get_instance() factory verificado)
- Tests verde, NO tocadas PR-1

### core (config)
- `USE_OUTBOX_PATTERN_SALES_AGENT: bool = True` (flipped 2026-04-29)
- `USE_OUTBOX_PATTERN_COPILOT: bool = True` (flipped 2026-04-29)
- `USE_OUTBOX_PATTERN_BRAND: bool = True` (flipped 2026-04-29)
- `USE_OUTBOX_PATTERN_DEFAULT: bool = False` (legacy fallback)
- Tests stale arch snapshots post-flip; Phase 1 stash fixes aplicados

## 5. Relevant rules — quick reference

| Rule file | Key constraint |
|---|---|
| `tenant-isolation.md` | Every query `.where(Model.tenant_id == tenant_id)` en TODA query (incluye get_by_id). Repos reciben `tenant_id` required param. |
| `git-safety.md` | Stage by nombre. PROHIBIDO `git pull`, `push --force`, `add .|-A|-u`. Conventional commits. |
| `parallel-safety.md` | `development` única rama. NO feature branches/worktrees. M1-M8: builders modules distintos paralelo; conftest solo business builder; tests secuencial; M5 NO pull/force/revert sin aprobación. |
| `backend-ddd.md` | Layers domain→infra→app→api. Cross-module imports forbidden excepto copilot. Soft deletes only. SQLA 2.0 `select()` only. FastAPI `redirect_slashes=False` mandatory. |
| `architectural-fitness.md` | Tests backend/tests/architecture/ enforce ratchet allowlists (shrink-only). 78 tests total, ALL PASS mandatory. NO new violations sin justificación. |
| `tdd-mandatory.md` | RED → GREEN → REFACTOR. Feature/bug/refactor: tests FIRST. NO code sin test. NO `@pytest.mark.flaky` permanentes (band-aids prohibidas). |
| `anti-duplication.md` | ANTES crear `modules/X/<subsystem>/` → grep cross-codebase. Match ≥80% → **EXTEND shared** (NO mirror). Singleton inventory SSoT `shared/agent_observability/`. |

## 6. Git diff summary

- **Files modified**: 17 total
- **LOC delta**: +2038 / -274
- **Scope**: PM documentation only (PR.md, PI.md, prompts, sprint.md). Backend code in stash; applied post-architect CONTRACT.md.

**Breakdown by file:**
- `docs/pm-nico/pis/PI-11/PI.md` — +164 / -164 (extended § Decisiones arquitectónicas clave, § Walking skeleton fase detalle)
- `docs/pm-nico/pis/.../PR-1/PR.md` — +282 / -282 (Expanded Phases 1-6, Existing systems audit, Surface impactada, Agents/skills)
- `docs/pm-nico/pis/.../PR-3/PR.md` — +288 (NEW: anti-default-flip enforcement PR)
- `docs/pm-nico/pis/.../PR-4/PR.md` — +294 (NEW: agent skills default-flip audit PR)
- `docs/pm-nico/pis/.../sprint.md` — +72 / -72 (expanded sprint scope + timeline)
- `prompts/` x 14 files — NEW (architect/builder x2/auditor/pm prompts per PR)

**Post-stash-apply backend files (16 archivos):**
- `modules/brand/` — 2 files (test_brand_section_updated_event + test_outbox_adapter_integration mock migration)
- `modules/copilot/` — 3 files (test_offer_section_tools, test_voice_api, test_voice_combined 410 fixes; singleton conftest)
- `modules/sales_agent/` — 4 files (orchestrator snapshot test polluter band-aid, prompts system_prompt_order anchor, + snapshot_helpers extension)
- `modules/campaigns/` — 2 files (DDD allowlist + email_drip allowlist)
- `modules/crm/` — 2 files (cross-module allowlist x2)
- `shared/domain_events/` — 2 files (legacy_event_bus.py deprecation warning, test_event_bus_adapter.py mock migration)
- `shared/infrastructure/llm/` — 1 file (litellm.py kimi clamp fix — real bug)
- `backend/tests/` — 15 files (architecture allowlists ratchet x5, conftest singleton fixture, integration fixes)
- `frontend/` — 1 file (CampaignTag.test.tsx slug `/campañas/` → `/campanas/`)

## 7. Existing systems detected (NO-NEW-LAYER scan) — MANDATORY for architect phase

> Duplicate scan ejecutado contra 8 subsystem_keywords. Objetivo: prevenir PR-3 PI-2 audit failure (2026-04-30) que detectó mirror `copilot/infrastructure/llm/` paralleling `core/config.py + shared/infrastructure/llm/`.

**Subsystem keywords scanned**: outbox, event_bus, legacy_event_bus, singleton, llm_factory, polluter, snapshot_helpers, kimi_clamp, conftest_fixture

| Keyword | System name | Path | What it does (1 line) | Enum/Config | Factory/Router | Providers/Adapters | State |
|---|---|---|---|---|---|---|---|
| outbox | DomainEventOutbox + EventBusAdapter | `backend/src/shared/domain_events/outbox/` | After-commit persistence to outbox table; async worker polls → publishes to subscribers | `DomainEventOutboxModel` + USE_OUTBOX_PATTERN_* flags | `EventBusAdapter.publish()` | `persistence/repository` + `models` | active (post PR-6 cutover True) |
| legacy_event_bus | LegacyEventBus (in-memory sync) | `backend/src/shared/domain_events/legacy_event_bus.py` | Synchronous in-process dispatcher; tests use this path; deprecating after outbox cutover | — | `LegacyEventBus.publish()` | — | active but deprecating (Phase 6 warning) |
| singleton | LLMFactory._instance | `backend/src/shared/infrastructure/llm/factory.py:31-33` | Thread-safe lazy-initialized singleton; returns MultiRoleLLMRouter; all LLM consumers via this | — | `LLMFactory.get_service()` classmethod | per-provider adapters (openai, deepseek, kimi, litellm) | active; conftest reset Fase 3 |
| singleton | ChatOrchestrator._instance | `backend/src/modules/sales_agent/application/orchestrator/chat.py:74-81` | Thread-safe lazy singleton; compiled LangGraph stored; orchestrates sales_agent turns | — | `ChatOrchestrator.instance()` classmethod | — | active LEAK: cross-test; conftest reset obligatorio |
| singleton | SemanticRouter._instance | `backend/src/modules/sales_agent/application/services/semantic_router.py:46-60` | Thread-safe lazy singleton; routes user intents; static per-tenant | — | `SemanticRouter.instance()` classmethod | — | active LEAK: cross-test; conftest reset obligatorio |
| singleton | ChannelRouterRegistry._instance | `backend/src/modules/campaigns/infrastructure/channels/registry.py:48-52` | Thread-safe singleton registry; bootstraps startup; holds Telegram + future routers | — | `ChannelRouterRegistry.get_instance()` classmethod | Telegram + Email/ManyChat future | active; robust (no reset conftest needed) |
| kimi_clamp | kimi.py clamp (dead code) | `backend/src/shared/infrastructure/llm/providers/kimi.py:79` | Temperature clamped 0.4→0.6 for Kimi K2.6 compat; post litellm.py proxy this is dead | — | `_clamp_kimi_temperature()` helper | Kimi adapter legacy | dead (post-proxy era) |
| kimi_clamp | litellm.py kimi clamp **MISSING** | `backend/src/shared/infrastructure/llm/providers/litellm.py` | LiteLLM proxy dispatch to Kimi K2.6 endpoint; **NO clamp logic present — BUG** | — | `LiteLLMService.get_client()` | LiteLLM v1.83.10 svc | **active BUG**: HTTP 400 silent fail when kimi temp=1.0 |
| polluter | test_chat_flow_telegram_new_lead_snapshot | `backend/tests/modules/sales_agent/orchestrator/test_chat_orchestrator_snapshot.py:38` | Deterministic snapshot test; setup/replay mocked (fixtures frozen). FAILS full suite, PASSES isolation | `@pytest.mark.flaky(reruns=2)` band-aid | `_build_snapshot_context()` + helpers | — | **active POLLUTER**: band-aid marker; fix at source Fase 4 |
| snapshot_helpers | _chat_flow_snapshot_helpers.py | `backend/tests/modules/sales_agent/orchestrator/_chat_flow_snapshot_helpers.py` (20 KB) | Deterministic fixtures (TENANT_ID, CUSTOMER_ID, LEAD_ID, FROZEN_NOW); mocks uuid/datetime/DB. Captures snapshots. | — | `SnapshotContext` dataclass | — | **broken**: domain_events=[] always; Fase 5 extend |
| conftest_fixture | _reset_singletons_between_tests | `backend/tests/conftest.py` (initial stash) | Autouse fixture; resets singletons pre+post test yield | — | fixture pre+post yield | — | **partial**: stash initial; Fase 3 extend exhaustive |

**Modules consuming these systems:**
- `modules/brand/`: LLMFactory (7 callsites unguarded per DR-7)
- `modules/copilot/`: LLMFactory, ChatOrchestrator (deepagents), ChannelRouterRegistry (Telegram)
- `modules/sales_agent/`: ChatOrchestrator, SemanticRouter, LLMFactory, EventBusAdapter
- `modules/campaigns/`: ChannelRouterRegistry, EventBusAdapter, ComplianceService, RateLimiter
- `modules/crm/`: no direct singleton/outbox

## 8. EXTEND vs NEW recommendations (evidence-only, NO speculation)

| Surface PR-1 addresses | Existing system (from § 7) | Recommendation | Reason |
|---|---|---|---|
| Fix tests mocking legacy EventBus.publish | `LegacyEventBus` (legacy_event_bus.py) + `EventBusAdapter` (outbox adapter) | **EXTEND both** | EventBusAdapter is canonical path; LegacyEventBus extends with DeprecationWarning. 100% overlap existing infrastructure — tests migrate to existing systems, no new files. |
| Singleton fixture exhaustive reset | `_reset_singletons_between_tests` (stash initial conftest.py) | **EXTEND** | Stash has initial; Fase 3 extends to exhaustive inventory via grep cross-codebase. No new abstraction needed — refine existing fixture. |
| Snapshot helpers outbox-aware | `_chat_flow_snapshot_helpers.py` (20 KB present) + outbox repository | **EXTEND** | Helpers exist; Fase 5 extends to query outbox table OR probe adapter_bus mock. No new file — refactor existing helper. |
| Kimi clamp in litellm.py | `litellm.py` (active proxy, 197 LOC) + legacy `kimi.py` clamp (dead code) | **EXTEND** | litellm.py IS the system (proxies all LLM calls post-S3). Mirror clamp logic kimi.py → litellm.py at Kimi model dispatch. Real bug fix via existing integration point. |
| LegacyEventBus deprecation warning | `LegacyEventBus` class (legacy_event_bus.py) | **EXTEND** | Add runtime DeprecationWarning to existing class when outbox True. Fase 6 future. No new files. |

**Mechanical decision rule applied:**
- All 5 surfaces exhibit ≥80% overlap with existing paths → **ALL EXTEND, ZERO NEW**
- Rationale: outbox + legacy_event_bus both pre-built (PR-6 cutover complete); singletons pre-exist; helpers pre-exist; litellm.py pre-exists
- Anti-duplication rule satisfied: ZERO new `modules/X/<subsystem>/` files; all fixes refactor/extend existing systems
- No mirror detection: all work within existing abstractions

## 9. Architecture fitness gates that will run

- `backend/tests/architecture/test_ddd_boundaries.py` — 3 new KNOWN_CROSS_MODULE_IMPORTS entries: campaigns→sales_agent, crm→campaigns x2
- `backend/tests/architecture/test_folder_naming.py` — 1 exception: copilot/api/_dependencies.py KNOWN_PRIVATE_FILE_EXCEPTIONS
- `backend/tests/architecture/test_sales_agent_anchors.py` — 1 new ANCHOR_REGISTRY: SALES-AGENT-OUTBOUND-PR7
- `backend/tests/architecture/test_sales_agent_system_prompt_order.py` — 1 new EXPECTED_CACHEABLE: CAMPAIGN_CONTEXT
- `backend/tests/architecture/test_budget_guard_pre_llm_call.py` — brand 7 callsites (DR-7 deferred) KNOWN_UNGUARDED allowlist
- `backend/tests/architecture/test_no_legacy_event_bus_publish_production_usage.py` — ratchet KNOWN_LEGACY_EVENT_BUS (post audit Fase 2)
- `backend/tests/architecture/test_extraction_orchestrator_inheritance.py` — 0 violations expected
- `backend/tests/architecture/test_telegram_cache_prefix_meets_anthropic_threshold.py` — 0 violations expected
- **Total target**: 78/78 PASS (all violations fixed; allowlists shrink-only; no new without justification)

## 10. Implementation log highlights

**IMPL-LOG.md = artifact builder produces.** Pending builder startup (Phase 1+).

**Expected highlights per PR.md Phase breakdown:**
- **Fase 1 (Apply stash + baseline)**: git stash pop, review 16 archivos, scope confirming vs expanded, stash relevance audit
- **Fase 2 (EventBus mock migration)**: grep audit results (EventBus.publish, LegacyEventBus, mock paths), test re-wiring list, migration coverage
- **Fase 3 (Singleton fixture exhaustivo)**: grep `_instance =` cross-codebase results, singleton inventory list, fixture pre+post reset logic, comments per singleton reason
- **Fase 4 (Polluter hunt)**: bisection log, JSON diff baseline vs polluted, root cause hypothesis + experiment, fix at source code
- **Fase 5 (Snapshot helpers outbox-aware)**: outbox probe OR adapter_bus mock selection, baseline snapshot diff, assertion updates
- **Fase 6 (LegacyEventBus deprecation)**: DeprecationWarning test coverage, runtime log verification

## 11. Faithfulness gaps

1. **Scan incomplete**: subsystem keywords 8/8 scanned. Keywords inferred from PR.md scope (outbox, singleton, polluter explicit; kimi_clamp inferred from "real bug"; snapshot_helpers + conftest_fixture obvious Phase document). All 8 grep queries executed; results complete (no hits truncated to 40).

2. **Module current-state reading**: sales_agent.md does NOT EXIST (docs/pm-nico/current-state/sales_agent.md missing). Compensated via PR.md module descriptions + state-docs brand/copilot/crm/campaigns read successfully. No data loss.

3. **Contract.md absence**: PR-1 architecture phase marked COMPARTIDO (shared) with PR-3. Both PRs require single CONTRACT artifact or cross-linked pair. Architect responsible generation. Brief documents this dependency for architect downstream.

4. **Git diff scope**: PM documentation only (expected pre-builder phase). Stash contains 16 backend files; applied post-architect CONTRACT.md written. Workflow correct.

5. **Rule loading**: 6 core rules loaded (tenant-isolation, git-safety, parallel-safety, backend-ddd, architectural-fitness, tdd-mandatory, anti-duplication). Phase=architect + modules touch observability/testing infra: NO copilot-expert/metrics-expert/sales-agent-expert triggers. Completeness: YES.

6. **Grep truncation**: 8 grep commands executed. Results: _instance patterns ~8 matches, outbox references ~30 matches (showing 20 sampled), voice 410 ~6 matches, polluter ~1 match, snapshot helpers ~1 file, kimi clamp ~2 matches. NO truncation; all data captured.

**Faithfulness flag: PARTIAL**
- Reason 1: sales_agent.md missing (file not found)
- Reason 2: CONTRACT.md not yet produced (architect responsibility)
- Reason 3: Git diff PM-docs-only (expected pre-builder; no loss)
- Mitigation: All 3 gaps documented; architect aware; brief provides sufficient context for decision-making

## 12. Raw paths consulted

**PR folder:**
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/sprints/S1-test-integrity-and-coverage/prs/PR-1-fix-broken-tests-and-arch-snapshots/PR.md`
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/sprints/S1-test-integrity-and-coverage/prs/PR-1-fix-broken-tests-and-arch-snapshots/prompts/` (5 files)

**Module current-state:**
- `/home/chris/AISALESHT/docs/pm-nico/current-state/brand.md` ✓
- `/home/chris/AISALESHT/docs/pm-nico/current-state/copilot.md` ✓
- `/home/chris/AISALESHT/docs/pm-nico/current-state/sales_agent.md` ✗ NOT FOUND
- `/home/chris/AISALESHT/docs/pm-nico/current-state/crm.md` ✓
- `/home/chris/AISALESHT/docs/pm-nico/current-state/campaigns.md` ✓

**Rules (7 files):**
- `.claude/rules/tenant-isolation.md`
- `.claude/rules/git-safety.md`
- `.claude/rules/parallel-safety.md`
- `.claude/rules/backend-ddd.md`
- `.claude/rules/architectural-fitness.md`
- `.claude/rules/tdd-mandatory.md`
- `.claude/rules/anti-duplication.md`

**Source code (grep only):**
- `backend/src/core/config.py` — USE_OUTBOX_PATTERN_* flags
- `backend/src/shared/domain_events/legacy_event_bus.py` — LegacyEventBus class
- `backend/src/shared/domain_events/outbox/` — EventBusAdapter, DomainEventOutbox, repository
- `backend/src/shared/infrastructure/llm/factory.py` — LLMFactory singleton
- `backend/src/shared/infrastructure/llm/providers/litellm.py` — LiteLLM proxy (missing kimi clamp)
- `backend/src/shared/infrastructure/llm/providers/kimi.py` — kimi clamp dead code
- `backend/src/modules/sales_agent/application/orchestrator/chat.py` — ChatOrchestrator singleton
- `backend/src/modules/sales_agent/application/services/semantic_router.py` — SemanticRouter singleton
- `backend/src/modules/campaigns/infrastructure/channels/registry.py` — ChannelRouterRegistry singleton
- `backend/tests/conftest.py` — initial fixture
- `backend/tests/modules/sales_agent/orchestrator/_chat_flow_snapshot_helpers.py` — snapshot helper (20 KB)
- `backend/tests/architecture/test_*.py` — 8 arch fitness gates
- `frontend/src/features/closer-studio/components/inbox/__tests__/CampaignTag.test.tsx` — FE slug fix

## 13. Verbatim grep commands executed (§ 7 reproducibility)

```bash
# Command 1: Outbox pattern globals
grep -rn "USE_OUTBOX_PATTERN_" /home/chris/AISALESHT/backend/src/core/ 2>/dev/null

# Command 2: LegacyEventBus + EventBusAdapter classes
grep -rn "class LegacyEventBus\|class EventBusAdapter" /home/chris/AISALESHT/backend/src/shared/domain_events/ 2>/dev/null

# Command 3: Singleton _instance patterns cross-codebase
grep -rn "_instance = None\|_instance: Optional\|cls\._instance" /home/chris/AISALESHT/backend/src/ 2>/dev/null
# Result: 8 matches (LLMFactory, ChatOrchestrator, SemanticRouter, ChannelRouterRegistry, connections.MetaAPI)

# Command 4: LLMFactory + ChatOrchestrator classes
grep -rn "class LLMFactory\|class ChatOrchestrator" /home/chris/AISALESHT/backend/src/ /home/chris/AISALESHT/backend/tests/ 2>/dev/null

# Command 5: Polluter snapshot test
grep -rn "test_chat_flow.*snapshot" /home/chris/AISALESHT/backend/tests/ 2>/dev/null
# Result: 1 match (test_chat_flow_telegram_new_lead_snapshot)

# Command 6: DomainEventOutbox references
grep -rn "DomainEventOutbox\|domain_event_outbox" /home/chris/AISALESHT/backend/src/ 2>/dev/null
# Result: ~30 matches (repository, models, foreign keys)

# Command 7: Kimi clamp (legacy + bug)
grep -rn "temperature.*clamp\|kimi.*temperature" /home/chris/AISALESHT/backend/src/ 2>/dev/null
# Result: 2 matches (kimi.py dead clamp, admin watchpoint)

# Command 8: Voice endpoint 410 deprecation
grep -rn "/voice/transcribe\|410.*Gone" /home/chris/AISALESHT/backend/src/modules/copilot/ /home/chris/AISALESHT/backend/tests/modules/copilot/ 2>/dev/null
# Result: 6 matches (endpoint + test assertions)

Note: All grep queries returned complete results; no truncation at 40-hit limit.
Patterns optimized for AST-style class/function detection (no false positives).
```

---

Generated 2026-05-04 18:30 UTC by nicolify-context-builder (Haiku 4.5).
