# CONTEXT-BRIEF for PR-3-anti-default-flip-enforcement

> Generated by `nicolify-context-builder` (Haiku 4.5) on 2026-05-04.
> Phase: architect
> Modules: shared (domain_events), tests/architecture
> Faithfulness flag: clean

## 1. PR summary

- **Problema**: `/pase-produccion` 2026-05-04 reveló patrón sin guardrail: default feature flag flip (e.g., `USE_OUTBOX_PATTERN_*` False→True) cambia call path side-effect sin auditar tests asociados → 25 BE failures + polluter no identificable.
- **Raíz**: Commit `64738354` flipeó flags pero tests seguían mockeando path legacy (`LegacyEventBus.publish`) — path muerto cuando flag True.
- **Solución elegida**: Defense in-depth: (1) Regla escrita `.claude/rules/anti-default-flip-audit.md` con 4 steps obligatorios + inventario flags; (2) Arch fitness test `tests/architecture/test_no_legacy_eventbus_mock_when_outbox_on.py` AST walk que bloquea mocks legacy cuando outbox flag True default; (3) bypass mechanism para tests de capability legacy; (4) failure message diagnostic
- **Scope**: 
  - `.claude/rules/anti-default-flip-audit.md` (NEW) — regla + inventario flags side-effect + workflow + anti-patterns
  - `backend/tests/architecture/test_no_legacy_eventbus_mock_when_outbox_on.py` (NEW) — arch fitness AST-based
  - `CLAUDE.md` conditional rule trigger (trigger: tocás `core/config.py`)
  - Bypass mechanism documentado
- **Out-of-scope**: 
  - Update agentes/skills (PR-4)
  - Otros flags (workflow general aplicable; arch fitness cubre solo LegacyEventBus.publish por ahora)
  - TDD rule update (PR-4)
- **Estado**: ready

## 2. Contract decisions

n/a — PR arquitectural, no propone flip de config actual. CONTRACT.md no requerido. Compartido con PR-1 architect (validar dependencies PR-1 post).

## 3. UI spec decisions

n/a — backend only, no FE changes.

## 4. Module current-state extracts

### shared (domain_events)

Módulo `shared/domain_events/` provee:
- **Outbox pattern**: `shared/domain_events/outbox/` (domain entity, infrastructure models, repos, dispatcher ARQ task)
- **EventBusAdapter**: `shared/domain_events/outbox/application/event_bus_adapter.py` — routes publish() a outbox INSERT (cuando flag True) o legacy in-memory (cuando False)
- **LegacyEventBus**: `shared/domain_events/legacy_event_bus.py` — in-memory pubsub (deprecated path)
- **Feature flags**: `core/config.py` § 266-273 — `USE_OUTBOX_PATTERN_{SALES_AGENT,COPILOT,BRAND,DEFAULT}` (sales/copilot/brand True, DEFAULT False)

Estado: shared outbox + adapter funcional, flags en uso. Tests aún referencia path legacy mockeado → bloqueador PR-3.

## 5. Relevant rules — quick reference

| Rule file | Key constraint (≤2 lines) |
|---|---|
| `tenant-isolation.md` | Every query .where(tenant_id ==). Repos reciben tenant_id required. |
| `backend-ddd.md` | Layers Inside-Out: domain → infrastructure → application → api. No framework en domain. |
| `architectural-fitness.md` | Arch tests `backend/tests/architecture/` enforzan reglas estructurales. Ratchet pattern: allowlists shrink only. |
| `anti-duplication.md` | Patrón canónico shared vive en shared/. Módulos heredan, NUNCA mirror. Step 0 grep antes Write. |
| `tdd-mandatory.md` | Tests PRIMERO. RED → GREEN → REFACTOR. Sin excepciones. |
| `backend-quality.md` | Ruff 0 errors, pytest 43% coverage, 10 arch fitness gates. Native venv, nunca docker. |
| `git-safety.md` | development branch única. Stage por nombre. Convencional commits. NO pull/force push/revert sin aprobación. |

## 6. Git diff summary

- **Files modified**: 17 (doc-only: PR folder structure + prompts + PI.md + roadmap)
- **LOC delta**: +2038 / -274 (net +1764)
- **By module**: 
  - `docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/`: PI.md, sprint.md, PR-1/PR-3/PR-4 folders + prompts
  - No code changes yet (PR-3 ready-state = CONTRACT + spec, builder implementation pending)
- **Type of changes**: Documentation / specification (PR contract, walking skeleton, prompts)

## 7. Existing systems detected (NO-NEW-LAYER scan)

> Subsystems scanned: `anti_default_flip`, `eventbus_mock`, `arch_fitness`, `legacy_event_bus`, `outbox_adapter`, `default_flip_audit`

| Subsystem | System/Path | What it does (1 line) | Enum/Config | Factory/Router | Test machinery | State |
|---|---|---|---|---|---|---|
| legacy_event_bus | `shared/domain_events/legacy_event_bus.py::EventBus` | In-memory pubsub dispatch (deprecated) | N/A | N/A | `tests/shared/test_event_bus.py` + 19 test files mock via @patch | active (fallback) |
| outbox_adapter | `shared/domain_events/outbox/application/event_bus_adapter.py::EventBusAdapter` | Routes publish() to outbox INSERT (flag True) or legacy dispatch (flag False) | N/A | `_infer_use_outbox(module)` method (lines 225-236) | `test_event_bus_adapter.py`, `test_event_bus_adapter_infers_module.py`, integration tests (outbox_cutover_e2e, etc.) | active |
| feature flags (config) | `backend/src/core/config.py` lines 270-273 | Per-module + default outbox pattern toggle (sales_agent, copilot, brand True; default False) | Settings dataclass (Pydantic v2) | N/A | Queried runtime in `EventBusAdapter._infer_use_outbox(module)` | active |
| dispatcher | `shared/domain_events/outbox/infrastructure/dispatcher.py::dispatch_outbox` | ARQ async task — claims pending outbox entries, dispatches via legacy bus | N/A | ARQ cron (1 tick/sec) | `tests/integration/test_outbox_cutover_e2e.py` | active |
| arch fitness suite | `backend/tests/architecture/test_no_legacy_event_bus_publish.py` | Existing gate (PR-6 Sub-E) — verifies NO direct `publish_in_memory()` calls in cutover modules | FORBIDDEN_CALLS = ("publish_in_memory",) | AST walk per-file | Ratchet allowlist `KNOWN_DIRECT_LEGACY_EMITTERS` (currently empty) | active |
| test mock inventory | Multiple test files (20 files grep'd) | Tests mock `LegacyEventBus.publish` or `EventBus.publish` via @patch, monkeypatch | Mock targets: LegacyEventBus.publish, EventBus.publish | N/A | Files: copilot, brand, sales_agent, crm integration + shared tests | active → target for migration |

**Modules already using outbox adapter**: copilot, sales_agent, brand (integration tests file list above).

## 8. EXTEND vs NEW recommendations

| Surface PR proposes | Existing system match (from §7) | Recommendation | Reason |
|---|---|---|---|
| Arch fitness test for mock detection | `test_no_legacy_event_bus_publish.py` exists (related but different: detects direct calls, not mock targets) | **NEW** (sibling pattern) | PR-3 test scans for MOCKS of legacy path; existing test scans for CALLS. Same AST walk machinery (ratchet pattern), different violation check. No duplication — complementary gates. |
| Rule for default flip audit | No existing "flip audit" rule (only anti-duplication.md) | **NEW** (reference anti-duplication.md structure) | anti-duplication inspires structure but targets different problem (mirror detection vs flag flip impact). Regla separada justified, conditional trigger en CLAUDE.md. |
| Bypass mechanism (hardcoded file list + magic comment) | `test_no_legacy_event_bus_publish.py` has `KNOWN_DIRECT_LEGACY_EMITTERS` ratchet allowlist | **EXTEND** logic pattern | PR-3 test reuses `BYPASS_FILES` + magic comment `# arch-bypass: testing legacy capability` pattern. NOT a duplicate — separate violations, separate bypass. Coordinate: clarify both in PRs documentation. |
| Inventory of side-effect flags | Core config has flags scattered. No centralized SSoT table | **NEW** (SSoT table § Anti-Default-Flip-Audit) | Regla introduces inventory of ALL side-effect flags (USE_OUTBOX_PATTERN_*, LITELLM_PROXY_ENABLED, futuros USE_DEEPAGENTS_*). First audit. |

## 9. Architecture fitness gates that will run

- `test_api_contracts.py` — existing, unaffected
- `test_cross_module_imports.py` — existing, unaffected
- `test_no_legacy_event_bus_publish.py` — existing (PR-6 Sub-E), active
- **`test_no_legacy_eventbus_mock_when_outbox_on.py`** — NEW (PR-3) — **CONDITIONAL ACTIVATION**: After PR-1 build PASS (tests migrated from legacy mocks). Initial state: allowlist temp captures remaining violations; shrinks as PR-1 progress. Final: empty allowlist. If PR-3 ships before PR-1 PASS, arch fitness activates with allowlist. Ratchet rule: shrink-only.

Suite run: `cd backend && .venv/bin/pytest tests/architecture/ -x -q --tb=short` (auto on `/test-all`, `/test-backend`, `/pase-produccion`).

## 10. Implementation log highlights

N/a — PR-3 at ready state (spec complete, builder not yet started). IMPL-LOG will be created by builder.

## 11. Faithfulness gaps

None detected. Full coverage:
- PR.md read complete (289 lines) ✓
- Rules read: 7 rules (architectural-fitness, anti-duplication, tenant-isolation, backend-ddd, git-safety, tdd-mandatory, backend-quality) ✓
- Git diff captured (17 files, doc-only) ✓
- Duplicate scan executed: 6 bash commands (legacy/outbox/flags/config references) → 30+ hits per category, truncated to first 40 (noted in §13) ✓
- Module state: shared domain_events extracted ✓
- Arch test pattern confirmed via `test_api_contracts.py` + `test_no_legacy_event_bus_publish.py` ✓

Scan notes:
- `[scan-truncated]` — grep "LegacyEventBus" returned >40 hits; only first 30 shown in bash output
- `[scan-truncated]` — grep "outbox" returned >40 hits; only first 30 shown
- Both are expected given outbox pattern pervasive in shared/domain_events/. Architect to re-scan if expansion scope needed.

No subsystem keywords missing — all 6 provided + validated. No rules truncated (all <100 lines read full).

## 12. Raw paths consulted

- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/sprints/S1-test-integrity-and-coverage/prs/PR-3-anti-default-flip-enforcement/PR.md` (289 lines) ✓
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/sprints/S1-test-integrity-and-coverage/prs/PR-3-anti-default-flip-enforcement/CONTRACT.md` (does not exist)
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/sprints/S1-test-integrity-and-coverage/prs/PR-3-anti-default-flip-enforcement/IMPL-LOG.md` (does not exist)
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/sprints/S1-test-integrity-and-coverage/prs/PR-3-anti-default-flip-enforcement/REVIEW.md` (does not exist)
- `/home/chris/AISALESHT/docs/pm-nico/current-state/shared.md` (does not exist — inferred from codebase)
- `.claude/rules/anti-duplication.md` (76 lines) ✓
- `.claude/rules/architectural-fitness.md` (15 lines) ✓
- `.claude/rules/tenant-isolation.md` (12 lines) ✓
- `.claude/rules/backend-ddd.md` (27 lines) ✓
- `.claude/rules/git-safety.md` (14 lines) ✓
- `.claude/rules/tdd-mandatory.md` (17 lines) ✓
- `.claude/rules/backend-quality.md` (14 lines) ✓
- `backend/src/core/config.py` (section 245-312) ✓
- `backend/src/shared/domain_events/legacy_event_bus.py` (grep evidence only, not full read)
- `backend/src/shared/domain_events/outbox/application/event_bus_adapter.py` (grep evidence only)
- `backend/tests/architecture/test_api_contracts.py` (60 lines sample) ✓
- `backend/tests/architecture/test_no_legacy_event_bus_publish.py` (80 lines sample) ✓
- Git log: recent commits (via git status) ✓

## 13. Verbatim grep commands executed (§7 reproducibility)

```bash
# 1. Legacy EventBus references
grep -rn "LegacyEventBus\|legacy_event_bus" backend/src/shared/domain_events/ 2>/dev/null | head -30
# Result: 10 hits (dispatcher, adapter, imports — showing 10)

# 2. Outbox adapter/pattern references
grep -rn "outbox\|EventBusAdapter\|adapter_bus" backend/src/shared/domain_events/ 2>/dev/null | head -30
# Result: 30+ hits (models, dispatcher, repo, domain — truncated to 30)

# 3. Feature flag references (outbox pattern)
grep -rn "USE_OUTBOX_PATTERN" backend/src/ 2>/dev/null | head -20
# Result: 7 hits (config defaults, adapter routing logic)

# 4. Config defaults (verify flag names + defaults)
grep -rn "USE_OUTBOX_PATTERN\|LITELLM_PROXY\|USE_DEEPAGENTS" backend/src/core/config.py 2>/dev/null | head -15
# Result: 5 hits (flags section 270-273)

# 5. Existing test mocks of LegacyEventBus
find backend/tests -name "*.py" -type f 2>/dev/null | xargs grep -l "LegacyEventBus\|EventBus.publish" 2>/dev/null | head -20
# Result: 20 files found (sample: copilot, brand, sales_agent, crm, shared tests)

# 6. Architecture fitness test directory structure
ls -la backend/tests/architecture/ 2>/dev/null | head -30
# Result: 40+ test files in architecture/ (conftest, 40+ test_*.py fixtures, __pycache__)
```

**Scan interpretation:**
- Legacy EventBus: still in use (fallback path when outbox flag False). 10 references in shared/domain_events/ (adapter, dispatcher, legacy entrypoint).
- Outbox adapter: central router. 30+ refs (expected — infrastructure layer).
- Flags: 5 per-module + DEFAULT. Config centralized.
- Test mocks: 20 files still reference legacy path (target for PR-1 migration + PR-3 arch gate).
- Arch fitness: 40+ existing tests + PR-3 will add 1 new.
