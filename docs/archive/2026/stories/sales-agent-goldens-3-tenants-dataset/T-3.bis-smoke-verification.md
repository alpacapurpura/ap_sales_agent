# T-3.bis Smoke Verification (2026-05-08 post-builder)

Verification run by /dev-team orchestrator post-T-3.bis builder commit `1533292f`. Native WSL pytest with `POSTGRES_HOST=127.0.0.1` (Docker network hostname `postgres` not resolvable from WSL native — required override).

## Surface bugs verified

| Bug | Verification command | Result |
|---|---|---|
| Bug 1 — simulation_id deterministic | `POSTGRES_HOST=127.0.0.1 RUN_EVALS=1 pytest tests/scripts/test_generate_golden_candidates.py::TestReproducibilitySmoke -v --timeout=120` | **PASS** ✅ both runs produced `simulation_id=7a2b5158-704b-5278-a642-295460853500` (same UUID — D8/D-A-15 cement honored) |
| Bug 3 — `module.__import__` AttributeError | `POSTGRES_HOST=127.0.0.1 RUN_EVALS=1 pytest tests/scripts/test_promote_golden.py::TestE2ESmoke -v` | AttributeError gone ✅ (test proceeds further) |
| Bug 4 — `settings.DATABASE_URL` AttributeError | `POSTGRES_HOST=127.0.0.1 EVAL_GOLDENS_COST_BUCKET_VERIFY=1 pytest tests/architecture/test_goldens_cost_bucket_invariant.py -v` | AttributeError gone ✅ (test proceeds further) |
| Bug 2 — SessionUnavailable | (Bug 1 test) | PARTIAL ✅ — graceful degradation works: SessionLocal() acquired, passed to run_simulation. BUT agent_bridge `_resolve_session_for_simulation` T-8 stub still returns None even with session injected (deeper bug in modules/sales_agent/ runtime — out of T-3.bis scope) |

## DEEPER bugs surfaced (NEW — out of T-3.bis scope)

### DEEPER-A — agent_bridge T-8 stub broken
- **Symptom**: `simulator.agent_invalid_state error='Runner failed to inject SQLAlchemy session for simulation' error_class=SessionUnavailable` despite db_session passed correctly to run_simulation
- **Root cause**: `modules/sales_agent/.../agent_bridge.py::_resolve_session_for_simulation` is a T-8 stub that returns `None` regardless of injected session. Likely needs sales_agent runtime fix (NOT T-3 territory)
- **Owner**: builder-agentic Opus (R23 — production_code:true sales_agent runtime)
- **Recommended ticket**: `agent-bridge-session-injection-fix` standalone story OR PI-12 sub-épica scope decision

### DEEPER-B — e2e empty archetype_slug
- **Symptom**: `ERROR: no se pudo construir golden: Unknown archetype_slug ''. Valid slugs: ['tenant_coach_lat', ...]`
- **Root cause**: e2e test creates artifact where archetype_slug missing. Either generate_golden_candidates artifact JSON schema OR promote_golden `_build_golden` flow doesn't propagate archetype_slug from tenant
- **Owner**: builder-backend Sonnet (still T-3 territory)
- **Recommended**: T-3.bis-2 follow-up OR roll into next pass

## Real LLM cost incurred

$0.00 — agent crashed turn 0 due to DEEPER-A before LLM fired. Smoke could not exercise real LiteLLM proxy.

## Recommendation

T-3.bis CLOSED with surface bugs fixed (commit 1533292f). Two deeper bugs surfaced by smoke run that need separate handling:

- **Path A (defer)**: Honor auditor APPROVED + merge Story D as-is. Open 2 new tickets for DEEPER-A (Opus, sales_agent runtime) + DEEPER-B (Sonnet, T-3.2). Smoke remains CI-nightly-opt-in until both deeper bugs fixed.
- **Path B (block merge)**: Keep state=reviewing until DEEPER-A + DEEPER-B fixed. DEEPER-A requires Opus 4.7 (~$2-5 cost) for sales_agent runtime work.
- **Path C (partial close)**: Merge Story D + open agent-bridge-session-injection-fix as urgent follow-up (eval-foundation sub-épica blocker — Stories E/F/G/H/I all need working session injection for their own smokes).

Deterministic UUID fix (Bug 1) is the most load-bearing; fixed and verified GREEN. CI nightly smoke will still fail until DEEPER-A fixed but unit tests + arch fitness + Bug 1 reproducibility in mocked tests all GREEN.
