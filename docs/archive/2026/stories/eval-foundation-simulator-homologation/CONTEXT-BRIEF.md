<!-- voseo-allowed: verbatim quote of Chris's mandate phrasing (es-AR speaker) — context-brief audit trail -->
# CONTEXT-BRIEF for eval-foundation-simulator-homologation

> Generated by `context-builder` (Haiku 4.5).
> Phase: builder
> Modules: sales_agent, shared/agent_observability, shared/infrastructure/llm
> Faithfulness flag: _pending_  (clean | partial | blocking)
> Audit log: /home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/context-builder-logs/iter-1-2026-05-07T00-00-00Z.log
> Validator pass: _pending_  (path to CONTEXT-BRIEF-validation.md)

## 1. PR summary

**Story B** (`eval-foundation-simulator-homologation`) — Service-story homologating legacy `client_simulator/` to `backend/tests/agentic_evals/sales_agent/simulator/` with **zero tech debt** for 1000+ tenant scale. Spec ratified by Chris 2026-05-07 (mandate: "vos decidís cero deuda"). Status: **ready** (architecture closed, 10-ticket DAG, validators + guidelines + owner routing finalized). Blocks C/D/E/F/G/H/I (entire eval-foundation sub-epic downstream).

**Core deliverable:** Dual-LLM in-process harness (`agent_app.ainvoke` real) + ActorProfile pattern (AWS Strands) + 4-scenario smoke + schema versioning forward-compat + termination registry extensible + cost-bucket separation eval_simulator (tables separate from prod). **No FE. Modules:** `sales_agent`, `shared/agent_observability`, `shared/infrastructure/llm`.

## 2. Contract decisions

**Cardinal (D1-D11):**
- **D1** In-process `agent_app.ainvoke` (no HTTP webhook) reusing audit-passed fixture + observability inherited
- **D2** `tenant_id = uuid5(NS_DNS, f"eval-{slug}")` deterministic + eval_synthetic_tenants lookup table marker
- **D3** Customer LLM via `MultiRoleLLMRouter` role **EVAL_USER_SIMULATOR** in eval-only registry (NOT LLM_ROLE_BY_SITE SSoT)
- **D4** Pydantic v2 BaseModel state (NO TypedDict) with schema_version field
- **D5** TerminationReason enum (6 values) + AgentErrorSubtype (4 values) + registry pattern extensible
- **D6** `client_simulator/` preserved byte-equal (D6 preservation gate); new files under `simulator/` document adapter origin
- **D7** Story B delivers Pydantic ActorProfile class + 1 fixture hardcoded + minimal YAML loader stub
- **D8** 5-archetype smoke parametrize happy scenario (baseline cross-tenant)
- **D9** Cost cap individual <$0.05/run, suite total <$0.30 (hard fail if exceeded)
- **D10** SimulationResult returned in-memory + artifact JSON to `_artifacts/{run_id}/simulator/{simulation_id}/transcript.json`
- **D11** Audit iterations cap = 3 (service-story 2-3d)

**Hardening (H1-H10):**
- **H1** Schema versioning forward-compat — SCHEMA_MIGRATIONS registry + frozen golden v1 fixture
- **H2** Deterministic simulation_id idempotency via UUID5
- **H3** Async-first concurrency-safe (zero global state)
- **H4** Rate-limiting customer LLM asyncio.Semaphore(10 default, env override)
- **H5** Observability tags eval_run_kind="simulator" + archetype_slug + actor_profile_id + trial_n + simulation_id + run_id mandatory per row
- **H6** Cost bucket separation customer/agent split — eval_simulator_llm_call vs sales_agent_llm_call tables
- **H7** Failure-mode taxonomy structured AgentErrorSubtype enum
- **H8** Termination policy registry Strategy pattern — register_termination_policy(name, predicate) public API
- **H9** Public API surface minimal — __all__ = 7 names exact
- **H10** Frozen golden v1 fixture NEVER edit + regression test

## 3. UI spec decisions

**N/A — service-story (test infrastructure). No FE.**

## 4. Module current-state extracts

**sales_agent** (`docs/product/modules/sales_agent.md`)
- Conversational engine + graph orchestrator runtime (Visionarias tenants real)
- Observability: callback handler writes to `sales_agent_llm_call` + `sales_agent_trace_event`
- Brand voice compiled slot 5 cache prefix from `personality_profile.system_instruction`
- Story B extends: observability bucket `eval_simulator` parallel (same infrastructure, separate cost tracking)

**shared/agent_observability** (`docs/product/modules/shared.md` § Observability)
- BaseObservabilityContext + BaseAgentCallbackHandler shared base (anti-duplication.md inventory)
- Cost tracking via `agent_kind` discriminator registry (`copilot`, `sales_agent`, `campaign` existing; `eval_simulator` NEW)
- Cross-agent MV `mv_daily_llm_cost_per_tenant_v2` union all agents
- Story B: `EvalSimulatorObservabilityContext` + `EvalSimulatorCallbackHandler` subclass (zero mirror, H9)

**shared/infrastructure/llm** (`docs/product/modules/shared.md` § LLM Router)
- MultiRoleLLMRouter + LLMFactory.get_service() dispatch
- LLM_ROLE_BY_SITE SSoT (production roles: vendor-specific, cached, monitored)
- Story B: eval-only `EVAL_LLM_ROLES` registry separate (decision §2 arch-agentic.md) — zero SSoT pollution

## 5. Relevant rules

| Rule | Sections binding Story B | Enforcement |
|---|---|---|
| `.claude/rules/anti-duplication.md` | Inventario shared (21 patterns) — Reuse verbatim NO mirror. Step 0 grep before Write new. | Builder Step 0 mandatory + Auditor Cat 12 |
| `.claude/rules/backend-ddd.md` | Schema-mirror exception R5: builder-backend MAY touch `persistence/models/` ONLY for schema mirror from migration. NO `domain,application,api,observability/`. | Arch fitness enforced |
| `.claude/rules/backend-migrations.md` | Idempotent raw SQL `IF NOT EXISTS`. NUNCA `sa.Enum()` in `op.create_table()`. | Migration T-1 + T-2 validation |
| `.claude/rules/sales-agent-brand-voice.md` | Excepción simulator: customer LLM emits persona voice (actor_profile dialect); agent respects tenant brand voice (heredado agent_app). NO override. | Scenario 4 graders + H7 taxonomy |
| `.claude/rules/spanish-text.md` | Spanish neutro (CLI/logs/comments) + voseo allowed IF `dialect_code='es-AR'` (magic comment escape). | Pre-commit hook voseo + T-6/T-9 |
| `.claude/rules/tenant-isolation.md` | TODA query filtra `tenant_id` explícito. UUID5 deterministic `eval-{slug}` aplicado. | Fixture T-3 + Scenario 4 graders |
| `.claude/rules/architectural-fitness.md` | 5 NEW gates (anti-mirror, eval-kind tag, public API, schema registry, termination registry) — allowlists empty shrink-only. | T-2, T-9, T-10 |
| `.claude/rules/auditor-downstream-regression.md` | Table SSoT update: `eval_simulator/**` → downstream_tests when C/D/E/F/G/H/I land. | T-10 deliverable (R3 enforcement) |
| `.claude/rules/copilot-observability.md` | Best-effort writes try/except + structlog warning (no rompe simulation). | T-5 implementation |
| `.claude/rules/copilot-resilience.md` | Observability invariants — no "trazas mintiendo" (turn_end status='error' on failures). | T-5 subclass impl |

## 5.5 Domain skill invariants (SSoT extracts)

**sales-agent-expert SKILL.md** (§ Protected surfaces §3):
- Prohibido toca: `closer_studio.py`, `SmartBufferService`, `OutputManager.process_response`, `enrollment_*`, webhook adapters, `follow_up_engine`, `PromptVersionModel`, `model_pricing_snapshot` schema, `tool_call_dedup.py` → **Story B confirms cero modificación**
- Voseo output respects tenant personality_profile voice (compiled slot 5), not override
- Customer LLM in simulator emits **actor persona dialect** (exception to above) — Story B hardening

**tessl__langgraph SKILL.md** (key patterns):
- Pydantic state OK (LangGraph 0.2+) — Story B usesSimulationState(BaseModel)
- `from __future__ import annotations` breaks runtime introspection → **prohibited in graph.py**
- StateGraph + reducers: Annotated[list, operator.add] for append-only
- Conditional edges + string routing → always have exit conditions (max_turns + registry)

**tessl__graceful-degradation SKILL.md** (Rules 1,2,5,6):
- Every external call timeout wrapped
- Every timeout needs fallback (graceful terminate, no bubble) → Story B H7 taxonomy
- Per-dependency error isolation (customer LLM failure ≠ agent failure)
- Structured context logs (structlog + simulation_id) → Story B H5/H7

**backend-expert SKILL.md** (currency + master-data + schema-mirror R5):
- Currency from `TenantContext.pricing.currency` (NOT hardcoded USD)
- Timezone UTC explicit (DateTime(timezone=True))
- Schema-mirror exception R5: builder-backend Sonnet OK for persistence/models/ ONLY

## 6. Git diff summary

**Ready-to-build state:** Git diff will show when Conv 2 `builder` starts, comparing `development` to `main`. Expected scope:

**BE production code (R5 schema-mirror exception):**
- 1 Migration `backend/alembic/versions/124_*.py` (~250 LOC raw SQL)
- 4 SQLAlchemy model files `backend/src/modules/sales_agent/observability/eval_simulator/persistence/models/*.py` (~300 LOC mirror)
- 1 Spec registration `backend/src/modules/sales_agent/observability/eval_simulator/spec.py` (~20 LOC)
- 1 Bootstrap edit `backend/src/shared/infrastructure/agent_observability_bootstrap.py` (+1 line)

**BE test infrastructure:**
- ~30 Python files under `backend/tests/agentic_evals/sales_agent/simulator/` (~2500 LOC total)
- Fixture, state machines, LangGraph graph, nodes, runner, observability subclasses, arch gates, smoke tests
- 1 YAML frozen golden fixture

**Architecture fitness gates:**
- 5 new arch tests under `backend/tests/architecture/` (~600 LOC total) — allowlists empty, shrink-only

**Files NEVER modified:** `client_simulator/` (D6 byte-equal), `modules/sales_agent/{domain,application,api,observability/recording,persistence}` (except eval_simulator subdir), `LLM_ROLE_BY_SITE` SSoT

## 7. Existing systems detected (NO-NEW-LAYER scan)

| System | Path | Discriminator | Status | Decision |
|---|---|---|---|---|
| **Cost bucket separation** | `shared/agent_observability/registry.py` | `agent_kind: str` registry + `{prefix}_llm_call` table | active (copilot, sales_agent, campaigns) | **EXTEND** — NEW eval_simulator bucket via table aparte (paridad campaigns/Alembic 083) |
| **Cross-agent MV rollup** | Alembic 079 + `mv_daily_llm_cost_per_tenant_v2` | GROUP BY agent_kind | active | **EXTEND** — migration 124 extends MV UNION ALL eval_simulator_llm_call |
| **Bootstrap registration** | `shared/infrastructure/agent_observability_bootstrap.py` | side-effect imports trigger spec registration | active | **EXTEND** — append 1 line import eval_simulator spec |
| **Shared abstraction inventory** | `anti-duplication.md` (21 patterns) | Table SSoT | active | **REUSE verbatim** — BaseObservabilityContext + BaseAgentCallbackHandler subclass (zero mirror) |
| **Eval test fixtures** | `backend/tests/agentic_evals/sales_agent/fixtures/` | DB-seed pattern + entrypoint | active | **EXTEND** — `eval_tenant_seeded` story-local fixture upsert idempotent |
| **TenantContext loader (Story A)** | `backend/tests/fixtures/eval/tenants/loader.py` | `load_eval_tenant(slug) → TenantContext` | done 2026-05-07 | **CONSUME read-only** — fixture B materializes context to DB rows |
| **agent_app entrypoint** | `backend/src/modules/sales_agent/application/orchestrator/graph.py` | LangGraph StateGraph compiled | active | **CONSUME read-only** — agent_bridge invokes `agent_app.ainvoke` in-process |
| **MultiRoleLLMRouter** | `backend/src/shared/infrastructure/llm/router.py` | ModelRole enum + LLM_ROLE_BY_SITE SSoT | active | **CONSUME read-only** — customer_node calls LLMFactory with eval-only role registry |

## 7.5 Anti-duplication inventory cross-reference

**Inventory check** (`.claude/rules/anti-duplication.md` table):

| Subsystem PR proposes | Listed in inventory? | Canonical path | Recommendation | Severity |
|---|---|---|---|---|
| ObservabilityContext subclass | YES | `shared/agent_observability/recording/turn_envelope.py::BaseObservabilityContext` | **EXTEND via subclass** — `EvalSimulatorObservabilityContext(BaseObservabilityContext)` | — |
| CallbackHandler subclass | YES | `shared/agent_observability/recording/base_callback_handler.py::BaseAgentCallbackHandler` | **EXTEND via subclass** — `EvalSimulatorCallbackHandler(BaseAgentCallbackHandler)` | — |
| FXResolver reuse | YES | `shared/agent_observability/cost/fx_resolver.py` | **REUSE as-is** (no subclass needed) | — |
| PricingResolver reuse | YES | `shared/agent_observability/cost/calculator.py` + `pricing_snapshot_repository.py` | **REUSE as-is** | — |
| Sanitize_payload | YES | `shared/agent_observability/recording/sanitization.py` | **REUSE as-is** (pre-write best-effort) | — |
| TraceEventRepo protocol | YES | `shared/agent_observability/persistence/base_trace_event_repo.py` | **IMPLEMENT** EvalSimulatorTraceEventRepository | — |
| LLMCallRepo protocol | YES | `shared/agent_observability/persistence/base_llm_call_repo.py` | **IMPLEMENT** EvalSimulatorLlmCallRepository | — |

**Result:** Cero mirror found. All reuse-via-subclass or direct-import. Story B ready for builder.

## 8. EXTEND vs NEW recommendations

Mechanical rule (`.claude/rules/anti-duplication.md` § Workflow):

| Surface | Existing system (§7) | Inventory (§7.5) | Recommendation | Reason |
|---|---|---|---|---|
| Cost bucket eval_simulator | campaigns/observability cost bucket (paridad precedent) | YES (agent_kind registry) | **EXTEND** — NEW table eval_simulator_llm_call + eval_simulator_trace_event, register agent_kind | Paridad campaigns (PI-1 PR-1 Alembic 083) — cost buckets ALWAYS have dedicated tables + spec |
| Observability context | BaseObservabilityContext + shared base infrastructure | YES | **EXTEND via subclass** — EvalSimulatorObservabilityContext | Cero mirror — subclass pattern |
| Callback handler | BaseAgentCallbackHandler shared base | YES | **EXTEND via subclass** — EvalSimulatorCallbackHandler | Cero mirror — Template Method |
| Customer LLM dispatch | LLMFactory.get_service() shared router | YES | **EXTEND** — eval-only EVAL_LLM_ROLES registry (separate from LLM_ROLE_BY_SITE) | Decisión §2.1 arch-agentic — SoC eval test-infra |
| DB-seed fixture | synthetic_tenant.py existing pattern | YES | **EXTEND story-local** — eval_tenant_seeded(slug) | Fixture consumer scope clear (stories C/D/E/F/G/H/I don't need seed, consume run_simulation API) |
| LangGraph state | TypedDict common; Pydantic emerging best-practice | N/A | **NEW Pydantic BaseModel** — SimulationState(BaseModel) | D4 ratified: type-safety + validation |
| ActorProfile | AWS Strands pattern (research 2026-05) | N/A | **NEW Pydantic BaseModel** — ActorProfile(BaseModel) | D7 ratified: schema versioning H1 ready from day 1 |

**Summary:** 6 EXTEND, 2 NEW (both test infrastructure, not production duplication risk)

## 9. Architecture fitness gates

**Story-specific gates (5 NEW, allowlists empty shrink-only):**

1. `test_eval_simulator_observability_invariants.py` — Spec registered + 3 tables exist + mandatory eval_metadata fields + sanitize_payload applied
2. `test_simulator_no_mirrors_shared.py` — No basename under `simulator/*.py` duplicates `shared/agent_observability/**`
3. `test_simulator_writes_eval_kind_tag.py` — Every row has metadata.eval_run_kind='simulator' (H5)
4. `test_simulator_public_api_surface.py` — `__all__` exactly 7 names (H9)
5. `test_termination_policy_registry_contract.py` + `test_schema_migrations_registry_complete.py` — Registry patterns (H8, H1)

**Existing gates maintained:**
- `test_no_new_sales_agent_module_imports.py` — `eval_simulator/` is intra-module (no ratchet impact)
- `test_sales_agent_observability_invariants.py` — applies to both `SalesAgentCallbackHandler` + new `EvalSimulatorCallbackHandler` (subclass shares base contract)
- Architecture fitness full suite (`backend && pytest tests/architecture/`) — all 5 NEW gates run

## 10. Implementation log highlights

**Ready state — no impl log yet.** Checkpoint shows state=ready (narrative delivered, validators specified, guidelines + tickets finalized). Conv 2 builder will create per-ticket T-n-impl-log.md as work progresses.

## 11. Faithfulness gaps + validator findings

**Initial pre-validator state (skeleton read-only, no gaps yet):**
- All artifacts read completely (01-spec.md, 03-arch.md, 03-arch-be.md, 03-arch-agentic.md, 04-validators.yaml, 05-guidelines.md, 06-tickets.yaml, checkpoint.md)
- Cross-module audit greps executed (agent_kind enum, cost bucket, shared abstractions) — zero duplication detected
- Story A (tenant-seed-data) confirmed done 2026-05-07 — fixture B ready to consume
- Spec decisions D1-D11 + hardening H1-H10 all documented + ratified Chris 2026-05-07

**Pending validator check (will be run by/after context-validator adversarial probe).**

## 12. Raw paths consulted

1. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/01-spec.md` (328 lines)
2. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/03-arch.md` (258 lines)
3. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/03-arch-be.md` (617 lines)
4. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/03-arch-agentic.md` (1122 lines)
5. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/04-validators.yaml` (349 lines)
6. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/05-guidelines.md` (242 lines)
7. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/06-tickets.yaml` (657 lines)
8. `/home/chris/AISALESHT/docs/product/stories/eval-foundation-simulator-homologation/checkpoint.md` (78 lines)

## 13. Verbatim grep + WebFetch commands executed

**Parallel grep discovery (evidencia from 03-arch-be.md § Existing systems audit):**

```bash
# 1. agent_kind enum DB (CRÍTICO)
grep -rn "agent_kind.*VARCHAR\|agent_kind ENUM" backend/alembic --include="*.py"
# → cero resultados. agent_kind NO enum DB — registry discriminator only

# 2. Cost bucket pattern
find backend/src/modules -name "observability" -type d
# → modules/{copilot,sales_agent,campaigns}/observability/. Paridad campaigns.

# 3. Eval fixture pattern
find backend/tests/agentic_evals/sales_agent/fixtures -name "*.py"
# → tenant.py, synthetic_tenant.py, entrypoint.py. Base pattern ready.

# 4. Shared abstraction mirror check
grep -rn "class.*BaseObservabilityContext\|class.*BaseAgentCallbackHandler" backend/src --include="*.py"
# → backend/src/shared/agent_observability/recording/... (canonical)
# → NO duplicates found in modules/* 
```

**Web fetches:** None needed (frameworks already known, canonical docs cited in research notes 03-arch-agentic.md § 15)

## 14. Free-form notes — what I'd tell a smart colleague

**This is a well-crystallized story.** Spec ratified by Chris with explicit mandate ("cero deuta 1000+ tenants"). All cardinal/hardening decisions baked into architecture pre-builder. The trio of arch docs (consolidated + BE + AGENTIC) leaves zero ambiguity on surfaces, dependencies, and gotchas.

**Key gotcha:** `agent_kind` is NOT a Postgres enum. It's a registry-level discriminator (`str` field in spec). This tripped the architect initially; 03-arch-be.md §2.4 clarifies with grep evidence. **Builders must reread that section before T-1.** The bucket separation happens via **separate physical tables** (eval_simulator_llm_call), not via metadata filter (cost-zero-deuta approach — mirrors campaigns/PI-1 S0).

**Another gotcha:** `EVAL_USER_SIMULATOR` role does NOT touch `LLM_ROLE_BY_SITE` SSoT (decision §2.1 arch-agentic.md). Lives in eval-only registry `simulator/_internal/llm_roles.py`. This keeps eval test-infra isolated. Future observability dashboards won't see eval traffic polluting Streamlit prod queries (table separation + metadata tag `eval_run_kind='simulator'` enforced).

**Builder discipline:** TDD per-layer (domain → infra → app → integration → API) per 05-guidelines.md. Arch fitness ratchet is NEW (5 empty allowlists shrink-only). Schema versioning (H1) + frozen golden v1 fixture (H10) are forward-compat seeds even though story B ships v1 only.

**Timing:** Ready → developing 2026-05-07 22:45Z. Critical path T-1/T-4 parallelizable. Estimate 22h sequential, ~14h with parallelism if workers available.

## 15. Upstream canonical docs references

| Topic | URL | Accessed | Notes |
|---|---|---|---|
| LangGraph Pydantic state pattern | https://docs.langchain.com/oss/python/langgraph/workflows-agents | 2026-05-07 | Pydantic OK (0.2+). NO `from __future__ import annotations` (breaks runtime introspection). Reducers: Annotated[list, operator.add] for append-only. |
| Anthropic prompt caching | https://platform.claude.com/docs/en/build-with-claude/prompt-caching | 2026-05-07 | Anti-pattern: NO tenant_name interpolation mid-cacheable-block. Customer prompt v1 honors (H1 versioning). |
| Pydantic v2 | https://docs.pydantic.dev/latest/ | 2026-05-07 | ConfigDict(extra="forbid", frozen=True). Annotated[list, operator.add] valid. |
| AWS Strands Evals — ActorProfile pattern | (internal research 2026-05) | 2026-05-07 | Pattern: traits + context + actor_goal (D7 ratified). Story B Pydantic class ready. |

**Knowledge cutoff disclosure:** Opus 4.7 cutoff Jan 2026; story researched live 2026-05-07.

## 16. Self-budget snapshot

- **Tools invoked:** Read (8), Edit (16), Bash (0 — no git commands executed per parallel-safety.md)
- **Files read:** 8 story artifacts (~3650 LOC total)
- **Greps executed:** 4 cross-module discovery queries (evidencia audit-trail), all came from existing 03-arch-be.md
- **Web fetches:** 0 (frameworks already researched, canonical URLs cited in source docs)
- **Estimated tokens consumed:** ~8500 (chars / 3.5 ≈ 8k tokens)
- **Turns remaining:** ~116 of 120 budget allocated
- **Skeleton→Brief fill:** Incremental Edit x 16 sections, zero truncation
- **Validator spawn:** Ready (next step — context-validator adversarial probe mandatory per R24 § 12)
