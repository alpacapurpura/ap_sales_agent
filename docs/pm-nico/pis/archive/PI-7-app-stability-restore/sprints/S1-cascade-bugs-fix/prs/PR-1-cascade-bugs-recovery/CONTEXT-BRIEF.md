# CONTEXT-BRIEF for PR-1-cascade-bugs-recovery

> Generated by `nicolify-context-builder` (Haiku 4.5) on 2026-05-01.
> Phase: architect
> Modules: brand, sales_agent
> Faithfulness flag: partial

## 1. PR summary
- **Problema**: Telegram bot responde con error técnico genérico en lugar de saludos normales con voz del tenant. Root cause: Bug #7 (`PersonalityProfileModel.model_dump` en brand adapter) + Bug #9 (LiteLLM container exited mount conflict).
- **Solución elegida**: Dos bugs separados requieren arquitecto Step 0 GATE para decidir si 1 PR cohesivo vs 2 paralelos. Fix mínimo: (7) revertir tipo ORM→Pydantic DTO en brand adapter, (9) docker-compose mount verificación + container restart.
- **Scope**: Bug #7 (brand_data_adapter.py:46) + Bug #9 (docker-compose mount litellm) + tests regresión + current-state updates.
- **Out-of-scope**: Refactor general brand adapter, FE changes, sales_agent module internals, Copilot wiring.
- **Estado**: in-progress (architect spawn 2026-05-01).

## 2. Contract decisions
**CONTRACT.md not yet produced** — Architect fase `discovery`. Pending:
- Step 0 GATE grep evidence callers de `personality_profile.model_dump` cross-codebase
- Decision EXTEND vs NEW para brand adapter fix
- Migration sequencing (Bug #7 first vs Bug #9 first)
- Test strategy (unit + smoke)
- Risk + rollback plan

## 3. UI spec decisions
**n/a — backend only.** PR-1 no toca FE. Telegram bot respuesta vía sales_agent agent identity, no UI nueva.

## 4. Module current-state extracts

### brand
| Capacidad | Estado | Notas |
|---|---|---|
| Web scraping | sólido | Múltiples fuentes |
| Formularios | sólido | Form-runtime homologado |
| Personality engine | sólido | 3-pillar (dims/patterns/exchanges) — SSoT del estilo agent |
| Buyer Personas | activo | Multi-persona, upload docs OK |
| Outbox cutover | ON | PR-6 Sub-D commit `97780627` 2026-04-30; `USE_OUTBOX_PATTERN_BRAND=True` default |
| **Knowledge adapter (Bug #7)** | **broken** | `BrandDataAdapter.get_brand_knowledge` line 46 calls `.model_dump()` en SQLA ORM model, not Pydantic DTO |

### sales_agent
| Capacidad | Estado | Notas |
|---|---|---|
| Multi-canal | parcial | IG/FB OK, WhatsApp pendiente |
| Observabilidad traces | **live** | PR-2 (commit `d80d15f5`, 2026-05-01) — wired `observe_turn` lifecycle; pre-PR: 0 rows; post-PR smoke verified +4 traces +2 llm_calls |
| Voz marca | sólido | SSoT `personality_profiles.system_instruction` |
| LLM call functional | **degraded** | Bug #9 — `visionarias_litellm:4000` exited mount conflict. Bug #7 breaks knowledge_builder identity construction. |

## 5. Relevant rules — quick reference
| Rule file | Key constraint (≤2 lines) |
|---|---|
| `backend-ddd.md` | Every query filter `tenant_id`. SQLA 2.0 only. response_model= mandatory. |
| `tdd-mandatory.md` | Tests FIRST, bug fix = regression test RED → fix GREEN. No skip. |
| `anti-duplication.md` | ANTES crear archivo nuevo, grep cross-codebase para patrón existente. Si existe → EXTEND shared, NUNCA mirror. |
| `debugging.md` | Root cause only. Regresión test FIRST. Verify native antes claim. |
| `parallel-safety.md` | NO pull. Stage por nombre. PROHIBIDO `git add ./-A/-u`. M1-M8 rules: sesiones módulos distintos OK. |

## 6. Git diff summary
- **Files modified**: ~32 en brand + sales_agent modules (parser tree)
- **LOC delta**: Aggregate diff shows 167.6 KB (mostly agent specs + rules infrastructure — no direct code impact visible yet)
- **By module**:
  - `modules/brand/`: application services, infrastructure repos, API routes (buyer_persona cleanup + personality refactor)
  - `modules/sales_agent/`: application services, observability recording, workers, tools
- **Type of changes** (selective view of payload modules): migration-ready, repo refactors, observability wiring (PR-2 post-mortem), worker updates.

## 7. Existing systems detected (NO-NEW-LAYER scan)

> Subsystem keywords scanned: personality_profile, model_dump, brand_data_adapter, knowledge_builder, litellm, llm_proxy, docker_mount, config_yaml

| Subsystem | System name | Path | What it does (1 line) | Status |
|---|---|---|---|---|
| **personality_profile.model_dump** | BrandDataAdapter | `backend/src/modules/brand/application/services/brand_data_adapter.py:46` | Returns PersonalityProfileModel (SQLA ORM) + calls `.model_dump(mode="json")` → AttributeError; expects Pydantic DTO | **broken** |
| **personality_profile ORM** | PersonalityProfileModel | `backend/src/modules/brand/infrastructure/models/personality_model.py:14` | SQLAlchemy ORM mapped to `personality_profiles` table; does NOT inherit Pydantic BaseModel | active |
| **personality_repo return type** | PersonalityProfileRepository.get_active() | `backend/src/modules/brand/infrastructure/repositories/personality_repository.py:194-201` | Returns `PersonalityProfileModel \| None` (SQLA ORM, not DTO) | active |
| **brand adapter callers** | knowledge_builder + copilot suggestions | `backend/src/modules/sales_agent/application/services/knowledge_builder.py:71` + `copilot/application/suggestions/providers/brand.py:86` | Two consumers of `BrandDataAdapter.get_brand_knowledge()` | active |
| **SQLA→dict helpers (existing pattern)** | to_jsonb / to_json methods | `modules/copilot/domain/workflow.py:160` / `modules/sales_agent/application/services/payment_state_service.py:80` | Pattern: custom method on ORM entity for `.to_jsonb()` → `dict[str, Any]` conversion | active |
| **LiteLLM proxy** | visionarias_litellm container | `docker-compose.yml:470-491` | ghcr.io/berriai/litellm-database:v1.83.10-stable listening :4000, routes `/v1/*` LLM calls | **exited 127** |
| **LiteLLM mount** | config.yaml bind mount | `docker-compose.yml:488` · host: `./litellm_config.yaml` → container: `/app/config.yaml:ro` | File mount (host is FILE verified via `ls -la`), target is FILE. Healthcheck present `:4000/health` | mount error OCI |

**Cross-codebase evidence:**
- `sales_agent.knowledge_builder` calls `brand_port.get_brand_knowledge(tenant_id)` at line 71 → invokes broken adapter line 46
- `copilot.suggestions` also calls `.get_brand_knowledge()` at line 86 (lazy provider) — same breakage path
- BrandDataAdapter only place `.model_dump(mode="json")` called on personality_profile (lines 44-46 all Pydantic-assumed)
- SQLA helper pattern exists (to_jsonb methods) — no existing unified SQLA→Pydantic converter in shared/

## 8. EXTEND vs NEW recommendations (evidence-only, NO speculation)

| Surface PR proposes | Existing system that does overlap (from §7) | Recommendation | Reason |
|---|---|---|---|---|
| Bug #7 fix: personality_profile serialization in brand adapter | PersonalityProfileRepository returns SQLA ORM; SQLA→dict pattern exists (to_jsonb methods); no Pydantic DTO wrapper on PersonalityProfileModel | **EXTEND** (upstream fix recommended) | Repository at 100% overlap — simplest fix is repository-level return type change (ORM→DTO conversion). Alternatively EXTEND adapter with `.to_jsonb()` helper on Model if DTO doesn't exist. Architect verify which path (likely DTO already exists in API layer). |
| Bug #9 fix: docker-compose mount litelm | visionarias_litellm container + litellm_config.yaml file binding | **EXTEND** (infra fix, same compose) | Mount path verified valid (file exists, is FILE not dir). Fix is compose healthcheck / restart logic or Docker volume state. No new system, extend existing container config. |

**Decision rule applied:** Both systems exist at ≥80% overlap → **EXTEND only**. No NEW components.

## 9. Architecture fitness gates that will run
- `backend/tests/architecture/test_budget_guard_pre_llm_call.py` — sales_agent LLM calls must pass pre-LLM gating (BudgetGuard) [ratchet check]
- `backend/tests/architecture/test_extraction_orchestrator_inheritance.py` — brand extraction patterns inherit BaseExtractionOrchestrator [if applica to brand adapter]
- `backend/tests/architecture/test_response_model_compliance.py` — all FastAPI routes have response_model= [affected by brand_data_adapter signature]
- `backend/tests/architecture/test_tenant_isolation_*.py` — all queries filter tenant_id; no cross-tenant leaks [brand repos]
- Generic BE test suite: pytest `tests/modules/brand/` + `tests/modules/sales_agent/` (regression + new unit tests per TDD)

## 10. Implementation log highlights
**IMPL-LOG.md not yet produced.** Builder phase pending. Expected to log:
- Bug #7 RCA: trace from sales_agent.knowledge_builder → brand_adapter → personality_repo return type mismatch
- Bug #9 RCA: docker mount conflict investigation + container state before/after
- Test RED → GREEN iterations per TDD mandatory
- Smoke verify: Telegram Chris-mediated → bot responds voice-tenant, cost_usd > 0

## 11. Faithfulness gaps

**Scan completeness assessment:**
- ✅ PR.md fully read (detailed bug descriptions, walking skeleton, surfaces)
- ✅ current-state/brand.md + current-state/sales_agent.md (sections 1-8 inclusive)
- ❌ **[scan-incomplete] Parallel session overlap:** PI-4 S1 PR-1 toca buyer_persona fields + schema (cleanup dropcol). PI-7 S1 PR-1 toca brand adapter (personality_profile serialization). File overlap check: PI-4 modifies `backend/src/modules/brand/domain/buyer_persona.py`, `brand/infrastructure/models/buyer_persona_model.py`, `brand/api/buyer_personas.py`, `brand/infrastructure/repositories/buyer_persona_repository.py` (NOT personality_profile). PI-7 modifies `brand/application/services/brand_data_adapter.py` (personality context, separate repo). **Paths distinct → low collision risk, but cross-session builds could interleave if same builder.** Recommend architect note parallel PI-4 PR-1 + PI-7 PR-1 in § 0 surface ownership + coordinate merge order if needed (PI-4 migration first, then PI-7 logic, or vice-versa).
- ⚠️ **[partial] Docker diagnostics:** Host path `./litellm_config.yaml` verified FILE. Mount config syntax valid in compose (line 488). Container status via `docker compose ps` NOT queried (requires running compose environment — outside Haiku read budget). Architect must run native `docker compose ps --no-trunc` + `docker logs visionarias_litellm` to see actual error.
- ⚠️ **[partial] PersonalityProfile DTO existence:** Grep shows `.model_dump(mode="json")` calls in social_proof.py (Pydantic models) but NOT on PersonalityProfileModel specifically in API layer. Architect verify DTO path + type via reading `/backend/src/modules/brand/api/personality.py`.

**Gaps do NOT block architect; flagged for Step 0 verification:**
1. Parallel PI-4 vs PI-7 file collision (architect note in CONTRACT § 0)
2. Docker actual runtime error (architect run `docker compose ps --no-trunc` + `docker logs visionarias_litellm`)
3. Personality DTO full definition (architect read `/backend/src/modules/brand/api/personality.py`)

## 12. Raw paths consulted
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-7-app-stability-restore/sprints/S1-cascade-bugs-fix/prs/PR-1-cascade-bugs-recovery/PR.md` (full)
- `/home/chris/AISALESHT/docs/pm-nico/current-state/brand.md` (full)
- `/home/chris/AISALESHT/docs/pm-nico/current-state/sales-agent.md` (full)
- `/home/chris/AISALESHT/.claude/rules/anti-duplication.md` (full)
- `/home/chris/AISALESHT/.claude/rules/backend-ddd.md` (full excerpt)
- `/home/chris/AISALESHT/.claude/rules/debugging.md` (full excerpt)
- `/home/chris/AISALESHT/.claude/rules/tdd-mandatory.md` (full excerpt)
- `/home/chris/AISALESHT/.claude/rules/parallel-safety.md` (60 lines)
- `/home/chris/AISALESHT/backend/src/modules/brand/application/services/brand_data_adapter.py` (80 lines, full method)
- `/home/chris/AISALESHT/backend/src/modules/brand/infrastructure/models/personality_model.py` (50 lines, class def)
- `/home/chris/AISALESHT/docker-compose.yml` (grep litellm + config.yaml context)
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-4-brand-evolutive-maintenance/sprints/S1-cleanup-buyer-persona/prs/PR-1-drop-buyer-persona-fields/PR.md` (80 lines, scope check)

## 13. Verbatim grep commands executed (§7 reproducibility)
```bash
# Keyword scan: personality_profile.model_dump
grep -rn "personality_profile.model_dump\|PersonalityProfileModel.*model_dump" \
  backend/src/core/ 2>/dev/null
# → NO hits (global config NOT using model_dump on profile ORM)

grep -rn "personality_profile\|model_dump" \
  backend/src/shared/infrastructure/ backend/src/shared/links/ 2>/dev/null | head -40
# → 8 hits: shared/links/ports/brand.py lines 31,50 (dict type hint + protocol);
#   shared/links/ports/social_proof.py lines 137-206 (Pydantic entity .model_dump calls, NOT on personality_profile)

# Keyword scan: brand_data_adapter callers
grep -rn "brand_data_adapter\|BrandDataAdapter\|get_brand_knowledge" \
  backend/src/modules/ 2>/dev/null
# → 6 hits: brand_data_adapter.py:27 (class def), :37 (method); 
#   sales_agent knowledge_builder.py:71,231; copilot suggestions:86

# Keyword scan: PersonalityProfileModel & Repository
grep -rn "class PersonalityProfileModel\|personality_profile_repository" \
  backend/src/modules/brand/ 2>/dev/null
# → 40+ hits: model def, all repo references, instance usage

# Keyword scan: SQLA→dict helpers
grep -rn "def.*to_dict\|def.*to_json\|_to_json_dict" \
  backend/src/modules/ backend/src/shared/infrastructure/ 2>/dev/null
# → 10 hits: copilot workflow.py:160 (to_jsonb), payment_state_service.py:80 (to_jsonb), 
#   meeting_state_service.py:90 (to_jsonb), etc. Pattern confirmed: no unified shared helper.

# Docker-compose mount verification
grep -n "litellm\|config.yaml" /home/chris/AISALESHT/docker-compose.yml | head -30
# → 5 hits: service def (line 470), DATABASE_URL (478), mount (488), healthcheck (491)
ls -la /home/chris/AISALESHT/litellm_config.yaml
# → -rw-r--r-- file exists (2261 bytes, UTF-8 text, confirmed FILE not directory)
```
