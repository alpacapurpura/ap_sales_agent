# CONTEXT-BRIEF for PR-2-coverage-p0-modules
> Generated by `nicolify-context-builder` (Haiku 4.5) on 2026-05-04.
> Phase: builder
> Modules: crm, scheduling
> Faithfulness flag: clean

## 1. PR scope and objectives

**Objetivo:** Elevar cobertura de tests de `crm` (87.15% actual → target ≥75%, already there!) y `scheduling` (43.71% actual → target ≥75%, 31% lift needed) a través de tests unitarios + integración liviana para capas `application` e `infrastructure`.

**Scope PR-2:**
- Tests unitarios para servicios de aplicación sin cobertura: `lead_query_service`, `nps_service`, `referral_service`, `availability_service`, `public_links` (scheduling API).
- Tests integración con mocks de repositorio (sin cambiar lógica de negocio).
- Fixture support para transactional DB en tests (reuse `conftest.py` existente).

**Problema:** `crm` y `scheduling` tienen cobertura más baja del backend. Servicios críticos son operables desde copilot (PI-3) sin tests defensivos, creando riesgo de regression silenciosa en ventas + agendamiento.

## 2. Module CRM — current state + path inventory

**Capacidades operables desde copilot:**
- Buscar contacto (parcial)
- Ver pipeline conversacionalmente (parcial)
- Gap: crear/modificar contacto vía copilot
- Gap: segmentación dinámica conversacional

**Servicios application sin cobertura o parcial (target para PR-2):**
| Service | File | Stmts | Miss | Cover % | Notas |
|---|---|---|---|---|---|
| `contact_query_service` | `application/services/contact_query_service.py` | 152 | 47 | 69% | Complex filter logic; 47 lineas sin cobertura |
| `lead_metrics_repository` | `infrastructure/repositories/lead_metrics_repository.py` | 120 | 120 | 0% | Completamente sin cobertura — metadatos del lead |
| `sale_repository` | `infrastructure/repositories/sale_repository.py` | 43 | 6 | 86% | 6 lineas faltantes en edge cases |
| `customer_repository` | `infrastructure/repositories/customer_repository.py` | 85 | 5 | 94% | Trait filtering edge cases |

**API actual:**
- `GET /api/v1/contacts` — paginated + 18 canonical filters (forward-compat)
- `GET /api/v1/contacts/{id}` — unified Profile+Lead detail
- `GET /api/v1/contacts/_filter-schema` — metadata FE dynamic UI

**Tests existentes:** 306 passed (crm suite), cobertura **87.15%** (ya arriba target 75% global, pero servicios específicos < 80%).

## 3. Module scheduling — current state + path inventory

**Capacidades operables desde copilot:**
- Crear event type nuevo (parcial)
- Ver agenda próximos días (parcial)
- Gap: modificar disponibilidad conversacionalmente

**Servicios application sin cobertura o parcial (target para PR-2):**
| Service | File | Stmts | Miss | Cover % | Notas |
|---|---|---|---|---|---|
| `availability_service` | `application/services/availability_service.py` | 251 | 100 | 60% | 100 lineas sin cobertura — schedule migration + slot generation complexity |
| `event_type_service` | `application/services/event_type_service.py` | 117 | 14 | 88% | 14 lineas en edge cases slug uniqueness |
| API `public_links.py` | `api/public_links.py` | 140 | 140 | 0% | Booking public page — 0% cobertura |
| API `agenda.py` | `api/agenda.py` | 65 | 65 | 0% | Calendar agenda endpoints — 0% cobertura |

**Tests existentes:** 35 passed (scheduling suite), cobertura **43.71%** (need 31% lift).

## 4. Baseline coverage analysis (CRM + scheduling)

### CRM — baseline

```
TOTAL: 1891 stmts | 243 miss | 87.15% cover [BASELINE]

**By layer:**
- api/: 8 files, mostly 99-100% (contact_query_service filters @ 69% pulling down api/contacts.py to 79%)
- application/services/: 8 files, range 69-100% (contact_query_service 69%, nps_service 100%, lead_query_service 100%)
- infrastructure/repositories/: 5 files, range 0-94% (lead_metrics_repository 0%, lead_repository 94%, sale_repository 86%)
- domain/: 11 files, 100%
```

**Gap analysis for PR-2:**
- `contact_query_service.py` (69%) — 47 miss stmts: complex filter logic chains, edge-case filters (#106-111, #118-137, #147-157, #189-222, #276-281, #328-330, #386-418, #444-449) — unit tests mock repo, parameter combinations
- `lead_metrics_repository.py` (0%) — 120 stmts untouched: metrics aggregations, no API consumer yet
- `sale_repository.py` (86%) — 6 miss: bulk upsert logic, soft-delete edge cases
- `customer_repository.py` (94%) — 5 miss: JSONB trait queries (PostgreSQL-only, skip in unit suite)

**Verdict:** CRM already ≥75% target. PR-2 focus on **raising service unit tests to ≥90% per service** to support copilot PI-3 operability.

### scheduling — baseline

```
TOTAL: 723 stmts | 407 miss | 43.71% cover [BASELINE → need +31%]

**By layer:**
- api/: 3 files, 0% (public_links 0%, agenda 0%, event_types 33%)
- application/services/: 2 files, 60-88% (availability_service 60%, event_type_service 88%)
- domain/: 4 files, 100%
- infrastructure/repositories/: 1 file, 91% (appointment_repository)
```

**Gap analysis for PR-2:**
- `availability_service.py` (60%) — 100 miss stmts: schedule migration from legacy `weekly_hours`, slot generation algo, edge-case timezone handling (#49-55, #59-81, #112-137, #171-218, #248-291, #312-364, #379-481, #493-516) — critical for booking
- `event_type_service.py` (88%) — 14 miss: slug uniqueness auto-suffix logic (#33, #67-72, #96, #152-159)
- `public_links.py` (0%) — 140 stmts: entire Calendly-like public booking UI backend — must test booking creation, link generation, public schema
- `agenda.py` (0%) — 65 stmts: calendar agenda endpoint — must test Google Calendar integration, timezone conversion

**Verdict:** scheduling requires **comprehensive coverage overhaul**. +31% lift achievable via 40-50 new tests (API endpoints + service unit tests).

## 5. Existing test patterns and fixtures

### CRM test fixtures (conftest.py exists)

**Available fixtures** (from `tests/modules/crm/conftest.py`):
- `tenant_id()` — UUID fixture
- `sample_profile()` — basic SUBSCRIBER profile
- `sample_mql_profile()` — MQL stage profile
- `sample_customer_profile()` — CUSTOMER stage profile
- `sample_journey_events()` — journey event list
- (more defined in file)

**Pattern:** Setup profiles in DB via `db` session fixture (pytest-asyncio + transactional). Tests import conftest fixtures + inherit `db` from root `tests/conftest.py`.

### Scheduling test fixtures

**No conftest.py exists yet.** Scheduling tests use inline fixture construction (see `test_availability_service.py` `_make_schedule()` helper).

**Pattern:** Should reuse root `tests/conftest.py` `db` fixture + create scheduling-specific fixtures (e.g., `event_type_factory`, `appointment_factory`).

### Root conftest (tests/conftest.py, 419 lines)

Provides:
- `db` — transactional SQLAlchemy session (rollback per test)
- `app` — FastAPI test client
- Singletons reset fixtures (post-PI-11 PR-1 hardening)
- Markers: `integration`, `verify`

**Built-in for use:** No need for custom `pytest_plugins` or additional DB setup — inherited automatically.

### Test naming + structure convention

```
tests/modules/{module}/test_{domain}.py  # Domain entity tests
tests/modules/{module}/test_{service_name}_service.py  # Application service tests
tests/modules/{module}/integration/test_{domain}_integration.py  # Optional: integration with real DB
```

**Example** (CRM):
- `test_identity_service.py` — unit tests for `IdentityService` (mocked repo)
- `test_lead_service.py` — unit tests for `LeadService` (mocked repo)
- `test_contacts_api.py` — integration tests for `GET /api/v1/contacts` endpoint (real DB)

## 6. Critical active decisions (D8, D10, M3)

**D8** (not numbered in PI.md, mentioned in Riesgos via M3):
- Tests + polluter hunt: **Builder PR-1 handles polluter snapshot test + singleton fixture resets**. PR-2 is pure coverage (no polluter/fixture scope).

**D10** (implicit from "Riesgos" cross-surface row):
- Cross-surface (business + agentic) in same PR-1: **2 builders paralelos** (`nicolify-backend` + `nicolify-agentic`) with exclusive paths per surface. `conftest.py` shared = lock prevention via builder negocio.
- **For PR-2:** NO cross-surface conflict (pure test coverage, isolated modules).

**M3 (Parallel-safety.md, rule 3):**
- **Tests/CI/Docker SEQUENTIAL.** Una sesión a la vez `/test-all`/`/dev-up`/`make ci-parity`. Container/port collision invisible hasta crash.
- **For PR-2:** Run `pytest tests/modules/crm/` and `pytest tests/modules/scheduling/` SEQUENTIALLY (not in parallel). Root conftest cleanup between.

**Applicability to PR-2 builder:**
- Write tests RED → write code GREEN → refactor (TDD obligatory).
- CRM: service tests mock repos (unit); API tests use transactional DB (integration).
- Scheduling: both needed (services have complex business logic; APIs currently 0% covered).
- No polluter risk (coverage tests are orthogonal to snapshot/singleton problems from PR-1).
- Sequential test runs respected (per M3).

## 7. Out-of-scope boundaries

✅ **In scope PR-2:**
- Tests unitarios aplicación + infraestructura (services + repos)
- Tests integración API (transactional DB)
- Fixtures factory pattern (schedule, event_type, appointment)
- Fixture setup vía conftest (scheduling, crm)

❌ **Out of scope PR-2:**
- Cambio lógica de negocio (tests prueban behavior actual)
- Endpoints API nuevos o modificados
- Polluter hunt (PR-1 scope)
- Singleton fixture hardening (PR-1 scope)
- Frontend tests (FE coverage PI separado)
- `sales_agent`/`copilot` coverage (S2)
- Arch fitness allowlist edits sin justificación
- Default flag flips (PR-3 rule + PR-4 agents/skills)

## 8. Acceptance criteria and target coverage

| Criterio | Target | Baseline | Lift % |
|---|---|---|---|
| `crm` coverage | ≥75% | 87.15% ✅ | 0 (exceeded) |
| `scheduling` coverage | ≥75% | 43.71% | +31% |
| `crm` service unit tests | ≥90% per service | 69-100% | Variable |
| `scheduling` service unit tests | ≥85% per service | 60-88% | Variable |
| Tests pass natively (WSL) | 100% | — | CI gate |
| Arch fitness tests | 78/78 PASS | — | No new violations |
| IMPL-LOG.md | Complete | — | Builder deliverable |
| REVIEW.md | No FAIL | — | Auditor deliverable |
| RESULT.md | Written by PM | — | PM deliverable |

**Acceptance gates:**
1. `pytest tests/modules/crm/ --cov=src/modules/crm` → ≥87% (maintain)
2. `pytest tests/modules/scheduling/ --cov=src/modules/scheduling` → ≥75% (lift 31%)
3. `pytest tests/architecture/ -x` → 78/78 PASS
4. ESLint/ruff 0 errors (pre-commit hooks native)
5. No `@pytest.mark.skip` or `xfail` without justification

## 9. Faithfulness gaps

None — all source files read directly (PR.md, current-state/*.md, conftest fixtures, coverage reports via pytest native). No truncation or omission of load-bearing details.

## 10. Raw paths consulted

- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/sprints/S1-test-integrity-and-coverage/prs/PR-2-coverage-p0-modules/PR.md`
- `/home/chris/AISALESHT/docs/pm-nico/current-state/crm.md`
- `/home/chris/AISALESHT/docs/pm-nico/current-state/scheduling.md`
- `/home/chris/AISALESHT/backend/src/modules/crm/application/services/` (all 8 service files listed)
- `/home/chris/AISALESHT/backend/src/modules/crm/infrastructure/repositories/` (5 repo files listed)
- `/home/chris/AISALESHT/backend/src/modules/scheduling/application/services/` (2 service files listed)
- `/home/chris/AISALESHT/backend/src/modules/scheduling/api/` (3 API files listed, 0% coverage baseline)
- `/home/chris/AISALESHT/backend/tests/modules/crm/conftest.py` (100 lines)
- `/home/chris/AISALESHT/backend/tests/modules/crm/` (26 test files listed)
- `/home/chris/AISALESHT/backend/tests/modules/scheduling/` (6 test files listed)
- `/home/chris/AISALESHT/docs/pm-nico/pis/active/PI-11-backend-quality-guardrails/PI.md` (200 lines of context)
- `/home/chris/AISALESHT/.claude/rules/tdd-mandatory.md` (top 60 lines)
- `/home/chris/AISALESHT/.claude/rules/backend-quality.md` (full file)
- `/home/chris/AISALESHT/backend/.venv/bin/pytest` coverage runs for both modules (native WSL)
