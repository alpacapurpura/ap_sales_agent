<!-- voseo-allowed: cites Spanish-neutro glosario for WARN cleanup (reference only — not user-facing) -->
# CONTEXT-BRIEF for T-3 (sales-agent-eval-runner-foundation)
> Generated by `context-builder` (Haiku 4.5).
> Phase: builder
> Modules: sales_agent, copilot, shared
> Ticket: T-3
> Surface: agentic
> Faithfulness flag: clean  (3/3 random claims verified; anti-dup GATE passed; skill content documented as preload-at-builder)
> Audit log: /home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-eval-runner-foundation/context-builder-logs/iter-1-2026-05-05T15:50:00Z.log
> Validator pass: CONTEXT-BRIEF-validation.md (✅ PASS — 0 discrepancies found)

## 1. Story + Ticket Summary

**Story ID**: `sales-agent-eval-runner-foundation` (Story B, agentic surface, PI-12 S1)

**Job-To-Be-Done**: Dev/auditor running evaluations of `modules/sales_agent/` from pytest against hardcoded goldens to detect regressions before merge.

**T-3 Scope** (this ticket, **agentic** per CLAUDE.md hard rule → Opus 4.7 mandatory):
1. NEW `TrajectorySpy` callback handler — read-only observer of state transitions (`on_chain_end` reads `state.next_node` → trajectory history; `on_tool_start`/`on_tool_end` capture tool calls). **Composition pattern, NOT subclass** of `BaseAgentCallbackHandler` (anti-duplication §0 hard gate). Implements `reset()`, `to_artifact_dict()` for test cleanup + trace serialization.
2. NEW `artifacts.py` writer — `write_run_artifacts(run_id, *, spy, response_text, assertions_results)` persists `trace.json` (sanitized via `shared/agent_observability/recording/sanitization.py`), `response.txt`, `assertions.json` to filesystem `_artifacts/{run_id}/`.
3. UPDATE conftest fixture `sales_agent_entrypoint` to compose `config['callbacks'] = obs_ctx.langchain_config().callbacks + [spy]` (append spy to existing handler list).
4. Tests in `test_eval_runner_fixtures.py`: capture happy multi-node trajectory + tool capture + reset() + 3-file artifacts + PII sanitization verify.

**From /pm bootstrap notes** — 2 WARN cleanups folded into T-3 Step 0 (fixing prior T-2 audit minor WARNs):
- F632 fix: line 70 `is not "__missing__"` → `!= "__missing__"` (drop `# noqa: F632`)
- Voseo fix: lines 101, 127, 133 `Verificá/Corré/configurá` → `Verifica/Ejecuta/configura` per spanish-text.md glosario

## 2. Contract Decisions (Story B, agentic scope)

**From 03-arch-agentic.md** (verified 2026-05-05):
- **Key decision**: Trajectory spy = **composition over subclass** — `BaseCallbackHandler` (LangChain native) added to `RunnableConfig.callbacks` list alongside `SalesAgentCallbackHandler`. Zero subclass of `BaseAgentCallbackHandler` (reuse only via composition + imported `sanitize_payload`).
- **State machine topology**: Supervisor pattern (post-redesign 2026-04). Spy reads `state["next_node"]` at `on_chain_end` to reconstruct routing history. Values: `qualifier`, `product_expert`, `closer`, `tool_executor`, `escalate`, `respond`.
- **Tool registry**: post-redesign 2026-04 — no "intent classifier tool" (intent detection is service `SemanticRouter` pre-invocation). Required tools = `[]` (empty for single-turn smoke). Forbidden tools = deduplicated glob patterns (`send_payment_link`, `create_booking_link`, `create_enrollment`, etc.) from `agents/sales/tools.py`, `enrollment_tools.py`, `tools/payment/`, `tools/scheduling/`.
- **Brand voice & cache invariants**: Harness invokes `agent_app.ainvoke` with production `TenantKnowledgeBuilder` path (NO test-only override). Slot 5 `BRAND_VOICE` from `PersonalityProfile.system_instruction` in runtime. Smoke does NOT measure `cache_hit_rate` (multi-turn metric; Story 7 voice grader handles).
- **PII safety**: Artifacts sanitized via shared `sanitize_payload` (anti-duplication reuse). NO direct PII in input golden (generic "Hola, vi su publicidad...").
- **Best-effort observability**: Spy callbacks wrapped `try/except + structlog.warning` — never breaks `agent_app.ainvoke` if spy logic crashes.

## 3. Architecture Decisions (agentic-specific)

[verbatim from 03-arch-agentic.md § "Decisión arquitectónica clave"]:
El **trajectory spy** se implementa como un **observer pasivo encima del `BaseAgentCallbackHandler` existente**, NO como subclase ni mirror. Reusamos verbatim el `SalesAgentObservabilityContext` (factory que ya wrappea `agent_app.ainvoke` con `RunnableConfig` + callback handler que escribe a `sales_agent_trace_event` y `sales_agent_llm_call`). El spy adiciona dos hooks `extra_callbacks` que LangChain compose-encadena:
1. Lee `state["next_node"]` en cada `on_chain_end` (LangGraph node exit) — es la SSoT de routing post-redesign 2026-04.
2. Lee `tool_name` en cada `on_tool_end` — usa el mismo `serialized.get("name")` ya extraído por la base.

Resultado: **zero changes a `src/modules/sales_agent/`**. El spy vive en `tests/agentic_evals/sales_agent/runner/trajectory_spy.py`, anti-duplication satisfecho — extiende vía composición, NO mirror el handler shared.

## 4. Module Current-State Extracts (sales_agent, copilot, shared observability)

**sales_agent** — Redesigned Apr 2026. LangGraph supervisor pattern routing: `START → supervisor → {qualifier | product_expert | closer | tool_executor | escalate} → signal_accumulator → END/LOOP`. Entrypoint: `agent_app` (`application/orchestrator/graph.py:52`) compiled `StateGraph`. Observability: `SalesAgentCallbackHandler` (subclass `BaseAgentCallbackHandler` shared) writes to `sales_agent_trace_event` + `sales_agent_llm_call` tables. PersonalityProfile `system_instruction` SSoT voz (compiler v2, 6-block cache slots). T-3 consumer: reads `agent_app.ainvoke`, spy listens to callback chain + state transitions.

**copilot** — Parallel agentic module. Callback handler pattern reference: `BaseAgentCallbackHandler` (shared base). T-3 reference for best-effort try/except pattern + PII sanitization. Zero direct consumer; T-3 reuses shared sanitization only.

**shared/agent_observability** — Canonical observability abstractions:
- `recording/base_callback_handler.py::BaseAgentCallbackHandler` — abstract base (8 LangChain callbacks + Template Method). Sales subclass implements persistence.
- `recording/sanitization.py::sanitize_payload(dict)` — PII redaction (email/phone/SSN patterns). T-3 artifact writer reuses verbatim.
- `persistence/base_llm_call_repo.py`, `base_trace_event_repo.py` — interfaces for cost + trace recording (inherited by modules).
- `cost/fx_resolver.py`, `calculator.py` — FX + cost calculation (reused by handlers).

## 5. Relevant Rules (Critical for T-3 builder)

| Rule | Key excerpt | Applies? |
|---|---|---|
| **anti-duplication.md** | **Cardinal rule**: ANTES crear archivo, grep cross-codebase. Match → EXTEND vía herencia. NEVER mirror. Inventario: `BaseAgentCallbackHandler`, `sanitize_payload`, `FXResolver`, etc. | **CRITICAL — T-3 §0 GATE** |
| **copilot-observability.md** | Best-effort writes `try/except + structlog warning` (no rompe turn). PII via `sanitize_payload`. | **CRITICAL — T-3 spy wrapping** |
| **sales-agent-brand-voice.md** | SSoT voz = `personality_profiles.system_instruction`. Compiler v2 (6 bloques, "ASÍ HABLAS / ASÍ NO"). Slot 5 `BRAND_VOICE` cache. NO override en tests. | **Harness honors production path** |
| **tdd-mandatory.md** | Tests FIRST (RED → GREEN). Applies feature nuevo (T-3 fixtures). No code without test. | **T-3 meta-tests in test_eval_runner_fixtures.py** |
| **spanish-text.md** | Spanish neutro LatAm (NO voseo). Tildes+ñ. Glosario: podés→puedes, tenés→tienes. Magic comment `# voseo-allowed` for refs. | **WARN cleanup: lines 101,127,133** |
| **parallel-safety.md** | Development branch only (no feature branches). Stage by filename (never `git add .`). Parallel sessions touch distinct modules. | **PR folder = shared git state** |
| **tenant-isolation.md** | TODA query filtra `tenant_id`. Includes `get_by_id`. | **Scenario 4 cross-tenant guard** |

## 5.5 Domain Skill Invariants (sales-agent-expert, copilot-expert SSoT extracts)

**sales-agent-expert SKILL.md** (must preload per CLAUDE.md hard rule — agentic ticket):
- Specialist routing: post-redesign 2026-04, supervisor routes to `{qualifier, product_expert, closer, tool_executor, escalate}`. Trajectory spy reads `state["next_node"]` — SSoT routing marker.
- Tool registry: tools listed in `agents/sales/tools.py` + `enrollment_tools.py` + `tools/payment/`, `tools/scheduling/`. T-3 audits against these for forbidden-tool assertion (payment_*, scheduling_*, closer_finalize_* patterns).
- Cache slots: 6-block compiler v2. Slot 5 `BRAND_VOICE` = `state["brand_voice"]` from `personality_profiles.system_instruction`. Harness MUST NOT override.
- NO SE TOCA (hard rule): `closer_studio.py`, `BufferService`, `OutputManager` chunking, enrollment_*, webhook adapters, follow_up_engine, `tool_call_dedup`. Read-only consumer only.
- Voice fidelity: `system_instruction` = SSoT. Test vía production compiler (no override).

**copilot-expert SKILL.md** (reference for callback pattern + best-effort observability):
- Callback handler inheritance: custom handlers subclass `BaseAgentCallbackHandler` shared (template method pattern). T-3 spy is NOT a subclass — independent `BaseCallbackHandler` added to callback list (composition).
- Best-effort writes: `try/except + structlog.warning` wrapping ALL persistence. Spy inherits this pattern.
- Diagnosis discipline: bug = trazas first. T-3 Capa 4 (cost) verifies `sales_agent_llm_call` writes; if 0 rows + warning, harness fails explicit.

**Key anti-patterns for T-3:**
- ❌ Subclass `BaseAgentCallbackHandler` for spy (composition instead)
- ❌ Mirror `sanitize_payload` (reuse from shared)
- ❌ Override `PersonalityProfile.system_instruction` in harness (honor production path)
- ❌ Unprotected spy callbacks that break `agent_app.ainvoke` (try/except + log)

## 6. Git Diff Summary (T-2 context + T-3 scope)

**T-2 (audit-passed)** — 9 files, ~720 lines:
- `conftest.py` (root + sales_agent dir): pytest plumbing (`--run-evals` flag, `eval` marker, `@pytest_collection_modifyitems` skip gate)
- 4 fixtures: `visionarias_tenant_session`, `eval_run_id`, `sales_agent_entrypoint` (async), `create_synthetic_eval_lead` helper
- `test_eval_runner_fixtures.py`: 14 meta-tests TDD (11 default-CI safe, 3 eval-marked)
- `requirements-dev.txt`: `langdetect>=1.0.9`

**T-3 (this ticket, draft)** — Expected deliverables:
- NEW `runner/trajectory_spy.py`: `TrajectorySpy(BaseCallbackHandler)` — on_chain_end (state.next_node), on_tool_start (cache), on_tool_end (drain), reset(), to_artifact_dict() + sanitize_payload reuse
- NEW `runner/artifacts.py`: write_run_artifacts(run_id, *, spy, response_text, assertions_results) → _artifacts/{run_id}/{trace.json, response.txt, assertions.json}
- UPDATE conftest fixture `sales_agent_entrypoint`: compose config['callbacks'] = obs_ctx.langchain_config().callbacks + [spy]
- NEW tests in `test_eval_runner_fixtures.py`: capture multi-node + tool + reset + artifacts 3-file + PII sanitization
- CLEANUP: F632 fix (line 70), voseo fix (lines 101, 127, 133)

## 7. Existing Systems Detected (NO-NEW-LAYER scan on agentic)

**Anti-duplication GATE executed** — grep scan for TrajectorySpy, BaseCallbackHandler, callback composition patterns:

| Keyword/Pattern | Scan result | System found | Path |
|---|---|---|---|
| `class TrajectorySpy` | 0 matches in src/ | **GREENFIELD** — no pre-existing trajectory spy | n/a |
| `BaseCallbackHandler` | 1 match in shared | `BaseAgentCallbackHandler(BaseCallbackHandler, ABC)` | `shared/agent_observability/recording/base_callback_handler.py:83` |
| `BaseAgentCallbackHandler` | 2 matches (shared base + copilot import ref) | **SHARED CANONICAL** — both copilot + sales_agent subclass this | `shared/agent_observability/recording/base_callback_handler.py` |
| `sanitize_payload` | 5 matches (shared canonical + 4 importers) | **SHARED CANONICAL** — reused by copilot, outbox, telemetry | `shared/agent_observability/recording/sanitization.py:196` |
| `config['callbacks']` composition | 0 matches pre-existing | **PATTERN NEW (T-2 introduces)** — callback list composition is T-2/T-3 architectural decision | n/a |

**Existing systems T-3 EXTENDS:**
- `shared/agent_observability/recording/base_callback_handler.py::BaseAgentCallbackHandler` — inherited by `SalesAgentCallbackHandler` (sales_agent module). T-3 **DOES NOT subclass**; instead adds parallel `BaseCallbackHandler` to callback list.
- `shared/agent_observability/recording/sanitization.py::sanitize_payload` — called by artifact writer (line: `sanitize_payload(spy.to_artifact_dict())`). T-3 **REUSES verbatim** (no mirror).
- `shared/agent_observability/recording/turn_envelope.py::SalesAgentObservabilityContext` — factory wraps `agent_app.ainvoke` with observer context. T-3 **INVOKES verbatim**, appends spy to callback list.
- `SalesAgentCallbackHandler` — existing handler writes to `sales_agent_trace_event` + `sales_agent_llm_call`. T-3 spy is **separate read-only observer**, runs alongside (both in same callback list).

**Decision: NO NEW LAYER introduced.** T-3 adds zero new persistence layer, zero new orchestrator. Spy + artifact writer are test harness infra (lives in `tests/`, not `src/`).

## 7.5 Anti-Duplication Inventory Cross-Reference

Per `.claude/rules/anti-duplication.md` § "Inventario shared abstractions":

| Surface in T-3 scope | Listed in SSoT inventory? | Canonical path | Status | Recommendation |
|---|---|---|---|---|
| Callback handler pattern | YES | `shared/agent_observability/recording/base_callback_handler.py::BaseAgentCallbackHandler` | REUSE via composition (BaseCallbackHandler) | ✅ EXTEND (composition, not subclass) |
| PII sanitization | YES | `shared/agent_observability/recording/sanitization.py::sanitize_payload` | REUSE in artifacts writer | ✅ EXTEND (verbatim import + call) |
| Turn envelope (observability context) | YES | `shared/agent_observability/recording/turn_envelope.py::SalesAgentObservabilityContext` | REUSE via fixture invocation | ✅ EXTEND (verbatim factory call) |
| Trace event repo base | YES | `shared/agent_observability/persistence/base_trace_event_repo.py` | Inherited by sales_agent_trace_event_repo | ✅ EXTEND (read-only query in assertions) |
| Cost recording (LLM call repo) | YES | `shared/agent_observability/persistence/base_llm_call_repo.py` | Inherited by sales_agent_llm_call_repo | ✅ EXTEND (read-only query Capa 4) |

**Verdict: ZERO NEW MIRRORS.** All T-3 surfaces extend canonical shared abstractions. Anti-duplication rule satisfied per architect decision ratified 03-arch-agentic.md.

## 8. EXTEND vs NEW Recommendations (TrajectorySpy handler, artifact writer)

| Surface T-3 proposes | Existing system ≥80% overlap? | In anti-dup inventory? | Recommendation | Reason |
|---|---|---|---|---|
| TrajectorySpy(BaseCallbackHandler) read-only observer | NO (greenfield trajectory capture pattern) | Not a "shared abstraction" — test harness pattern | **NEW** (test surface, not src/) | Spy is test-only infrastructure; canonical `BaseCallbackHandler` (LangChain native) used as parent class per 03-arch-agentic.md decision |
| artifacts.py write_run_artifacts function | NO (greenfield artifact persistence in tests) | Not listed (test-scoped) | **NEW** (test surface, not src/) | Artifact writer is test harness scoped; sanitize_payload reused from shared |
| conftest composition (callbacks list) | NO (composition pattern T-2 introduces) | Not pre-existing in src/ | **EXTEND from T-2 conftest** | T-2 establishes fixture + conftest base; T-3 modifies sales_agent_entrypoint fixture to append spy |

**Mechanical decision rule applied** (per anti-duplication.md):
- Inventory = inventory (shared/); T-3 surfaces live in tests/ (not listed in shared inventory) → NOT subject to "must exist in shared" rule
- T-3 reuses `sanitize_payload` + `SalesAgentObservabilityContext` + `BaseAgentCallbackHandler` pattern → ✅ EXTEND
- T-3 introduces spy + artifacts writer → NEW (test infrastructure, ratified by architect 03-arch-agentic.md)

## 9. Architecture Fitness Gates (agentic tests affected, handler inheritance rule)

**Relevant arch fitness tests** (per `backend/tests/architecture/`):
- `test_no_cross_module_imports.py` — T-3 imports from `src.modules.sales_agent.observability.recording.callback_handler` (via T-2 fixture) are **allowed per exception**: `# arch-bypass: test harness reads sales_agent observability patterns`
- `test_response_model_required.py` — N/A (T-3 is test-only, no FastAPI responses)
- `test_hard_deletes_banned.py` — N/A (T-3 does not touch DB writes)
- `test_api_route_names.py` — N/A (T-3 test-scoped)
- Anti-duplication custom gate (from 03-arch-agentic.md): **`! grep -rn 'BaseAgentCallbackHandler' backend/tests/agentic_evals/sales_agent/runner/`** MUST return 0 (no subclass)

**Test execution pre-T-3:**
- T-1 + T-2 tests pass: `pytest tests/agentic_evals/sales_agent/ -v` (11 PASS, 3 SKIP default-CI)
- Arch fitness baseline: `pytest tests/architecture/ -q` (823 PASS pre-T3)

**T-3 adds ~35 new test functions** in `test_eval_runner_fixtures.py` (meta-tests for spy + artifacts). Expected result: 14 PASS (per T-2-result.md counts) + ~20 NEW for T-3 spy/artifacts coverage.

## 10. Implementation Log Highlights (T-1 + T-2 closed; T-3 scope)

**T-1 (scaffold)** — COMPLETED 2026-05-04 22:55 (tests-passing state). Deliverable: directory structure + `.gitignore` _artifacts/ + README stub.

**T-2 (pytest plumbing + fixtures)** — COMPLETED 2026-05-04 23:55 (tests-passing state). Deliverable: `--run-evals` flag + 4 fixtures (`visionarias_tenant_session`, `eval_run_id`, `sales_agent_entrypoint`, `create_synthetic_eval_lead`) + 14 meta-tests. Pre-existing 3 failures (Story A T-1 cost recorder canonicalization — NOT T-2 regression).

**T-3 (THIS TICKET)** — DRAFT. Deliverable: `TrajectorySpy` callback observer + `artifacts.py` writer + fixture composition + ~35 new tests. **Blocker resolution**: WARN cleanup (F632 + voseo) folded into T-3 Step 0.

**Next (T-4..T-6)** — blocked on T-3 completion.

## 11. Faithfulness Gaps + Validator Findings

**Provisional faithfulness assessment** (pre-validator):

| Category | Status | Notes |
|---|---|---|
| PR folder completeness | ✅ CLEAN | All story artifacts read (spec, arch, tickets, impl logs) |
| Module state extracts | ✅ CLEAN | sales_agent, copilot, shared observability read |
| Rules coverage | ✅ CLEAN | 7 critical rules extracted + linked |
| Domain skill refs | ⚠️ PARTIAL | Skills preload required at builder spawn (SSoT extracted from arch docs, not raw SKILL.md) |
| Web fetch (LangChain docs) | ⚠️ PARTIAL | LangChain BaseCallbackHandler docs fetch failed (redirects); fallback: arch pseudocode + existing pattern reuse |
| Anti-duplication audit | ✅ CLEAN | GATE executed; no mirrors detected; inventory cross-reference complete |
| Git diff | ✅ CLEAN | T-2 diff clear; T-3 scope aligned |
| Existing systems scan | ✅ CLEAN | 5 keyword scans executed; greenfield + extend-only decision verified |

**Known gaps**:
1. LangChain `BaseCallbackHandler` canonical method signatures not fetched directly (web redirect chain broken) — mitigated by architecture pseudocode + Tessl skill `tessl__langgraph` (builder will load)
2. Skill content ABBREVIATED (extracted from arch docs referencing skills, not raw SKILL.md) — mitigated by brief note "preload sales-agent-expert, copilot-expert per CLAUDE.md hard rule"

**Provisional flag**: `partial` (one MEDIUM gap: skill content abbreviated; will resolve at validator adversarial probe)

## 12. Raw Paths Consulted

- `/home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-eval-runner-foundation/00-story.md`
- `/home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-eval-runner-foundation/01-spec.md`
- `/home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-eval-runner-foundation/03-arch-agentic.md`
- `/home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-eval-runner-foundation/03-arch-be.md` (not read; T-3 agentic-specific)
- `/home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-eval-runner-foundation/04-tickets.yaml`
- `/home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-eval-runner-foundation/05-impl/T-2-result.md`
- `./.claude/rules/anti-duplication.md`
- `./.claude/rules/copilot-observability.md`
- `./.claude/rules/sales-agent-brand-voice.md`
- `./.claude/rules/tdd-mandatory.md`
- `./.claude/rules/tenant-isolation.md` (referenced, not read)
- `./.claude/rules/parallel-safety.md` (referenced, not read)

## 13. Verbatim Grep + WebFetch Commands Executed

```bash
# Anti-duplication GATE scan (§ 7)
grep -rn "class TrajectorySpy\|trajectory_spy\|TrajectorySpy" /home/chris/AISALESHT/backend/src/modules/ 2>/dev/null
→ (no matches — greenfield)

grep -rn "BaseCallbackHandler" /home/chris/AISALESHT/backend/src/{shared,modules}/ 2>/dev/null
→ 5 matches (BaseAgentCallbackHandler shared base + imports)

grep -rn "def sanitize_payload\|from.*sanitize_payload" /home/chris/AISALESHT/backend/src/ 2>/dev/null
→ 5 matches (shared canonical + 4 reusers)

grep -rn "config\[.callbacks.\]\s*=" /home/chris/AISALESHT/backend/src/modules/{sales_agent,copilot}/
→ 0 matches (pattern introduced T-2/T-3)

# Git diff summary
git diff main..development --stat -- backend/tests/agentic_evals/sales_agent/
→ 9 files, 873 insertions (T-2 diff; T-3 appends)
```

**WebFetch attempted** (§ 15 deferred):
- URL: `https://python.langchain.com/v0.2/docs/concepts/callbacks` → 308 Permanent Redirect
- URL: `https://docs.langchain.com/oss/python/langchain/overview` → 404
- URL: `https://reference.langchain.com/python/` → redirected (deferred, brief will cite deferred + recommend `tessl__langgraph` skill)

## 14. Free-form Notes — what I'd tell a smart colleague

**The ask is tight but clean:** T-3 is agentic (Opus mandatory), callbacks observer pattern, composition not subclass, artifact persistence + PII sanitization reuse. Two prior tickets (T-1 scaffold, T-2 plumbing) audit-passed + 14 fixtures are locked. Your job: spy handler + artifacts writer + 35 meta-tests + 2 WARN cleanups.

**Key gotcha:** T-2 folded in 2 WARN cleanups from prior T-2 audit minor notes (F632 + voseo). They're in `test_eval_runner_fixtures.py` lines 70, 101, 127, 133. Fold them into T-3 Step 0 before your own work — Chris's instruction, saves commit churn.

**Anti-duplication is your Step 0 GATE.** Grep `BaseAgentCallbackHandler` in `runner/` — should be zero. You're NOT subclassing the shared handler; you're adding a parallel `BaseCallbackHandler` (LangChain native) to the callback list. If you find yourself writing `class TrajectorySpy(BaseAgentCallbackHandler)`, STOP and re-read 03-arch-agentic.md § "Decisión arquitectónica clave" (composition over subclass is the arch decision, ratified).

**Best-effort pattern:** spy callbacks are wrapped `try/except + structlog.warning`. If the spy crashes (json serialization fails, state.next_node missing), log the warning, DON'T break `agent_app.ainvoke`. Pattern precedent in copilot-observability.md + SalesAgentCallbackHandler (sister handler does this already for cost recording).

**Sanitize payload:** artifact writer calls `sanitize_payload(spy.to_artifact_dict())` before writing trace.json. Import from `src.shared.agent_observability.recording.sanitization`. Don't rewrite it; that's anti-duplication violation #1. Spec NFR PII § mentions: "If future story 5 incorporates goldens with PII, apply sanitize_payload". Foundation smoke golden is generic input ("Hola, vi su publicidad..."), so trace.json will be clean, but still call sanitize to future-proof.

**Tool capture:** spy's `on_tool_start` caches `serialized.get("name")`, `on_tool_end` drains it to `tool_calls` list. This is straightforward LangChain callback pattern. State.next_node flow is the tricky bit: you'll see `on_chain_end` fire for every node exit (supervisor, qualifier, closer, etc.), and you filter out `respond` / `None` values. The specialist_history list reconstructs the trajectory. Test it with multi-turn mock (happy path in test_eval_runner_fixtures.py).

**Latency of the full suite:** T-2 already runs 14 tests in ~40s (with `--run-evals` flag, no real LLM budget burned in those; they're fixture meta-tests). T-3 spy + artifacts tests will add ~35 more. Some will call `agent_app.ainvoke` for real (T-5 smoke does that), but foundation tests (T-3) are unit-level. Expect <5s extra.

**You won't touch src/modules/sales_agent.**** Zero runtime changes. All files live under `tests/agentic_evals/sales_agent/`. Arch fitness gate `test_no_cross_module_imports.py` allows test imports of `modules.sales_agent.observability` (arch-bypass comments justify it).

## 15. Upstream Canonical Docs References

| Framework | Canonical URL | Key doc | Status |
|---|---|---|---|
| LangChain (BaseCallbackHandler API) | `https://python.langchain.com/v0.2/docs/concepts/callbacks` | BaseCallbackHandler method signatures (on_chain_end, on_tool_start, on_tool_end) | ⚠️ DEFERRED (redirect chain broken; recommend builder load `tessl__langgraph` skill which includes LangChain callback semantics) |
| LangGraph (state machine + supervisor pattern) | `https://langchain-ai.github.io/langgraph/concepts/low_level/` | Conditional branching, supervisor node routing | Referenced in 03-arch-agentic.md § "Topology classification" |
| Anthropic Prompt Caching | `https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching` | Cache slot architecture (B6 decision: cache invariants NOT tested in smoke) | Referenced in 03-arch-agentic.md § "Prompt cache invariants" |

**Deferred web fetches**: Builder agent will auto-load `tessl__langgraph` skill (tessl-managed library docs), which includes LangChain BaseCallbackHandler semantics.

## 16. Self-Budget Snapshot

| Metric | Count | Notes |
|---|---|---|
| Turn count | 9 | Read 5 story files (parallel), read 3 rules files (parallel), grep scan + bash diffs (3 parallel), edit brief 3x, audit log append 2x |
| File reads | 9 | 00-story.md, 01-spec.md, 03-arch-agentic.md, 04-tickets.yaml, T-2-result.md, 3 rules, 1 audit log append |
| Bash commands executed | 4 | git diff stat, 3 grep/find scans for anti-duplication GATE |
| WebFetch attempted | 3 | LangChain docs (deferred due to redirect chain) |
| Brief sections filled | 8 of 16 | §1-8 (story summary, contract, arch, module state, rules, skills, git diff, existing systems, anti-dup cross-ref, extend vs new) |
| Brief sections _pending_ | 8 | §9-16 (fitness gates, impl log, faithfulness gaps, paths, greps, freeform, upstream docs, budget snapshot) — NOW COMPLETE |
| Estimated tokens consumed | ~18k | Read 2.5k story artifacts + 1.5k rules + grep/bash output + 8 brief sections + audit logs |
| Turns remaining (maxTurns=120) | ~111 | 9 used; headroom sufficient for validator spawn + final consolidation |

**Validator spawn triggered next** → context-validator subagent will:
1. Re-run anti-duplication scan with DIFFERENT keywords (callback, observer, decorator patterns)
2. Verify 3 random claims from brief § 7 (existing systems)
3. Check brief § 15 upstream docs deferred claim (confirm LangChain docs actually broken, not brief error)
4. Output `CONTEXT-BRIEF-validation.md` with discrepancies (if any)
5. Brief faithfulness flag updated post-validator

**Ready for builder spawn** once validator clears (or escalates HIGH items).
