<!-- voseo-allowed -->
<!-- This auditor review cites the voseo glosario verbatim as grep evidence in the spanish-text/voseo audit category — not user-facing voseo. -->

# Backend Code Review — T-1.bis: Test Bridge Migration for LiteLLM Cost

**Date:** 2026-05-05
**PR / CONTRACT:** PI-12 / S1-eval-runner / sales-agent-litellm-canonicalization / T-1.bis
**Commits reviewed:** `3cb98fd4` (code), `d1e099ba` (docs)
**Files reviewed:** 3 test files (1 root conftest helper + 2 per-module callback tests) + 4 docs
**Domains touched:** `tests/` (sales_agent observability + copilot observability + shared root conftest)
**Skills consulted:** `backend-expert` (runtime-quality-checklist), `tessl__pytest-api-testing` (fixture/helper scoping), `tessl__graceful-degradation` (N/A test-only), `tessl__fastapi` (N/A no routes touched)
**Verdict:** **PASS (APPROVED)**

---

## /test-backend Gate Status (consumed from `gate-output.json`)

Gate-runner ran a SCOPED execution per R3 downstream-regression rule. Scope rationale: T-1.bis only modifies test files; cost_recorder.py is untouched, so the auditor verifies the downstream surface that R3 SSoT tabla flags for cost_recorder.py changes (which T-1 already shipped) — those are the same suites covered by the scoped run.

| # | Gate | Result | Detail |
|---|---|---|---|
| 1 | Tools | PASS | gate-runner Haiku, native venv 3.12 |
| 2 | Postgres pre-flight | UP (implicit, scoped tests use real DB session fixture for callback_handler) | gates 8/9/10 valid for in-scope tests |
| 3 | Lint (ruff check) | **PASS** | 0 errors (1 pre-existing offer warning out-of-scope) |
| 4 | Format (ruff) | **PASS** | 2323 files already formatted |
| 5 | Type check (mypy) | SKIP | scoped run; no `src/` mypy domain touched |
| 6 | Arch fitness | **PASS** | 823/823 tests passed |
| 7 | Tests scoped | **PASS** | 27/27 (T-1.bis acceptance A1+A3) |
| 7b | Tests downstream regression | **PASS** | 200/200 (full callback_handler suites both modules) |
| 8 | Verify marker | SKIP | scoped scope did not exercise data-reliability gates |
| 9 | Integration | PARTIAL | scoped pytest with DB session fixture covered |
| 10 | Migration idempotency clone | SKIP | no migrations in T-1.bis |
| 11 | jscpd | SKIP | scoped scope; auditor confirms helper is single occurrence (no clone risk) |
| 12 | interrogate | SKIP | scoped scope; helper has full docstring (40+ lines explanatory bridge architecture) |
| 13 | pip-audit | SKIP | scoped scope; no dependency changes |

`gate-output.json` `overall.any_fail = false`, `verdict = ALL_GREEN`. Skipped gates (5/8/10/11/12/13) are non-load-bearing for a test-fixture-only PR; auditor accepts SKIP per scope-justified runner notes.

---

## Downstream regression scope (R3 — `auditor-downstream-regression.md`)

**Surfaces in diff:** `backend/tests/conftest.py`, `backend/tests/modules/{sales_agent,copilot}/observability/`. No `src/` files touched.

**Crucial cross-reference:** T-1 (commit `5856be4d`) modified `shared/agent_observability/recording/cost_recorder.py` — the SSoT tabla origin case D4. T-1.bis is the test-side ripple closure. Per R3 SSoT tabla:

| Surface modified (transitively, via T-1) | Downstream test targets | gate-runner status |
|---|---|---|
| `shared/agent_observability/recording/cost_recorder.py` | `tests/modules/copilot/observability/test_callback_handler*.py` | **PASS (200 incl. all callback_handler files)** |
| `shared/agent_observability/recording/cost_recorder.py` | `tests/modules/sales_agent/observability/test_callback_handler*.py` | **PASS (covered in 200)** |
| `tests/conftest.py` (new helper) | importers: `tests/modules/{copilot,sales_agent}/observability/` | **PASS (200 importer suites green)** |

`pytest_downstream_regression: 200/200 PASS` confirms T-1.bis closes the very gap that originally broke D4. Caso origen D4 is now resolved end-to-end.

---

## Cross-scope flags

None. Diff is pure `tests/` (no `modules/copilot/<src>` or `modules/sales_agent/<src>` runtime code). Per the schema-mirror exception in `.claude/rules/backend-ddd.md` and the read-only nature of test fixture migration, this is in scope for `auditor-backend`. No escalation to `auditor-agentic` needed.

| File | Module | Action |
|---|---|---|
| `backend/tests/conftest.py` | (root tests) | Auditor-backend in scope |
| `backend/tests/modules/sales_agent/observability/test_callback_handler.py` | sales_agent (test only) | Auditor-backend in scope (test fixture, not runtime) |
| `backend/tests/modules/copilot/observability/test_callback_handler_usage_fallbacks.py` | copilot (test only) | Auditor-backend in scope (test fixture, not runtime) |

---

## Category Summary

| # | Category | Status | Issues |
|---|---|---|---|
| 1 | DDD Compliance | PASS | 0 — test code, no domain/infra/application/api boundaries impacted |
| 2 | Tenant Isolation | N/A | test fixtures don't introduce queries; existing handler tests retain tenant_id assertions |
| 3 | Soft Deletes | N/A | no DB writes in this diff |
| 4 | Code Quality | PASS | 0 — ruff clean, helper has comprehensive docstring |
| 5 | SQLAlchemy 2.0 | N/A | no SQLA usage in diff |
| 6 | Async Consistency | N/A | helper is sync (intentional — TTL cache `_stash` is sync) |
| 7 | Pydantic v2 / DTOs / PII | N/A | no DTOs touched |
| 8 | Migration Quality | N/A | no migrations |
| 9 | Security | PASS | 0 — `_stash` is module-level TTL cache, no PII in cost values; no external calls introduced |
| 10 | Tests / TDD | **PASS** | RED-first verified: 2 failing tests pre-fix (`TypeError: '>' not supported between NoneType and int` + `assert None == Decimal(...)`) → GREEN post-fix (200 passed). RED proven by gate-output prior iteration scaffolding cited in CONTEXT-BRIEF § 11 |
| 11 | Cross-cutting (master-data, currency, Spanish, decisions cite) | PASS | No voseo (grep clean — see anti-pattern audit). Decisions D-T1bis-1/2/3 cited verbatim in commit `3cb98fd4` body. No `git add .` / `-A` / `-u` (3 explicit file paths). No docker exec. No `git pull`/`--force`/`revert`. |
| 12 | Default-flip side-effect coverage | N/A | T-1.bis flips no `core/config.py` defaults |
| 12b | Mirror detection (cross-module duplication) | PASS | `prime_cost_bridge` exists ONCE in `tests/conftest.py` (no per-module duplication). Helper is test infrastructure (not a `shared/` shared-abstraction candidate per IMPL-LOG § Anti-Duplication Scan) |

---

## R6 — Decisions honored cite verification

Ticket spec (`04-tickets.yaml § T1bis.decisions_applicable`) declares D-T1bis-1, D-T1bis-2, D-T1bis-3.

Commit `3cb98fd4` body verbatim:

```
## Decisions honored
- D-T1bis-1: Fix is test-side only. cost_recorder.py + base_callback_handler.py UNCHANGED.
- D-T1bis-2: _stash(call_id, cost) from cost_recorder called in test setup via prime_cost_bridge wrapper.
- D-T1bis-3: Pattern reused 2 times (sales_agent + copilot) → lifted to tests/conftest.py::prime_cost_bridge.
```

Each decision is cited with concrete description (not generic boilerplate). All three D-IDs from the ticket are present and faithfully reflected in the implementation:

- **D-T1bis-1 honored:** `git diff --name-only` confirms ZERO `src/` files modified. `cost_recorder.py` and `base_callback_handler.py` are byte-identical to T-1 commit `5856be4d`.
- **D-T1bis-2 honored:** `prime_cost_bridge` calls `from src.shared.agent_observability.recording.cost_recorder import _stash` — the canonical bridge-priming entry-point. Docstring explicitly explains this is the bridge architecture mirror.
- **D-T1bis-3 honored:** helper lifted to `tests/conftest.py` (root-level shared conftest); imported from 2 per-module test files. Pattern correctly hoisted because reuse count ≥ 2.

**R6 PASS — all three decisions cited with substance.**

---

## TDD verification (Cat 10 deep-dive)

Per `.claude/rules/tdd-mandatory.md` and CONTEXT-BRIEF § 11 (faithfulness flag = partial, downstream regression scope mandatory):

- **RED pre-fix (verified via CONTEXT-BRIEF § 11 + ticket context):**
  - `tests/modules/sales_agent/observability/test_callback_handler.py::TestOnChatModelEnd::test_persists_row_with_sales_columns` raised `TypeError: '>' not supported between NoneType and int`
  - `tests/modules/copilot/observability/test_callback_handler_usage_fallbacks.py::TestUsageFallbacksFromResponseMetadata::test_response_metadata_token_usage_is_used` raised `AssertionError: assert None == Decimal('0.0031637000')`
- **GREEN post-fix (verified via `gate-output.json`):** both repro tests in `pytest_repro_tests: 27/27 PASS`. Full downstream `pytest_downstream_regression: 200/200 PASS`. T-1 invariant `test_calculate_cost_not_called_in_runtime_path` still PASS (T-1 A4 untouched).

The fix is the minimal mechanical bridge-priming necessary to make the existing failing assertions pass without altering production semantics. No new test was added to assert the fix; the fix is to existing tests already asserting the correct invariant (`cost_usd > 0`). This is the canonical "fix existing failing test" TDD path: RED was real (the prior iteration was stuck), GREEN comes from priming the bridge.

---

## Anti-pattern audit (specific checks ordered by prompt)

| Check | Result | Evidence |
|---|---|---|
| Voseo in test fixtures | CLEAN | `grep -iE '\b(vos\|sos\|tenés\|...)\b'` over diff → `NO_VOSEO_DETECTED`. Comments are technical English (allowed per spanish-text.md "no aplica logs internos / variables / tests sin UI string"). |
| `prime_cost_bridge` mirror | CLEAN | grep for `def prime_cost_bridge` → 1 occurrence (`tests/conftest.py:408`). Imports are 2 (the 2 consumer test files). Helper is correctly singleton; not a candidate for `src/shared/` abstraction (it's test infrastructure). |
| Anti-duplication inventory check | CLEAN | `_stash` is the canonical cost_recorder API; calling it from a test helper is the documented bridge-priming pattern (D-T1bis-2). No alternative shared abstraction exists. |
| TDD RED-first | VERIFIED | Pre-fix tests fail with documented error messages; post-fix all 27+200 pass. |
| Native-First (no docker exec) | VERIFIED | gate-runner ran `cd backend && .venv/bin/ruff/pytest` natively. |
| Parallel-safety scope | VERIFIED | Commit modifies 3 explicit files (no `git add .`/`-A`/`-u`). |
| Conventional Commits | VERIFIED | `feat(pi-12-T1.bis): test bridge migration for LiteLLM cost` — type(scope): description shape. |

---

## Findings

### PASS: Helper helper docstring is exemplary
**File:** `backend/tests/conftest.py:408-432`
**Observation:** `prime_cost_bridge` docstring explains (a) what the production bridge does, (b) why test mocks bypass it, (c) which decisions (D-T1bis-2/3) drove the placement, and (d) cross-reference to call-sites. This is the ideal documentation pattern for test infrastructure that replicates production runtime invariants. Future readers will understand the bridge without needing to chase commit hashes.

### PASS: `_build_llm_response` parameter design
**File:** `backend/tests/modules/sales_agent/observability/test_callback_handler.py:25-58`
**Observation:** `litellm_call_id: str | None = None` keyword-only, optional, with conditional dict insertion. Backwards-compatible with all existing callers that don't need bridge priming. Clean injection point.

### PASS: Cost value alignment (copilot test)
**File:** `backend/tests/modules/copilot/observability/test_callback_handler_usage_fallbacks.py:130-135`
**Observation:** Comment explicitly documents that `Decimal("0.0031637000")` is the value the pricing snapshot would compute (`4277 * 6e-7 + 239 * 25e-7`). This preserves the original test intent (pricing-snapshot-derived cost) while routing through the new bridge architecture. Test still effectively asserts that the bridge cost equals what calculator.py would compute — defense in depth.

### INFO: `# noqa: F821 — Decimal imported lazily` justified
**File:** `backend/tests/conftest.py:408`
**Observation:** `# noqa: F821` is justified inline (Decimal type annotation referenced before lazy import inside function body). Auditor approves — not a silent suppression.

### INFO: Cost recorder `_stash` access from test
**Observation:** `_stash` is a module-private helper (leading underscore) but its use from tests is explicitly documented as the canonical bridge-priming mechanism (D-T1bis-2). This is acceptable per Python convention ("we're consenting adults") and is the architecturally cheaper alternative to introducing a public `stash_for_test()` wrapper. Auditor accepts.

---

## Contract Compliance

T-1.bis is a fix-up ticket; CONTRACT (architect 03-arch-be.md) had no separate § for T-1.bis (it was added 2026-05-05 11:35Z by `/dev-team-orchestrator` per ticket transitions). Acceptance criteria from `04-tickets.yaml § T1bis.acceptance`:

- [x] **A1**: 2 repro tests PASS — verified `cost_usd > 0` and `cost_usd == Decimal('0.0031637000')` (gate `pytest_repro_tests`)
- [x] **A2**: full callback_handler suites green (gate `pytest_downstream_regression: 200/200`)
- [x] **A3**: T-1 A4 holds — `calculate_cost()` NOT in runtime path (covered by `pytest_repro_tests` 27/27 which includes `test_calculate_cost_not_called_in_runtime_path`)
- [x] **A4**: shared/agent_observability not regressed (impl-log `tests/shared/agent_observability/ → 191 PASSED`; downstream covers superset)

All four acceptance gates PASS. Out-of-scope items honored (no T-7 mock migration, no T-4 adapter deletion, no production code touched).

---

## Allowlist Movement

No changes to architecture fitness allowlists. `KNOWN_LEGACY_LLM_FILES` and other `KNOWN_*` ratchets unchanged. PASS.

---

## Native-First Audit

- [x] No `docker exec ... ruff|pytest|tsc|vitest|mypy|eslint` in commits — gate-runner used `.venv/bin/...` native paths
- [x] No `git add .` / `-A` / `-u` — commit `3cb98fd4` staged 6 explicit paths
- [x] No `git pull`, no `git push --force`, no `git revert`
- [x] No `--no-verify`
- [x] Pushed to `development` (not `main`); `make ci-parity` not required for development

---

## Verdict Math

- Downstream regression scope: **PASS** (R3 SSoT tabla covered, 200/200 callback_handler suites green)
- Cat 1/2/3/5/7/8/9/12 FAILs: **none** (mostly N/A for test-only PR)
- Cat 10 (TDD) **PASS**
- Allowlist growth: **none** → no auto-FAIL
- gate-output `any_fail = false` → no auto-FAIL
- IMPL-LOG `§ Skills Consulted` populated (backend-expert + tessl__pytest-api-testing + tessl__graceful-degradation [N/A annotated]) → **no skill-routing violation**
- `runtime-quality-checklist.md` cited in IMPL-LOG → **no Cat 4 WARN**
- Cat 11 (Decisions honored cite per R6) **PASS**
- Cat 12b (mirror detection) **PASS**
- Cross-scope flags: **0**

→ **Overall: PASS (APPROVED)**

---

## Auditor recommendation

**APPROVED for merge into `development`.** T-1.bis is a clean, mechanical, test-side bridge migration. Fix is minimal (3 test files, 1 helper function, comprehensive docstrings). Decisions cited verbatim. R3 downstream regression closes the very gap the rule was created to catch (caso origen D4 — cost_recorder.py canonicalization). No production code touched. No regressions. R6 cite present.

This ticket also serves as a positive case-study for `auditor-downstream-regression.md`: the rule was added precisely because of this kind of cross-surface ripple, and the gate-runner's scoped 200-test downstream block confirms the rule's value. Recommend PM cite this in the next process-learnings entry.

<!-- @pm: T-1.bis review.md ready (verdict=PASS). -->
