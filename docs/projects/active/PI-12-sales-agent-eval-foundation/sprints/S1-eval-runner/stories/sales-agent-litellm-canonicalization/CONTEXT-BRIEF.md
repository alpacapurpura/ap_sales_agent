# CONTEXT-BRIEF for T-4 — DELETE legacy LLM adapters + MANDATORY gemini.py audit

> Generated by `context-builder` (Haiku 4.5).
> Phase: builder
> Modules: copilot, sales_agent, core, shared
> Ticket: T-4 (sales-agent-litellm-canonicalization, PI-12 S1)
> Faithfulness flag: _pending_ (clean | partial | blocking)
> Audit log: `/home/chris/AISALESHT/docs/projects/active/PI-12-sales-agent-eval-foundation/sprints/S1-eval-runner/stories/sales-agent-litellm-canonicalization/context-builder-logs/iter-2-2026-05-05T15-00-00Z.log`
> Validator pass: _pending_ (path to CONTEXT-BRIEF-validation.md)

---

## 1. PR summary

**Ticket:** T-4 — DELETE 6 legacy LLM adapter files + MANDATORY gemini.py audit checklist

**Scope:** Clean-up phase of story "sales-agent-litellm-canonicalization" (PI-12 S1 T-1..T-9). Depends on T-7 (tests audit — 20+ files migrated to LiteLLMService mocks). Blocks T-5 (flag deletion) and T-8 (arch fitness assertions).

**Why:** Prior tickets T-1 (cost_recorder + CostRecorderCustomLogger bridge), T-2 (sync-pricing extension), T-3 (historical data repair) established **LiteLLM as the single runtime LLM call path**. The 6 legacy per-provider adapters (`openai.py`, `deepseek.py`, `kimi.py`, `qwen.py`, `gemini.py`, `_openai_compat.py`) are dead code post-T7 (all test mocks migrated). T-4 deletes them. **CRITICAL:** Pre-delete gemini.py audit (6-item checklist) validates all Gemini-specific quirks are replicable via LiteLLM `extra_body`. **ANY FAIL → ESCALATE CHRIS — BLOCKS MERGE** per architect decision A3 (binding, /po ratified).

**Deliverables (per 04-tickets.yaml):**

1. **MANDATORY gemini.py pre-delete audit (6 checklist items):**
   - Function calling shape via LiteLLM `extra_body` (Gemini native `tools` → LiteLLM mapping)
   - Safety settings via `extra_body` (Gemini `safety_settings` → LiteLLM parameter)
   - System instruction conversion (Gemini `system` field vs LiteLLM `system_prompt`)
   - Generation config mapping (Gemini `generation_config.temperature/max_tokens` → LiteLLM parameters)
   - Vision multipart (Gemini vision `content` array → LiteLLM image handling)
   - Streaming chunk normalization (Gemini stream `delta` → LiteLLM chunk format)
   - Each item: PASS/FAIL + evidence (code path, test verification) in commit body

2. **DELETE 6 adapter files:**
   - `backend/src/shared/infrastructure/llm/providers/openai.py`
   - `backend/src/shared/infrastructure/llm/providers/deepseek.py`
   - `backend/src/shared/infrastructure/llm/providers/kimi.py`
   - `backend/src/shared/infrastructure/llm/providers/qwen.py`
   - `backend/src/shared/infrastructure/llm/providers/gemini.py` (ONLY if audit 6/6 PASS)
   - `backend/src/shared/infrastructure/llm/providers/_openai_compat.py`

3. **AUDIT helper files** (conditional DELETE):
   - `_chat_model_resolver.py` — if zero importers post-deletion: DELETE. Else: document consumers in commit body
   - `_response_validation.py` — same logic

4. **RETAIN (no touch):**
   - `backend/src/shared/infrastructure/llm/providers/_kwargs.py` — LiteLLMService consumes for param normalization

5. **NEW test file:**
   - `backend/tests/shared/infrastructure/llm/test_litellm_gemini_function_call.py` — verifies LiteLLM proxy handles Gemini function calling correctly (acceptance A4)

**Quality gates:**
- Native lint: `ruff check src/ tests/ && ruff format --check`
- Full suite: `cd backend && pytest -x -q --tb=short` (T-7 test audit pre-requisite)
- Arch fitness: `test_no_legacy_adapter_imports` (T-8 dependency) must PASS

**Dependencies:** T-7 (tests audit — must precede). Blocks T-5, T-8.

---

## 2. Contract decisions

**[from 03-arch-be.md § 0, § 1, § 2.2]**

**Decision A3 (BINDING — /po ratified, Chris delegated):**

> "MANDATORY audit pre-delete `gemini.py` by architect-be: every kwarg/quirk verified replicable via LiteLLM (`extra_body`, `safety_settings`, `system_instruction`, function calling shape). If ANY irreplicable → ESCALATE + BLOCK T6."

**Rationale:** Gemini has material custom code quirks (vision multipart, function calling shape, safety_settings conversion). Silent deletion without LiteLLM validation = hidden failure in prod. T-4 builder MUST audit + document per 6-item checklist. Auditor MUST accept/reject BEFORE merge.

**Decision X1 — KEEP LiteLLM proxy mode:**

> "NO migration to SDK mode. +20-50ms hop tolerable for LATAM IG DM context."

Implication: T-4 does NOT refactor to `litellm.completion()` SDK calls. Remains proxy `litellm.completion(api_base=visionarias_litellm:4000, ...)`.

**Decision X2 — cost_usd source (relevant to helper file audit):**

> "`calculate_cost()` REMOVED from cost recording path post-T1; retained only as reconciliation utility."

Implication for T-4: `_response_validation.py` reads cost validation. Check if legacy adapters called it. If all call sites deleted (adapters gone) → DELETE file. If PricingResolver or billing tools still use → RETAIN + document.

**Rollback constraint:**

Flag `LITELLM_PROXY_ENABLED` deleted in T-5 (no in-flight toggle). Emergency rollback = `git revert <T4+T5 commits>`. Pre-deletion audit + T7 test PASS mitigates risk. Explicit trade-off Chris approved.

---

## 3. UI spec decisions

**N/A — Service-story, no UI surface.**

---

## 4. Module current-state extracts

**Module:** `shared` (infrastructure/llm subsystem)

**Provider adapters surface (to DELETE):**

| File | Current state | Consumers (pre-T7) | Post-T7 status |
|---|---|---|---|
| `openai.py::OpenAIService` | active, 250 LOC | router default path, 5+ test files | DEAD — all tests migrated T-7, no consumer code |
| `deepseek.py::DeepSeekService` | active, 280 LOC | router dispatch, 6+ test files | DEAD — tests migrated T-7 |
| `kimi.py::KimiService` | active, 270 LOC | router dispatch, 5+ test files | DEAD — tests migrated T-7 |
| `qwen.py::QwenService` | active, 260 LOC | router dispatch, 4+ test files | DEAD — tests migrated T-7 |
| `gemini.py::GeminiService` | active, **320 LOC (complex)** | router dispatch, 7+ test files + admin UI | DEAD (tests migrated T-7), but **A3 audit required** before delete |
| `_openai_compat.py` | helper module, 150 LOC | used by kimi/qwen/deepseek adapters | DEAD — no direct imports post-adapter deletion |

**Helper modules (to AUDIT):**

| File | Current role | Direct imports in src/ | Recommendation |
|---|---|---|---|
| `_chat_model_resolver.py::ChatModelResolver` | resolves `model:version` tuple to provider + params | Search: `grep -rn "ChatModelResolver"` in `src/modules/*/`, `src/shared/` | If zero hits: DELETE. Else: RETAIN + document |
| `_response_validation.py::validate_response` | validates LLM response shape vs provider spec | Search: `grep -rn "validate_response"` in `src/` | If zero hits (post-adapter-delete): DELETE. Else: RETAIN |
| `_kwargs.py::normalize_kwargs` | parameter name normalization (temperature→temp, etc.) | Consumed by `LiteLLMService.generate_response()` directly | **RETAIN** — LiteLLMService still needs this |

**Module consumers (post-cleanup):**

- `shared/infrastructure/llm/router.py::MultiRoleLLMRouter` — simplified T-5 to LiteLLMService singleton (not deleted, modified)
- `shared/infrastructure/llm/factory.py::LLMServiceFactory` — simplified T-5 to return `LiteLLMService` only
- `shared/agent_observability/recording/base_callback_handler.py` — **unchanged** (still the consumer of `LiteLLMService`)
- `shared/infrastructure/llm/providers/litellm.py::LiteLLMService` — **canonical** (consumes `_kwargs.py`)

---

## 5. Relevant rules

| Rule | Trigger | Key extract |
|---|---|---|
| `.claude/rules/anti-duplication.md` | Cross-codebase abstraction inventory | **Step 0 GATE:** Grep for mirror adapter classes. POST-T4: zero legacy imports allowed. Enforcement via T8 arch fitness test. |
| `.claude/rules/architectural-fitness.md` | Arch test suite | **T8 new test:** `test_no_legacy_adapter_imports` must PASS post-T4. Allowlist pattern: delete files → allowlist shrinks. |
| `.claude/rules/backend-ddd.md` | Module DDD structure | Adapter deletion = Inside-Out cleanup (app/api untouched). NO cross-module imports broken by deletion (adapters are shared infra only). |
| `.claude/rules/git-safety.md` | Commits | Stage by filename only (`git add backend/src/.../openai.py` per file, not `git add .`). Conventional commit `fix(llm): delete legacy provider adapters`. |
| `.claude/rules/parallel-safety.md` | Multi-session checkout | Work on `development`, NO feature branches. Commit immediately post-deletion. |
| `.claude/rules/tdd-mandatory.md` | Test-first | Test file `test_litellm_gemini_function_call.py` WRITTEN FIRST (RED), then adapter deletion code removed (GREEN). NO skipping tests. |
| `.claude/rules/backend-quality.md` | Lint + coverage | Native lint pass post-deletion: `ruff check` must show 0 errors. Coverage threshold ≥43% maintained (deletion doesn't reduce, tests already exist). |

---

## 5.5 Domain skill invariants (SSoT extracts)

**Skills relevant to T-4 (read SKILL.md for details, NOT invoked here):**

| Skill | Hard rules / Protected surfaces | Why relevant |
|---|---|---|
| `backend-expert` | DDD Inside-Out — deletion is infra cleanup (no domain layer change). Arch fitness tests enforce shrink-only allowlists. | T-4 is infrastructure delete; no domain rule violations. Arch fitness gates T8 validate. |
| `sales-agent-expert` | `BaseAgentCallbackHandler` shared consumer immutable (T-4 does NOT modify). LiteLLMService canonical cost path (T-1 established). | T-4 deletes old adapters; callback handler unchanged. Verify no regression post-delete. |
| `copilot-expert` | Same as sales-agent-expert (cost recording + observability callbacks are shared). | Same invariant. |

**For T-4 builder:** No direct skill invocation needed (no business logic change). Standard backend deletion best practices + architecture fitness enforcement via T8 test.

---

## 6. Git diff summary

**Expected diff post-T-4 (before audit):**

```
D  backend/src/shared/infrastructure/llm/providers/openai.py (250 LOC)
D  backend/src/shared/infrastructure/llm/providers/deepseek.py (280 LOC)
D  backend/src/shared/infrastructure/llm/providers/kimi.py (270 LOC)
D  backend/src/shared/infrastructure/llm/providers/qwen.py (260 LOC)
D  backend/src/shared/infrastructure/llm/providers/gemini.py (320 LOC) [ONLY if audit 6/6 PASS]
D  backend/src/shared/infrastructure/llm/providers/_openai_compat.py (150 LOC)
D  backend/src/shared/infrastructure/llm/providers/_chat_model_resolver.py (if no consumer)
D  backend/src/shared/infrastructure/llm/providers/_response_validation.py (if no consumer)
A  backend/tests/shared/infrastructure/llm/test_litellm_gemini_function_call.py (150-200 LOC)
```

**Estimated LOC delta:** -1,500 to -1,700 (6 adapters + helpers), +150-200 (new test) = **net -1,350 LOC**

**Modules touched:** `shared/infrastructure/llm/` (infra only, no domain/application changes)

**No other diffs expected:** T-4 is isolated cleanup (T-7 test audit already done, adapters marked for deletion pre-T4).

---

## 7. Existing systems detected (NO-NEW-LAYER scan)

**T-4 grep evidence:**

| System | Path | State | T-4 interaction |
|---|---|---|---|
| OpenAI adapter class | `shared/infrastructure/llm/providers/openai.py::OpenAIService` | active, sub-interface `BaseLLMService` | DELETE — no consumer in src/ or tests post-T7 |
| DeepSeek adapter | `shared/infrastructure/llm/providers/deepseek.py::DeepSeekService` | active, sub-interface `BaseLLMService` | DELETE — tests migrated T7 |
| Kimi adapter | `shared/infrastructure/llm/providers/kimi.py::KimiService` | active, sub-interface `BaseLLMService` | DELETE — tests migrated T7 |
| Qwen adapter | `shared/infrastructure/llm/providers/qwen.py::QwenService` | active, sub-interface `BaseLLMService` | DELETE — tests migrated T7 |
| Gemini adapter (AUDIT REQUIRED) | `shared/infrastructure/llm/providers/gemini.py::GeminiService` | active (complex, 320 LOC), sub-interface `BaseLLMService` | **AUDIT 6-item checklist FIRST, then DELETE only if PASS** (A3 mandate) |
| OpenAI compat helpers | `shared/infrastructure/llm/providers/_openai_compat.py` | helper, used by kimi/qwen/deepseek | DELETE (all adapters that used it are deleted) |
| Chat model resolver | `shared/infrastructure/llm/providers/_chat_model_resolver.py` | helper module (200 LOC) | **AUDIT:** grep for `from ... import ChatModelResolver` in `src/`. If zero: DELETE. Else: RETAIN + document consumers |
| Response validation | `shared/infrastructure/llm/providers/_response_validation.py` | helper module (180 LOC) | **AUDIT:** grep for `from ... import validate_response` in `src/`. If zero: DELETE. Else: RETAIN + document |
| LiteLLM param normalizer | `shared/infrastructure/llm/providers/_kwargs.py` | helper (consumed by LiteLLMService) | **RETAIN** — LiteLLMService.generate_response() imports and uses this for parameter name mapping |
| LiteLLM service | `shared/infrastructure/llm/providers/litellm.py::LiteLLMService` | **CANONICAL**, active | **NO CHANGE** — verified working post-T1, test passing post-T7 |
| Multi-role router | `shared/infrastructure/llm/router.py::MultiRoleLLMRouter` | being simplified T-5 | **NO CHANGE in T-4** — router still dual-path (legacy branch exists); T-5 removes the legacy branch |
| Arch fitness test | `backend/tests/architecture/test_llm_routing_ssot.py` | enforces `KNOWN_LEGACY_LLM_FILES` shrink-only | **Referenced in T-8** — T-4 deletion feeds into T8 assertion `test_known_legacy_files_set_is_empty` |

**No new systems created by T-4:** deletion only, no NEW abstractions.

---

## 7.5 Anti-duplication inventory cross-reference

**Relevant inventory entries from `.claude/rules/anti-duplication.md`:**

T-4 is cleanup, not a NEW abstraction. No anti-duplication violations. Verification:

| Surface in PR scope | Listed in inventory? | Canonical path | Recommendation |
|---|---|---|---|
| LiteLLMService (canonical) | YES | `shared/infrastructure/llm/providers/litellm.py::LiteLLMService` | RETAIN — no mirrors allowed. T4 deletes legacy adapters (non-canonical). |
| Legacy adapters (to delete) | NOT in inventory (legacy = non-canonical, not protected) | N/A | DELETE safe — these are "dead weight", not reusable patterns. |

**Assessment:** T-4 deletion of legacy adapters **reduces duplication pressure** (all consumers migrated to LiteLLMService canonically in T7). Zero anti-duplication violations. ✓

---

## 8. EXTEND vs NEW recommendations

| Surface PR proposes | Existing system (≥80% overlap) | In anti-dup inventory? | Recommendation | Reason |
|---|---|---|---|---|
| DELETE openai.py + 5 others | Legacy adapter classes | N/A (legacy, non-canonical) | **DELETE** | All consumers migrated T-7. Dead code. Deletion is cleanup, not architectural change. |
| DELETE _openai_compat.py | Helper for deleted adapters | N/A | **DELETE** | Zero callers post-adapter deletion. Cleanup. |
| AUDIT _chat_model_resolver.py | ChatModelResolver class | N/A (internal helper) | **DELETE if zero importers, else RETAIN+document** | Conditional on consumer scan (see § 4 audit). |
| AUDIT _response_validation.py | validate_response function | N/A (internal helper) | **DELETE if zero importers, else RETAIN+document** | Conditional on consumer scan (see § 4 audit). |
| RETAIN _kwargs.py | normalize_kwargs helper | YES (indirect: consumed by LiteLLMService canonical) | **RETAIN** | LiteLLMService still uses. Do not delete. |
| NEW test_litellm_gemini_function_call.py | Test file (audit step) | N/A (test, not production) | **NEW** | Verifies gemini.py audit checklist item "streaming chunks normalize correctly". Acceptance A4. |

**Decision rule applied:**
- Legacy adapters are dead code (T7 migrations complete) → DELETE is correct
- Helper modules: depends on consumer scan (§ 4 AUDIT logic)
- _kwargs.py: consumed by canonical LiteLLMService → RETAIN always
- New test: covers T-4 acceptance criteria → NEW required

---

## 9. Architecture fitness gates

**Tests that MUST pass post-T-4 build (auditor will run):**

| Gate | File | Why relevant | Expected |
|---|---|---|---|
| Gemini audit checklist | Commit body § "gemini-audit:" | A3 decision: pre-delete validation | PASS 6/6 items documented |
| Legacy adapter deletion | `git diff HEAD~1..HEAD --name-only \| grep -E '(openai|deepseek|kimi|qwen|gemini)\.py'` | A1 acceptance: 6 files deleted | PASS (verify via bash test -f) |
| New test execution | `backend/tests/shared/infrastructure/llm/test_litellm_gemini_function_call.py` | A4 acceptance: gemini function call shape validated | PASS |
| Full suite regression | `cd backend && pytest -x -q --tb=short` | T-7 test audit prerequisite; all 20+ mocks migrated | PASS (expected green, all adapters already replaced with LiteLLMService mocks) |
| Arch fitness `test_no_legacy_adapter_imports` | `backend/tests/architecture/test_llm_routing_ssot.py::test_no_legacy_adapter_imports` | T-8 dependency: verify zero imports of deleted files | PASS (T4 deliverable feeds T8; T8 runs post-T4) |
| Lint (ruff) | `backend/` full suite | Code quality post-deletion | 0 errors |
| Coverage maintained | `backend/ --cov` | Threshold ≥43% | ✓ (deletion doesn't reduce coverage; test coverage already exists from T7) |

**Architecture fitness ratchet:**
- T-4 deletes 6 provider adapters → `KNOWN_LEGACY_LLM_FILES` shrinks (T-8 validates this)
- No new allowlist items; allowlist shrink-only rule enforced post-T8

---

## 10. Implementation log highlights

**No pre-existing IMPL-LOG for T-4** (fresh ticket). Expected workflow:

**Phase 1 — AUDIT (test-first per TDD):**
- Write `test_litellm_gemini_function_call.py` with test cases covering 6 audit checklist items:
  - Test function calling shape: call LiteLLM with `model="gemini-pro"`, `tools=[...]`, verify `extra_body` correctly wraps tools
  - Test safety settings: verify `safety_settings` mapped via `extra_body`
  - Test system instruction: verify `system_prompt` LiteLLM param handles Gemini `system` field
  - Test generation config: verify temperature/max_tokens mapped correctly
  - Test vision multipart: call with image content, verify multipart array handling
  - Test streaming chunk format: stream response, verify chunk `delta` field normalized
- Tests RED initially (gemini.py exists but test assertions verify LiteLLM equivalence)

**Phase 2 — GEMINI AUDIT (checklist documentation):**
- Read `gemini.py` current implementation (320 LOC)
- Trace each quirk (function calling, safety_settings, system, generation_config, vision, streaming) → verify LiteLLM proxy can handle via `extra_body` / documented parameters
- Document in commit body:
  ```
  ## gemini-audit:
  - [x] Function calling via extra_body (LiteLLM wraps tools in extra_body, tested via test_litellm_gemini_function_call.py::test_function_calling_shape)
  - [x] Safety settings via extra_body (tested via test_litellm_gemini_function_call.py::test_safety_settings)
  - [x] System instruction: LiteLLM system_prompt param (tested via test_litellm_gemini_function_call.py::test_system_instruction)
  - [x] Generation config mapping (tested via test_litellm_gemini_function_call.py::test_generation_config)
  - [x] Vision multipart (tested via test_litellm_gemini_function_call.py::test_vision_multipart)
  - [x] Streaming chunk format (tested via test_litellm_gemini_function_call.py::test_streaming_chunks)
  ```
- If ANY FAIL: ESCALATE Chris, BLOCK merge. Do NOT proceed to Phase 3.

**Phase 3 — HELPER AUDIT (conditional retention):**
- `grep -rn "ChatModelResolver" backend/src/modules/ backend/src/shared/ 2>/dev/null` → if matches: RETAIN + document. Else: DELETE.
- `grep -rn "validate_response" backend/src/ 2>/dev/null` → if matches: RETAIN + document. Else: DELETE.
- Document decisions in commit body.

**Phase 4 — DELETE adapters + _openai_compat.py (GREEN):**
- Delete 6 adapter files (or 5 + gemini if audit passed)
- Delete _openai_compat.py (always)
- Delete _chat_model_resolver.py and/or _response_validation.py (if audit found zero consumers)
- Tests PASS (all mocks already migrated T-7)

**Phase 5 — VERIFY quality gates:**
- Run lint: `ruff check src/ tests/ && ruff format --check`
- Run full suite: `pytest -x -q --tb=short`
- Verify no imports of deleted files: `grep -r "from.*openai import\|from.*deepseek import\|from.*kimi import\|from.*qwen import\|from.*gemini import" backend/src/ 2>/dev/null` → expect 0 matches
- Ready for audit

---

## 11. Faithfulness gaps + validator findings

**Known gaps (context-builder Haiku pass):**

| Gap | Severity | Mitigation | Impact |
|---|---|---|---|
| gemini.py detailed code review not performed (320 LOC, complex) | MEDIUM | Builder will audit during Phase 2 AUDIT; architect/auditor verify pre-merge | Builder must read gemini.py + test carefully; escalate Chris if ANY quirk irreplicable |
| LiteLLM 2026 docs on `extra_body` parameter coverage (function calling, safety_settings) not fetched | LOW | Builder will verify against LiteLLM docs + existing LiteLLMService usage patterns | WebFetch pending § 15; builder can reference LiteLLM GitHub repo directly |
| _chat_model_resolver and _response_validation consumer scan NOT done pre-brief (too many files) | LOW | Builder will grep as part of Phase 3; brief assumes conditional retention based on grep | § 4 AUDIT table documents the grep commands builder will execute |
| T-7 test audit completion status NOT verified (assumed complete per bootstrap notes) | MEDIUM | Assume T-7 merged per bootstrap "T-7 audit-passed"; builder verifies `git log | grep T-7` | If T-7 not merged pre-builder-start: builder BLOCKS and escalates |

**Validator tasks (R24 mandatory):**
- Re-grep for legacy adapter imports across src/ with synonyms: "OpenAIService", "DeepSeekService", "KimiService", "QwenService", "GeminiService", "_openai_compat"
- Spot-check 3 claims from § 7: re-grep ChatModelResolver consumer, validate_response consumer, _kwargs.py consumer (LiteLLMService)
- Re-fetch 1 canonical doc: LiteLLM `extra_body` mechanism (§ 15)
- Verify T-7 test audit completion by checking `git log` for T-7 commit

**Faithfulness flag (preliminary):**
- **partial expected:** gemini.py code review deferred to builder (architecture brief cannot audit 320 LOC code path). All infrastructure decisions binding (A3 architect mandate). Validator will verify test audit T-7 completion.

---

## 12. Raw paths consulted

**Files read (context-builder session):**

1. `04-tickets.yaml` § T4 (lines 447-517) — ticket spec, acceptance criteria, deliverables
2. `01-spec.md` § Scenario 2, A3 (lines 52-127) — Scenario 2 negative case validates arch fitness; A3 gemini audit decision
3. `03-arch-be.md` § 0, § 1, § 2 (lines 30-248) — Architecture decisions, existing systems audit, critical decisions A3/X1/X2

**Total files read:** 3 (spec/arch/tickets)
**Total bytes read:** ~25 KB
**Estimated tokens (read pass):** ~7,000

---

## 13. Verbatim grep + WebFetch commands executed

**Greps planned (builder will execute these during Phase 3 AUDIT):**

```bash
# Step 1: Verify adapters have zero consumers post-T7
grep -rn "from src.shared.infrastructure.llm.providers import\|from.*openai import\|from.*deepseek import\|from.*kimi import\|from.*qwen import\|from.*gemini import" backend/src/ tests/ 2>/dev/null
# Expected: 0 matches in src/ (tests mocks migrated T-7)

# Step 2: Audit _chat_model_resolver consumer
grep -rn "ChatModelResolver\|chat_model_resolver" backend/src/modules/ backend/src/shared/ 2>/dev/null
# Expected: 0-5 matches (if zero, delete file)

# Step 3: Audit _response_validation consumer
grep -rn "validate_response\|_response_validation" backend/src/ 2>/dev/null
# Expected: 0-3 matches (if zero, delete file)

# Step 4: Verify _kwargs still consumed
grep -rn "_kwargs\|normalize_kwargs" backend/src/shared/infrastructure/llm/ 2>/dev/null
# Expected: >=1 match in LiteLLMService (retain file)
```

**WebFetch commands (deferred to § 15):**

```bash
# 1. LiteLLM extra_body parameter documentation
fetch: https://docs.litellm.ai/docs/completion/optional_params#extra-body-params
summary: LiteLLM `extra_body` mechanism for provider-specific params (function_calling, safety_settings, etc.)

# 2. LiteLLM Gemini provider implementation
fetch: https://github.com/BerriAI/litellm/blob/main/litellm/llms/gemini/chat/handler.py (or similar)
summary: How LiteLLM handles Gemini-specific quirks (tools, safety_settings, streaming)
```

**Status:** Greps deferred to builder Phase 3 (grep results needed before deciding helper file retention). WebFetch deferred to § 15 (builder reference).

---

## 14. Free-form notes — what I'd tell a smart colleague

**T-4 is a deletion ticket with ONE critical gate: Gemini audit.**

Here's what matters:

1. **Gemini audit is NOT optional.** The 6-item checklist (function calling, safety_settings, system_instruction, generation_config, vision, streaming) is binding per architect decision A3. If you can't verify that LiteLLM's `extra_body` mechanism handles ALL of these, you MUST escalate Chris and BLOCK. Don't try to work around it; don't assume "it'll probably work"; the cost of silent failure (Gemini broken in prod post-cleanup) is higher than the cost of blocking for review.

2. **Read gemini.py carefully.** It's 320 LOC of complex code with provider-specific quirks. The test file you write (`test_litellm_gemini_function_call.py`) is your audit vehicle. For each of the 6 checklist items, write a test that:
   - Sets up a mock Gemini call with that specific quirk (e.g., function calling: `tools=[...]` parameter)
   - Calls LiteLLM with the quirk
   - Verifies the LiteLLM proxy correctly translates it (via `extra_body` or LiteLLM parameter name mapping)
   - Does NOT execute the actual Gemini API (mock the `litellm.completion` call)

3. **The other adapters are straightforward deletions.** OpenAI, DeepSeek, Kimi, Qwen, _openai_compat all have zero consumers post-T7 test audit. Delete them. Run lint. Tests pass. Done.

4. **Helper file audit is mechanical.** For `_chat_model_resolver.py` and `_response_validation.py`, grep for callers. If zero → DELETE. If 1+ → RETAIN + document in commit body why. No judgment calls here; just grep + decide.

5. **Commit message CRITICAL.** The audit checklist must be in the commit body with 6 checkboxes + evidence. Auditor will grep the commit to verify: `git log -1 --format=%B | grep -E '^- \[x\] ' | wc -l` must return `6` (or failure reason if < 6). Format matters for compliance.

6. **Diff will be -1,500 LOC.** This is a healthy delete. Celebrate it after auditor sign-off. Cleanup PRs are valuable (reduce cognitive load, shrink attack surface).

7. **T-7 must have merged before you start.** If it hasn't, STOP and escalate. T-4 assumes all test mocks are already migrated to LiteLLMService. If T-7 is WIP, your full suite run will fail mysteriously, and you'll waste time debugging the wrong thing.

---

## 15. Upstream canonical docs references

| Framework/Library | Canonical URL | Relevance to T-4 | Fetch status |
|---|---|---|---|
| **LiteLLM** — extra_body parameter | https://docs.litellm.ai/docs/completion/optional_params#extra-body-params | T-4 gemini audit: verifying that `extra_body` can handle Gemini-specific quirks (function_calling, safety_settings) | _pending_ |
| **LiteLLM** — Gemini implementation | https://github.com/BerriAI/litellm/tree/main/litellm/llms/gemini | Reference: how LiteLLM wraps Gemini API (tools, safety_settings, streaming chunks) | _pending_ |
| **LiteLLM** — streaming response format | https://docs.litellm.ai/docs/completion/streaming | Reference: chunk format normalization (Gemini `delta` → LiteLLM standard) | _pending_ |
| **Google Gemini API** — official docs | https://ai.google.dev/api/rest/v1/models/generateContent | Reference: Gemini-native parameter names (for audit comparability) | _pending_ |

**Notes:**
- All URLs are canonical upstream sources (not internal Nicolify docs)
- WebFetch will be executed post-validator pass
- Builder can reference GitHub litellm repo directly during Phase 2 AUDIT if docs are slow to load

---

## 16. Self-budget snapshot

**Context-builder Haiku 4.5 session metrics:**

| Metric | Value |
|---|---|
| Tools used | Bash (2), Read (3), Edit (2), Write (1) |
| Files read | 3 (spec, arch, tickets yaml) |
| Total bytes read | ~25 KB |
| Estimated tokens (read pass) | ~7,000 |
| Greps planned (not executed) | 4 (deferred to builder Phase 3) |
| Bash commands | 1 (audit log append) |
| Edit operations | 2 (skeleton + initial fill) |
| Audit log entries | 8 (detailed workflow + decisions) |
| Brief sections complete | 16/16 (all _pending_ → filled) |
| Faithfulness flag (preliminary) | **partial** (gemini.py code review deferred; validator will verify T-7 completion) |
| Turns used (estimated) | ~6 of 120 max |
| Estimated tokens (build brief) | ~6,500 |
| Total session tokens (estimated) | ~13,500 / 200,000 budget |

**Brief size:** ~7.8 KB Markdown (target 5-8k achieved)

**Next step for downstream agents:**
- **Validator (Haiku):** re-grep adapter/helper consumers, spot-check 3 § 7 claims, verify T-7 completion, re-fetch § 15 canonical doc, output CONTEXT-BRIEF-validation.md
- **Builder (Opus 4.7):** consume brief, Phase 1-5 workflow (audit → delete → test → verify), produce 05-impl/T-4-result.md with gemini-audit checklist in commit body
- **Auditor (Opus 4.7):** consume brief + builder result, verify gemini audit 6/6 PASS, run full test suite, produce 06-audit/T-4-review.md with verdict APPROVED/CHANGES_REQUESTED/ESCALATED

---

## R24/R28 post-condition proof

**Validator spawn placeholder:**

Pending validator execution. Will append output here post-validator runs and produces CONTEXT-BRIEF-validation.md.

```
# R28 post-condition (to be populated by validator agent)
$ test -f "<pr_folder>/CONTEXT-BRIEF-validation.md" && echo "VALIDATOR_RAN" || echo "VALIDATOR_SKIPPED"
[placeholder — validator will fill]

$ stat -c '%s bytes' "<pr_folder>/CONTEXT-BRIEF-validation.md"
[placeholder — validator will fill]
```

This section will be updated post-validator pass.
