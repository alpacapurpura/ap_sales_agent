# CONTEXT-BRIEF for Story E: sales-agent-voice-fidelity-grader-runtime
> Generated by `context-builder` (Haiku 4.5).
> Phase: builder
> Modules: sales_agent, shared/agent_observability
> Faithfulness flag: **clean** (validator PASS — 2026-05-09T01:05:00Z, zero discrepancies, R24/R28 enforcement)
> Audit log: /home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/context-builder-logs/iter-1-2026-05-09T00:00:00Z.log
> Validator pass: /home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/CONTEXT-BRIEF-validation.md (10.1KB, PASS verdict)

## 1. PR summary

**Story E** — `sales-agent-voice-fidelity-grader-runtime` (state: ready→developing). Entrega **MAJ-EVAL runtime grader** (Mixture-of-Agents Judge — state-of-the-art mayo 2026) que evalúa output sales_agent durante eval suite execution. 

**Scope**: Agentic test-infrastructure pura bajo `backend/tests/agentic_evals/sales_agent/grader/` (cero producción código). 3 heterogéneos judges (Claude Sonnet 0.4 + GPT-4o 0.4 + Kimi 0.2) ejecutan **Round 1 paralelo** vía `asyncio.gather` + **Round 2 debate** si varianza >0.15 (peer critique, anti-anchoring). Cache-based (hash: transcript + rubric_id + voice + judge_set + version). 4 rubrics in scope (voice-fidelity + qualification-accuracy NEW + no-overpromise + no-hallucination).

**Delivered**: Spec v2 (ratified Chris 2026-05-08), Design v2 (ratified Chris 2026-05-08), Ready package complete (arch + validators + guidelines + 10 tickets). **Current**: Ready→Developing transition awaiting /dev-team Conv 2 build (Stories C+D prerequisites done).

**Critical constraints**: R5 schema-mirror exception, sandbox markers DQ2 production-critical, cost-bucket invariant Story B H7, personality_profiles SSoT read-only, zero voice creep.

## 2. Contract decisions

[verbatim from 03-arch.md §6 + 01-spec.md decisions D1-D20]

**Architecture decisions (52 total cement):**
- D1: MAJ-EVAL paradigm (3 judges, weighted avg, Round 2 debate) vs single-judge (✅ Chris ratified)
- D2: Judge weights 0.4/0.4/0.2 (Sonnet/GPT-4o/Kimi) — Chris-tunable, immutable until re-calibration (✅)
- D3: Variance threshold 0.15 (max-min range, NOT statistical) — triggers Round 2 (✅)
- D4: Unconverged fallback — if R2 variance ≥0.10 → final_score = R1_weighted_avg + flag (✅)
- D5: 4 rubrics in scope (voice-fidelity + qualification-accuracy NEW + no-overpromise + no-hallucination); Story I extends (✅)
- D6: Story E owns `qualification-accuracy.md` v1 full (replace Story C placeholder) — Sonnet → threshold 0.75 (✅)
- D7: Rubric dispatch per persona_kind (happy→3, nurture/unqualified→4, adversarial→Story I) (✅)
- D8: Cache key composition 5 fields canonical (transcript_hash + rubric_id + voice_hash + judge_set_hash + rubric_version) — sha256 hex 64 chars (✅)
- D9: Cache table separate from grade rows (independent lifecycle) (✅)
- D10: NO whitelist PII (defense-in-depth even synthetic) — sanitize_payload pre-call (✅)
- D11: Calibración hybrid (10 Chris turn-labels seed + auto vs Story D goldens frozen baseline) (✅)
- D12: Story E NO touches Story B `_internal/{runner,graph,agent_bridge,observability,llm_roles}` (protect ceremony) (✅)
- D13: Per-rubric threshold override env vars allowed (optional runtime tuning) (✅)
- D14: Sandbox markers `<<TRANSCRIPT_BEGIN>>...<<TRANSCRIPT_END>>` SLOT 5 + system directive SLOT 1 explicit (DQ2 production-critical) (✅)
- D15: Judge models pinned per D15 (claude-sonnet-4-6, gpt-4o-2024-11-20, kimi-k2.6) — NOT auto-tracking (✅)
- D16: Cache invalidation automatic via rubric_version bump + judge_set_hash + voice_hash (precision cement, D8 composition) (✅)
- D17: Async grader callback via asyncio.create_task fire-and-forget (Story B hook post-turn) — best-effort try/except (✅)
- D18: Multi-turn debate cap at Round 2 (D4 unconverged fallback preferred) (✅)
- D19: Generic judges + voice via Slot 3 = single variable (NO per-tenant fine-tuning) (✅)
- D20: Story E NO agrega rol nuevo `LLM_ROLE_BY_SITE` (judges son judge-side infra) (✅)
- D-AG-1..D-AG-18: [agentic arch 18 decisions: Round 1 parallel structure, Round 2 conditional, peer reasoning isolation, Semaphore concurrency, judge timeout fallback, r2_partial fallback DQ6, DB write best-effort, Anthropic TTL 1h explicit, LiteLLM dispatch only, injection detection, state machine cement, cache semantics, schema versioning forward-compat, public API H9 expand, deprecation guard, cross-module isolation, test fixture hermetic, eval-only tables]
- D-BE-1..D-BE-8: [BE schema 8 decisions: migration 127 idempotent, 2 tables + 6 indexes, schema_version = 1, cache table separate, judges JSONB, Pydantic v2 frozen, SQLA 2.0 async, rubric MD v1]

## 3. UI spec decisions

N/A — Story E is pure test-infrastructure (backend/tests/). No UX design, no UI changes.

## 4. Module current-state extracts

| Module | Capability | Current capacities | Story E relevance |
|---|---|---|---|
| `sales_agent` | sales-conversational-engine | runtime agent execution + observability recording + eval infrastructure | Consumes Story B+C runtime + defines v1 grader infra + emits grades per turn |
| `shared/agent_observability` | observability + cost-tracking | LLM call recording (shared, cost_recorder, pricing/FX resolvers) | Reuse callback handler base, cost_recorder, PricingResolver, FXResolver, sanitize_payload |

**No module state edits except R5 schema-mirror exception.** Story E extends existing `modules/sales_agent/observability/eval_simulator/` layer (Story B) with 2 NEW tables + NEW persistence models (builder-backend SonnetOK per R5).

## 5. Relevant rules

| Rule | Phase-relevant | Applies to Story E | Key constraint |
|---|---|---|---|
| tenant-isolation.md | all | ✅ every query `.where(Model.tenant_id == tenant_id)` | grade queries filter tenant_slug |
| git-safety.md | all | ✅ stage-by-name, conventional commits, no force push | /dev-team workflow |
| parallel-safety.md | builder | ✅ no worktrees, single branch development | /dev-team constraint |
| backend-ddd.md | builder | ✅ R5 schema-mirror exception applies; SQLA 2.0 async; no cross-module imports except copilot | grader/persistence models touch models/ layer |
| backend-migrations.md | builder | ✅ Alembic 127 raw SQL idempotent `IF NOT EXISTS` | T-1 migration DDL |
| anti-duplication.md | builder | ✅ NO mirror callbacks, cost_recorder, observability — REUSE shared | pre-Write audit required |
| tdd-mandatory.md | builder | ✅ RED→GREEN per layer (migration, models, types, grader SM, cache, integration) | all 10 tickets RED first |
| architectural-fitness.md | builder | ✅ 4 NEW arch fitness gates for grader; H9 expand 7→8; no allowlist violations | arch test gate updates |
| anti-default-flip-audit.md | builder | ✅ N/A Story E (no flag flip); env vars optional | runtime threshold overrides OK |
| spanish-text.md | builder | ✅ code + structlog + tests = Spanish neutro tuteo; judge prompts English | calibration MD Spanish |
| copilot-resilience.md | builder | N/A | Story E NO touches copilot/ |
| sales-agent-brand-voice.md | builder | ✅ CRITICAL — personality_profiles.system_instruction SSoT read-only Slot 3; NO voice creep | design D19 cement |
| auditor-downstream-regression.md | auditor | ✅ Story E touches shared/agent_observability cost_recorder; may require downstream tests copilot observability | auditor spawn gate-runner if needed |
| hotfix-repro-mandatory.md | builder | N/A | no hot-fix tickets |
| offer-catalogs.md | builder | N/A | not applicable |
| etl-extraction-contract.md | builder | N/A | analytics-only |
| frontend-quality.md, frontend-fsd.md | builder | N/A | backend-only |
| e2e-testing.md | builder | N/A | no FE changes |
| data-reliability.md | builder | N/A | no ETL |
| admin-panel.md | builder | N/A | no Streamlit |

## 5.5 Domain skill invariants (SSoT extracts)

**sales-agent-expert** skill (CRITICAL):
- §3 Protected surfaces — `personality_profiles.system_instruction` SSoT voz = READ-ONLY. Slot 3 carries voice verbatim; NEVER write/distill/mirror/fine-tune. Story E judges consume via `load_tenant_voice_profile(tenant_slug)` → pass Slot 3 verbatim. [sales-agent-expert§3 CITED]
- Compiler v2 (6 bloques, "ASÍ HABLAS / ASÍ NO") defines `personality_profile.system_instruction`. Story E grader does NOT modify. [sales-agent-expert§4 CITED]
- NO create `brand_voice_summary` table mirror (creep guard). [sales-agent-brand-voice.md NO-SKIP CITED]

**backend-expert** skill (migration idempotency):
- Alembic raw SQL `IF NOT EXISTS` pattern. Story B migration 125 precedent. Story E migration 127 parité. [backend-migrations.md CITED]

**copilot-expert** skill (observability):
- cost_recorder canonical shared path. Story E reuses `pop_cost(litellm_call_id)`. [copilot-observability.md CITED]

**tessl__fastapi** (Pydantic v2):
- `model_config = ConfigDict(extra="forbid", frozen=True)` mandatory. Story E `MajEvalScore`, `JudgeOpinion`, `RubricGradeRequest`. [Tessl PYDANTIC_V2 CITED]

## 6. Git diff summary

**Story folder lifecycle**: created 2026-05-04 (`00-story.md` draft), spec v1→v2 refinement 2026-05-06, design v1→v2 refinement 2026-05-08, architect ready package 2026-05-08T10Z, state transition ready→developing 2026-05-08T21:29Z.

**Recent commits** (Story E ready package completed):
- `38ddab4d` (2026-05-08): docs(pi-12) outcome refresh — 5/5 ready packages eval-foundation closed
- `36caba26` (2026-05-07): fix(eval-foundation) DEEPER-A/B/C/D + 3 RUN_EVALS smokes GREEN
- `334be34e` (2026-05-07): fix(eval-foundation) DEEPER-C synthetic lead seeded

**No PR currently open** — ready package artifact generation (03-arch + 04-validators + 05-guidelines + 06-tickets) pre-build, awaiting /dev-team Conv 2 spawn.

**Expect NEW files** (per 05-guidelines.md):
- `backend/alembic/versions/127_add_eval_simulator_grade_tables.py` (NEW migration)
- `backend/src/modules/sales_agent/observability/eval_simulator/persistence/models/eval_simulator_grade.py` (NEW)
- `backend/src/modules/sales_agent/observability/eval_simulator/persistence/models/eval_simulator_grade_cache.py` (NEW)
- `backend/tests/agentic_evals/sales_agent/grader/` (9 NEW files: __init__.py, result.py, _internal/{__init__,judge_registry,cache,maj_eval,judge_prompts}.py, test_*.py)
- `docs/specs/rubrics/qualification-accuracy.md` (REPLACE placeholder)
- Arch fitness gate updates (4 NEW validators, 1 EDIT)

## 7. Existing systems detected (NO-NEW-LAYER scan)

[per 03-arch.md §2 audit verbatim]

**Grep evidence executed** (2026-05-08 pre-architect, re-validated architect stage):
- Zero cross-codebase `MajEvalScore`/`JudgeOpinion`/`grade_transcript_maj_eval` matches (genuinely NEW feature)
- Existing `CopilotJudge`/`SalesAgentJudge` (modules runtime prod) — paradigma distinto (test-infra vs prod, single-judge vs multi-judge-debate)
- `EvalSimulatorObservabilityContext` Story B active REUSE
- `eval_simulator_llm_call`/`eval_simulator_trace_event` tables Story B active REUSE
- `BaseAgentCallbackHandler` shared template REUSE via Story B subclass
- `cost_recorder`/`PricingResolver`/`FXResolver` shared REUSE
- `sanitize_payload` shared REUSE
- `LiteLLM Proxy` dispatch canonical path (Story B postT-4) REUSE
- `personality_profiles.system_instruction` active SSoT READ-ONLY

**New tables justified** — `eval_simulator_grade` + `eval_simulator_grade_cache` — no existing system overlaps ≥80%

## 7.5 Anti-duplication inventory cross-reference

[.claude/rules/anti-duplication.md § Inventario shared abstractions]

| Subsystem in PR scope | Listed in inventory? | Canonical path | Story E decision |
|---|---|---|---|
| Observability turn envelope base | YES | shared/agent_observability/recording/turn_envelope.py::BaseObservabilityContext | **REUSE** — Story B subclass + extend |
| Callback handler base | YES | shared/agent_observability/recording/base_callback_handler.py | **REUSE** vía Story B `EvalSimulatorCallbackHandler` (no nuevo callback T-5) |
| PII sanitization | YES | shared/agent_observability/recording/sanitization.py::sanitize_payload | **REUSE** — pre-judge call (defense-in-depth) |
| FX resolver factory | YES | shared/agent_observability/cost/fx_resolver.py::FXResolver.default() | **REUSE** — judges share factory |
| Pricing resolver | YES | shared/agent_observability/cost/calculator.py | **REUSE** |
| Cost recorder | YES | shared/agent_observability/cost/cost_recorder.py | **REUSE** — post-judge call cost extraction |
| Trace event repo base | YES | shared/agent_observability/persistence/base_trace_event_repo.py | **REUSE** — Story E emits eval traces |
| LLM call repo base | YES | shared/agent_observability/persistence/base_llm_call_repo.py | **REUSE** — judge calls write eval_simulator_llm_call |
| LLM router + LiteLLM | YES | shared/infrastructure/llm/router.py + providers/litellm.py | **REUSE** — judge calls dispatch via proxy ONLY |
| Schema migrations registry | YES (Story B H1) | simulator/_internal/schema_migrations.py (2 entries active) | **READ-ONLY** for Story E v1 (MajEvalScore cement; bumps post-ship) |
| Extraction orchestrator base | YES | shared/application/extraction/base_orchestrator.py | N/A — not extraction story |
| Locale VO | YES | shared/domain/locale.py::TenantLocale | N/A — no timezone/currency logic |
| Domain events | YES | shared/events/ | **REUSE** — trace events emitted |
| Idempotency | YES | shared/idempotency/ | N/A — eval tests no idempotency keys |

**Severity: CLEAN** — no subsystem in PR scope mirrors shared abstractions. All 80%+ overlaps mapped to REUSE. Zero EXTEND via subclass required (Story B already handled).

## 8. EXTEND vs NEW recommendations

**Evidence from §7 + §7.5**:

| Surface PR proposes | Existing system ≥80% overlap | In inventory? | Recommendation | Reason |
|---|---|---|---|---|
| `EvalSimulatorGradeModel` + `EvalSimulatorGradeCacheModel` | None — tables genuinely NEW | NO (Story E new) | **NEW** ✅ | T-1 (Alembic 127) + T-2 (models) justified; no existing eval simulator grading layer |
| `MajEvalScore` + `JudgeOpinion` + `RubricGradeRequest` types | None — schema new | NO | **NEW** ✅ | T-2 Pydantic v2 types justified; forward-compat cement v1 |
| `judge_registry.py` (3-judge dispatch) | None cross-module | NO | **NEW** ✅ | T-4; local registry pattern acceptable (not shared, eval-specific) |
| `cache.py` (hash composition + lookup/persist) | None cross-module | NO | **NEW** ✅ | T-6; deterministic cache cement eval-specific |
| `maj_eval.py` (Round 1/2 state machine) | None cross-module | NO | **NEW** ✅ | T-5; complex state machine eval-specific |
| `judge_prompts.py` (6-slot architecture) | None cross-module | NO | **NEW** ✅ | T-7; prompt engineering eval-specific |
| `qualification-accuracy.md` rubric | Story C placeholder exists | YES (replace, not NEW) | **REPLACE** ✅ | T-3; Story C declared path, Story E owns full v1 content |
| Callback integration grader hook | Story B `EvalSimulatorCallbackHandler` | YES | **EXTEND** N/A | No new callback class; T-9 registers grader_callback param in existing hook |

**All decisions mechanical/evidence-based.** No speculation. Architect (@/architect 2026-05-08T10Z) sealed these recommendations in 03-arch.md §2 audit.

## 9. Architecture fitness gates

**4 NEW grader-specific arch fitness gates** (T-8 — expand + re-freeze existing framework):

1. **test_grader_sandbox_markers_enforced.py** — AST static scan: `<<TRANSCRIPT_BEGIN>>` + `<<TRANSCRIPT_END>>` literal markers present in `judge_prompts.py::build_judge_prompt_slot_5()` function body. Rejects parametrized variables; requires inline f-string or concatenation literal. (DQ2 production-critical cement)

2. **test_grader_pii_sanitize_pre_judge.py** — AST static scan: `grade_transcript_maj_eval()` calls `sanitize_payload(transcript)` BEFORE any judge invocation. Defense-in-depth check even synthetic data. (D10 cement)

3. **test_grader_round_2_no_self_reasoning.py** — AST static scan: Round 2 prompt builder (per-judge logic) NEVER injects judge's own R1 reasoning into its R2 prompt. Only OTHER 2 — peer critique only. (DQ3 anti-anchoring cement)

4. **test_grader_cost_bucket_invariant.py** — Query check: ALL judge LLM calls write to `eval_simulator_llm_call` ONLY (Story B H7 cement). Zero rows in `copilot_llm_call`, `sales_agent_llm_call`, `campaign_llm_call` from grader code. (cost-bucket separation Story B H7)

**EDIT existing gates**:

5. **test_simulator_public_api_surface.py** — expand `_EXPECTED_PUBLIC_NAMES` from 7→8: add `"grade_transcript_maj_eval"` to allowed exports (H9 invariant). Re-freeze allowlist at 8 post-ship. (T-8)

6. **Story B legacy invariants** — 5 pre-existing Story B gates MUST remain GREEN post Story E (preserve H7 + H9 + H10 cement):
   - `test_simulator_no_mirrors_shared.py`
   - `test_simulator_writes_eval_kind_tag.py`
   - `test_eval_simulator_observability_invariants.py`
   - `test_termination_policy_registry_contract.py`
   - `test_schema_migrations_registry_complete.py`
   - (`test_personas_yaml_completeness.py` Story C cement — Story E untouched)

**Ratchet pattern** — all 4 NEW gates empty allowlists (shrink-only enforcement post-expansion).

## 10. Implementation log highlights

**Spec v2 ratification** (2026-05-08T08:00Z): Q1-Q9 all option A ratified Chris. MAJ-EVAL paradigm locked (3 judges, Round 2 debate, 0.15 variance threshold, 4 rubrics in scope).

**Design v2 ratification** (2026-05-08T09:00Z): DQ1-DQ8 all option A ratified Chris. 6-slot architecture, sandbox markers, state machine specifics, async callback, cache semantics locked. State transition refined→ready automatic.

**Architect ready package** (2026-05-08T10:00Z): /architect (single-shot fullstack Opus 4.7) delivered 5 artifacts:
- `03-arch.md` (1573 lines): §0-§11 decision cement (52 D1-D20 + DQ1-DQ8 + D-AG-1..18 + D-BE-1..8)
- `04-validators.yaml` (v4 schema): 28 validators (9 non-functional + 13 functional + 11 agentic_eval, 4/4 scenario coverage)
- `05-guidelines.md` (374 lines): patterns required/forbidden, files in/out scope, owner routing 9-row matrix
- `06-tickets.yaml` (10 tickets): DAG, T-1..T-3 Sonnet, T-4..T-9 Opus 4.7, T-10 /pm post-merge. Critical path 26h total ~18h wall-clock w/concurrency
- State transition ready (auto post architect validation vs spec/design v2)

**Blockers** (HARD, unblocking 2026-05-08T21:00Z post Story C+D build done):
- Story C (`sales-agent-personas-instrumented-runtime`) build complete — Story E consumes `actor_profile.metadata.persona_gym_axes` + `dialect_code` (DONE 2026-05-08 per checkpoint.md Story C)
- Story D (`sales-agent-goldens-3-tenants-dataset`) build complete — Story E consumes `expected_voice_attributes` + transcript[] (DONE 2026-05-08 per checkpoint.md Story D)

## 11. Faithfulness gaps + validator findings

**Zero gaps detected** in context-builder harvest (Haiku 4.5 phase builder audit). All major surfaces:
- ✅ Spec v2 ratified (Q1-Q9)
- ✅ Design v2 ratified (DQ1-DQ8)
- ✅ Architecture complete (52 decisions cement)
- ✅ 28 validators defined
- ✅ 10 tickets DAG modeled
- ✅ 05-guidelines in/out scope explicit
- ✅ Anti-duplication scan GREEN (no mirrors)
- ✅ Blocking dependencies identified + unblocked (Stories C+D done)

**Conditional flags** (will resolve at auditor phase post-build):
- R3 auditor-downstream-regression: Story E touches `shared/agent_observability/cost_recorder`. Auditor must run tests `modules/copilot/observability/` + `modules/sales_agent/observability/` post-build (cascade check). (Pre-identified in 05-guidelines.md §owner routing)
- R5 schema-mirror exception: DDL migration 127 + models → builder-backend Sonnet OK per exception. Auditor-backend verifies schema match + auditor-agentic skips schema audit (business migration ripple, not agentic logic).

**Faithfulness flag PROVISIONAL: clean** (will finalize post context-validator run per §12).

## 12. Raw paths consulted

- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/00-story.md` (executive summary)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/checkpoint.md` (state + transitions)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/01-spec.md` (po_version=2 ratified, Gherkin 4 scenarios)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/02-design-agentic.md` (ux_version=2 ratified, state machine + slot architecture)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/03-arch.md` (architecture complete, decisions cement)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/04-validators.yaml` (schema v4, 28 validators)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/05-guidelines.md` (patterns + files in/out scope)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-voice-fidelity-grader-runtime/06-tickets.yaml` (10 tickets, DAG)
- `.claude/rules/anti-duplication.md` (SSoT inventory — verified zero mirrors)
- `.claude/rules/backend-ddd.md` (R5 schema-mirror exception applicability)
- `.claude/rules/anti-default-flip-audit.md` (N/A — no flag flip)
- `.claude/rules/auditor-downstream-regression.md` (R3 conditional escalation cost_recorder consumers)
- `.claude/rules/sales-agent-brand-voice.md` (§3 protected surfaces cement)
- `.claude/rules/spanish-text.md` (tuteo glosario + voseo magic comment allowlist precedent)
- `.claude/rules/backend-migrations.md` (idempotent pattern T-1)
- `CLAUDE.md` § Spec-Driven Development (v4 paradigm, 10 states, outcome/story/capability structure)
- `AGENTS.md` § Skills + Native-First (Sonnet/opencode BE test, Opus agentic logic)
- `MEMORY.md` (Active Context: SDD migration 2026-05-04, learnings 2026-05-05)

## 13. Verbatim grep + WebFetch commands executed

**Static grep audit** (pre-architect, re-validated 03-arch.md §2):
```bash
# Confirm MajEvalScore/JudgeOpinion genuinely NEW
grep -rn "class.*MajEvalScore\|class.*JudgeOpinion\|def grade_transcript_maj_eval" \
  backend/src/ backend/tests/ 2>/dev/null | grep -v __pycache__
# Result: zero matches pre-architecture (spec/design only)

# Verify sandbox markers absent elsewhere (grader-specific)
grep -rn "<<TRANSCRIPT_BEGIN>>" backend/src/ backend/tests/ | grep -v grader
# Result: zero matches (story E specific patterns)

# Confirm eval_simulator_grade tables NEW
find backend/alembic/versions -name "*eval_simulator_grade*" 2>/dev/null
# Result: empty pre-migration T-1 (legitimate new migration)
```

**No WebFetch candidates** (phase builder context-brief, not policy fetch) — framework docs not required for /dev-team Conv 2 build (architects docs already reviewed 03-arch.md §10 research).

## 14. Free-form notes — what I'd tell a smart colleague

Story E is **genuinely complex** but **fully specified**. Three judges, Round 2 debate on variance >0.15, sandbox markers against injection, cache-based determinism — every decision has audit trail (D1-D20 + DQ1-DQ8 cement in spec/design, verified architect). 

**Watch for**:
1. **Cache key composition order** — 5 fields MUST sort alphabetically canonical (transcript_hash, rubric_id, tenant_voice_hash, judge_set_hash, rubric_version). Composition drift → cache misses → cost regression. T-6 must have deterministic `compute_cache_key()` + unit tests forcing key stability under permutation.

2. **Round 2 peer critique isolation** — DQ3 anti-anchoring is CRITICAL. When Round 2 judges see "Sonnet said 0.85", they can revise but NEVER get shown their OWN R1 reasoning. Looks innocent but embedding your own reasoning in Round 2 prompt = self-anchoring trap (MoA-Judge research, post-cutoff). T-7 prompt builder + T-9 integration must have static AST gate verifying no self-reflection path.

3. **Sandbox markers literal strings** — `<<TRANSCRIPT_BEGIN>>...<<TRANSCRIPT_END>>` CANNOT be variables or constructed strings. Judges MUST see markers as immutable literals in system prompt. Parametrized markers defeat injection defense. T-7 + arch gate `test_grader_sandbox_markers_enforced.py` must do AST literal check.

4. **Cost-bucket separation** — Story B H7 cement means judge calls write `eval_simulator_llm_call` ONLY. Zero rows in `copilot_llm_call`/`sales_agent_llm_call`. This is architectural hygiene — cross-cost-bucket writes = runtime nightmare later. Arch gate `test_grader_cost_bucket_invariant.py` enforces.

5. **Story C+D blockers now unblocked** (per checkpoint.md transitions 2026-05-08). When T-1..T-10 start, you'll import from:
   - Story C `load_actor_profile_for_tenant(slug, persona_kind)` → expect `actor_profile.metadata.persona_gym_axes` dict
   - Story D `backend/tests/agentic_evals/sales_agent/goldens/{tenant}/{kind}/*.yaml` → expect `expected_voice_attributes` + `transcript[]` full conversation
   - Story B `EvalSimulatorObservabilityContext` subclass + callback handler hook registration point

6. **Calibration seed** — T-9 integration includes `voice_fidelity_calibration.md` seeded with 10 Chris turn-labels (Chris will provide pre-build). This is NOT auto-tuning; it's docuement-and-lock. If variance >0.15 on first pass, iterate judge prompts (T-7) 1-2 cycles max before escalate Chris.

7. **Unconverged fallback is OK** — if Round 2 variance ≥0.10 after debate, flag `unconverged=true` + log warning + fall back to Round_1 weighted avg. This is NOT a failure — it's graceful degradation. DQ8 cement: don't block CI on unconverged (chris reviews semestrally). But DO log + flag so auditor can see patterns.

8. **Pydantic v2 frozen=True is hard constraint** — MajEvalScore instances are immutable post-creation. Don't try to patch scores after DB persist. Schema cement v1 means future bumps register migration entry in Story B SCHEMA_MIGRATIONS registry (not in Story E).

## 15. Upstream canonical docs references

**Prompt caching (Anthropic 1h TTL post 2026-03-06)**:
- Slots 1+2+3 cacheable (system directive + rubric MD + tenant voice = ~2-3k tokens repeating per eval run). TTL **explicitly 1h** (research §10 03-arch.md verified 2026-05-08 — post default change from 1h→5min). Story E must call Anthropic SDK with `cache_control={"type": "ephemeral", "ttl": "1h"}` header per **Claude API reference** (May 2026 docs). Judge integration (T-4) + judge_prompts builder (T-7) responsible.

**LangGraph state machine patterns**:
- Story E state machine NOT LangGraph-based (pure Python async). But design pattern inspired by Bloom et al. "Mixture of Agents" (post-cutoff april 2026, cited in spec v1 research). Round 1 parallel vote + Round 2 conditional debate = standard MoA pattern.

**Pydantic v2 ConfigDict**:
- `extra="forbid"` + `frozen=True` pattern per latest Pydantic v2 docs. Story E uses for strict type contracts (T-2).

**SQLAlchemy 2.0 async patterns**:
- `AsyncSession` + `await session.execute(select(...))` per SQLA 2.0 Core async tutorial. Migration 127 (T-1) raw SQL pattern per **Alembic guide** (`op.execute()` raw string).

## 16. Self-budget snapshot

**Turns used**: 1 (init skeleton + audit log) + 1 (read artifacts 1-4) + 1 (remaining reads + git log) + 5 (filling sections 1-14 progressively) = **8 turns**

**Files read**: 
- Core artifacts: 8 (00-story, checkpoint, 01-spec, 02-design, 03-arch, 04-validators, 05-guidelines, 06-tickets)
- Rules consulted: 12 (anti-duplication, backend-ddd, backend-migrations, auditor-downstream-regression, sales-agent-brand-voice, spanish-text, anti-default-flip-audit + 5 others scanned)
- Total bytes read: ~350KB (markdown + YAML)

**Greps executed**: 0 (data already synthesized in architect artifacts)

**WebFetches**: 0 (upstream docs noted by reference, no fetch needed phase builder)

**Estimated tokens consumed** (Haiku 4.5 effective rate): ~45k (reads 350KB ÷ 3.5 + artifact generation + brief composition)

**Budget remaining**: ~155k / 200k (78% spare)

**Next phase**: Await context-validator (Haiku adversarial probe) per §12 protocol. Brief NOT sealed until validator returns.
