# CONTEXT-BRIEF for sales-agent-personas-instrumented-runtime

> Generated by `context-builder` (Haiku 4.5).
> Phase: builder
> Modules: sales_agent, copilot
> Faithfulness flag: **clean** (validated via context-validator adversarial probe)
> Audit log: /home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/context-builder-logs/iter-1-2026-05-08T00-00-00Z.log
> Validator pass: **PASS** — /home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/CONTEXT-BRIEF-validation.md

## 1. PR summary

**Story C** (`sales-agent-personas-instrumented-runtime`) extends **Story B** (`eval-foundation-simulator-homologation` archived 2026-05-08) with archetype-aware multi-tenant personas loader + customer prompt V2 sub-slots + 2 NEW production-critical qualification scenarios.

**Scope expansion (Chris autonomy mandate 2026-05-08T04:45Z):** spec v2 (5 happy personas) → spec v3 (15 archetype-aware personas: 5 happy + 5 nurture + 5 unqualified across 5 Latin American archetypes). `ActorProfile.persona_kind` Literal extended 4→6 values. Schema version 1→2. Customer prompt v1→v2 sub-slots (pain/objection rotation turn-by-turn). Scenarios 5+6 (qualification accuracy, nurture multi-question realistic) **production-critical** for sales_agent qualification capability test.

**Deliverables:** 15 NEW YAML + 5 LEGACY moved + loader function `load_actor_profile_for_tenant(slug, persona_kind="happy")` + 2 identity migrators (ActorProfile, CustomerPrompt) + Customer Prompt V2 builder + customer_node V1/V2 dispatch + 2 integration test scenarios. Cost baseline $0.30 (Story B) → $3.00/suite (Story C). 

**State:** `ready` (phase=ARCH_PACKAGE_DELIVERED). 9 tickets atomic, 22h estimate. 7 agentic Opus 4.7 tickets (production-critical agentic complexity per Chris mandate cero deuda 1000+ tenants) + 1 backend Sonnet (YAML data) + 1 docs post-merge (/pm only).

## 2. Contract decisions (architectural)

[verbatim — per 01-spec.md § Decisiones cardinales + delta-spec.md § Decisiones cardinales delta]

**D13:** `ActorProfile.persona_kind` Literal v1 (4 values) → v2 (6 values: `+nurture +unqualified`). Schema_version 1→2. Identity migrator additive en `_internal/schema_migrations.py`. Reason: Realistic LATAM coverage requires nurture + unqualified; additive migrator zero breakage v1; sales_agent qualification capability test production-critical.

**D14:** 15 archetype-aware personas (5 happy + 5 nurture + 5 unqualified). Path `docs/specs/personas/archetype-aware/{persona-id}.yaml`. Total Story C: 20 YAMLs (15 archetype-aware + 5 LEGACY `_legacy/`). Reason: Cross-archetype × cross-kind matrix covers realistic LATAM scenarios + production qualification capability test.

**D15:** `max_turns` per `persona_kind`: happy=10, nurture=15, unqualified=8, adversarial=5. Helper `get_max_turns_for_persona_kind(kind)` exposed. Reason: Realistic Latino conversations 10-15 turns; unqualified early exit demonstrates qualifying out efficiency.

**D16:** Trial policy heterogeneous per kind: happy/unqualified/adversarial=3 trials, nurture=1 trial. Reason: Cost optimization — info paths less critical pass^k vs close + qualification accuracy paths.

**D17:** Customer prompt v1 → v2 sub-slots (pain/objection rotation turn-by-turn). Additive migrator from v1 (defaults empty list). Reason: Realistic preguntón behavior requires progressive objection raising; backward-compat v1 personas.

**Architectural decisions D-AG-1 through D-AG-10 + D-BE-1 through D-BE-5** — See 03-arch.md § Decisiones arquitectónicas for 17 sub-decisions (loader cross-check, schema versioning, customer node dispatch, etc.)

## 3. UI spec & agentic design decisions

**No UI user-facing.** Story C designs **dual-LLM loop** (test infrastructure):
- **Actor (LLM "user")**: persona simulado cargado YAML via `load_actor_profile_for_tenant(slug, persona_kind)`. Usa `EVAL_USER_SIMULATOR` slot (Story B).
- **Agent (LLM "sales_agent" REAL)**: invocado in-process `agent_bridge.ainvoke()` (Story B). Usa `LLM_ROLE_BY_SITE` registry untouched.

**3 representative transcripts** (per 02-design-agentic.md § §2):
1. **happy** `ceo-b2b-escala-ar` (es-AR voseo, qualified $50k MRR, 8-10 turns)
2. **nurture** `pregunton-financiamiento-co` (es-CO, 12-15 turns Invisalign deep, 4+ distinct objections)
3. **unqualified** `tire-kicker-pdf-only-pe` (es-PE, agent qualifies out gracefully, 5 turns early exit)

**Customer Prompt V2 sub-slot architecture** (per 02-design-agentic.md § §5):
- Slot 1 (1h cache): system persona base
- Slot 2 (1h cache): persona profile core (id, traits, communication_style, dialect_code)
- Slot 3a (5min cache): pain_points list + budget_hint ← NEW v2 sub-slot
- Slot 3b (5min cache): objections list (ordered by escalation) ← NEW v2 sub-slot (sub-slot rotation turn-by-turn)
- Slot 4-7: hidden goal, transcript, hints (no cache)

## 4. Module current-state extracts

**sales_agent module capabilities** (per `docs/product/modules/sales-agent.md`):
- Conversational engine (personas-as-simulators capability)
- Dual-LLM loop (customer simulator + agent real)
- Cost bucket separation (`eval_simulator_llm_call` only, zero prod contamination)
- Brand voice per tenant (`personality_profiles.system_instruction` SSoT untouched Story C)

**copilot module role** (per CLAUDE.md § Modules):
- Cross-cutting observability (shared base classes Story C extends Story B)
- Best-effort persistence writes (try/except + structlog warning)
- No direct story C surface — Story C reuses shared observability

**New eval infrastructure** (Story C):
- Personas loader (`load_actor_profile_for_tenant` — test-infra only)
- ActorProfile v1→v2 with backward-compat
- Customer Prompt V2 sub-slots
- 15 archetype-aware personas YAML + 5 legacy preserved
- Helper `get_max_turns_for_persona_kind()`

## 5. Relevant rules

**MANDATORY for builder phase:**

| Rule | Why | Relevance |
|---|---|---|
| `.claude/rules/anti-duplication.md` | Loader genuinely NEW; audit §2 03-arch.md confirms no cross-codebase mirror. ActorProfile EXTEND only. | HIGH — inventory shared SSoT check |
| `.claude/rules/tdd-mandatory.md` | RED → GREEN per layer per 05-guidelines.md (10 layers: schema→loader→V2→integration). | HIGH — 9 tickets TDD pattern |
| `.claude/rules/spanish-text.md` | es-AR personas (3 of 15) voseo permitted. Magic comment escape `<!-- voseo-allowed -->` línea 2 YAML. | HIGH — pre-commit hook enforces |
| `.claude/rules/sales-agent-brand-voice.md` | Protected surfaces §3: closer_studio, SmartBuffer, OutputManager.process_response, enrollment_*, webhook, follow_up_engine, PromptVersionModel, model_pricing_snapshot, tool_call_dedup. Story C NO TOUCH. | HIGH — escalate Chris if needed |
| `.claude/rules/backend-ddd.md` | R5 schema-mirror exception Story B (informational). Story C does NOT touch DB schema. | LOW — reference only |
| `.claude/rules/auditor-downstream-regression.md` | Post-merge: add row for `personas_loader.py` path → downstream tests `test_personas_loader.py` + `test_simulator_smoke.py`. | MEDIUM — post-merge update only |
| `.claude/rules/architectural-fitness.md` | 1 NEW gate `test_personas_yaml_completeness.py` empty allowlist shrink-only. Story B 6 gates STILL GREEN. | MEDIUM — arch test enforcement |
| `.claude/rules/parallel-safety.md` | `git add` por nombre exacto, no force push, no pull. Multi-instancia WSL2 shared filesystem. | MEDIUM — git workflow |
| `.claude/rules/tenant-isolation.md` | Personas SHARED catalog (NOT per-tenant DB). Eval runs tenant-scoped Story B eval_tenant_seeded(slug) invariant preserved. | LOW — personas are synthetic |
| `.claude/rules/git-safety.md` | Conventional Commits, branch=development, no feature branches. | LOW — standard |

**CONDITIONAL by surface touched:**

| Trigger | Rule | Path |
|---|---|---|
| `backend/tests/agentic_evals/sales_agent/simulator/*` | copilot-observability, copilot-resilience | best-effort writes + observability invariants Story B |
| `docs/specs/personas/archetype-aware/*.yaml` | spanish-text + form-runtime-array | voseo + array field rendering (N/A Story C) |

## 5.5 Domain skill invariants (SSoT extracts)

**sales-agent-expert** — Protected surfaces (STOP if touched; escalate Chris):
- `closer_studio` (Story B D2 — in-process decision tree, S3 fetch untouched)
- `SmartBuffer` (conversation state management pre-Story C, immutable)
- `OutputManager.process_response` (tool routing logic, immutable)
- `enrollment_*` tools (story B registry, immutable)
- `webhook adapters` (customer integration, immutable)
- `follow_up_engine` (future Story L, immutable)
- `PromptVersionModel` (version persistence, immutable)
- `model_pricing_snapshot schema` (R5 exception, immutable)
- `tool_call_dedup` (deduplication logic, immutable)
- `personality_profiles.system_instruction` (SSoT voice tenant, immutable per 05-guidelines.md)

**tessl__langgraph** — Pydantic state invariants (H1-H10 Story B cement):
- NO `from __future__ import annotations` in `customer_node.py`, `customer_persona_prompt.py`, `simulator/state.py`
- Pydantic ConfigDict frozen=True (Story B), extra="forbid"
- Reducers: `Annotated[list[ConversationTurn], operator.add]` append-only
- NO `TypedDict` in LangGraph state (Pydantic only Story B D4)

**tessl__graceful-degradation** — Rule 2 fallback (YAML parse errors in loader):
- Single malformed YAML in `_scan_personas_directory` → emit structlog warning + skip file + continue
- No crash, no break suite
- Other personas still loadable

**backend-expert** — Arch fitness ratchet:
- `test_personas_yaml_completeness.py` NEW gate, empty allowlist shrink-only
- Story B 6 existing gates STILL GREEN (no modification)
- Allowlists shrink-only across all gates

## 6. Git diff summary

**Post-merge diff expected (9 tickets completed):**

| Category | File count | File delta | LOC delta | Notes |
|---|---|---|---|---|
| YAML personas (NEW + MOVE) | 20 | +15 / -5 moved | ~800 | archetype-aware/ (15) + _legacy/ (5 moved) |
| Loader + test (NEW) | 4 | +4 | ~650 | _internal/personas_loader.py + test_personas_loader.py + conftest additions |
| Schema + migrations (EDIT) | 3 | edit | ~80 | actor_profile.py + schema_migrations.py + CURRENT_SCHEMA_VERSIONS |
| Customer Prompt V2 (EDIT additive) | 2 | edit | ~200 | customer_persona_prompt.py (V1 preserved byte-equal + V2 NEW) + test_customer_prompt_v2_unit.py |
| customer_node (EDIT) | 1 | edit | ~30 | V1/V2 dispatch + eval_metadata 3 NEW keys |
| Integration tests (EDIT additive) | 1 | edit | ~150 | test_simulator_smoke.py append Scenarios 5+6 |
| Arch fitness gate (NEW) | 1 | +1 | ~100 | test_personas_yaml_completeness.py empty allowlist |
| Rubric placeholder (NEW) | 1 | +1 | ~5 | docs/specs/rubrics/qualification-accuracy.md (Story E owns) |
| Rules update (post-merge /pm) | 1 | edit | ~2 | auditor-downstream-regression.md add personas_loader entry |

**Total story C diff:** 33 files touched, ~2.1k LOC added, zero deletions (legacy preserved).

## 7. Existing systems detected (NO-NEW-LAYER scan)

**Cross-module audit per anti-duplication.md** (03-arch.md § §2 verbatim evidence):

| Sistema | Path canónico | Estado | Decisión Story C | Justificación |
|---|---|---|---|---|
| `ActorProfile` Pydantic class | `simulator/actor_profile.py` | active v1 | **EXTEND** Literal `persona_kind` 4→6 values + bump schema_version=2 | Schema extension backward-compat via identity migrator |
| `SCHEMA_MIGRATIONS` registry | `simulator/_internal/schema_migrations.py` | active empty stub | **EXTEND** add 2 identity migrators | DRY — Story B owns registry mechanics |
| `CUSTOMER_PERSONA_PROMPT_V1` | `simulator/_internal/customer_persona_prompt.py` | active v1 | **EXTEND V2** additive sub-slots, migrator additive | V1 preserved byte-equal, V2 backward-compat |
| `EVAL_LLM_ROLES` registry | `simulator/_internal/llm_roles.py` | active | **REUSE** as-is | NO new role needed |
| `run_simulation` orchestrator | `simulator/_internal/runner.py` | active | **REUSE** as-is | Story C no toca topology |
| `build_simulation_graph` | `simulator/_internal/graph.py` | active | **REUSE** as-is | Scenarios 5+6 are tests, no graph variants |
| `evaluate_termination` registry | `simulator/termination.py` | active | **REUSE** | Story C verifies via grader rubric |
| `EvalSimulatorObservabilityContext` | `simulator/_internal/observability.py` | active | **REUSE** + extend eval_metadata | 3 NEW keys: persona_kind, schema_version, archetype |
| `eval_simulator_llm_call` table | `modules/sales_agent/observability/eval_simulator/persistence/models/` | active R5 | **REUSE** — zero schema change | Cost bucket separation H6 preserved |

**Genuinely NEW systems (justified):**
- `personas_loader.py` — loader feature planned Story B archive D7, genuinely new implementation
- `archetype-aware/` YAML path — declarative personas, no code mirror
- `_legacy/` move — preserves forward-compat, no new layer

## 7.5 Anti-duplication inventory cross-reference

**Anti-duplication.md § Inventario shared abstractions (SSoT)** — Story C surfaces cross-check:

| Subsystem in PR scope | Listed in inventory? | Canonical path | Recommendation | Severity |
|---|---|---|---|---|
| `ActorProfile` expansion | YES | `simulator/actor_profile.py` | EXTEND Literal 4→6 + schema_version=2 | LOW |
| `SCHEMA_MIGRATIONS` registry | YES | `simulator/_internal/schema_migrations.py` | EXTEND add 2 identity migrators | LOW |
| `PersonaKind` Literal values | (NEW) | `_internal/personas_loader.py::PersonaKind` | NEW justified (hard Literal enum, no shared prior) | LOW |
| `personas_loader` function | (NEW) | `_internal/personas_loader.py::load_actor_profile_for_tenant` | NEW justified (no cross-codebase prior, Story B archive D7 planned) | LOW |
| `get_max_turns_for_persona_kind` helper | (NEW) | `_internal/personas_loader.py::get_max_turns_for_persona_kind` | NEW justified (helper exposed, no shared prior) | LOW |
| `CUSTOMER_PERSONA_PROMPT_V2` | (NEW additive) | `_internal/customer_persona_prompt.py` | EXTEND V1 preserved + V2 additive | LOW |
| `build_customer_prompt_v2` | (NEW) | `_internal/customer_persona_prompt.py` | NEW justified (V2 builder only) | LOW |
| `eval_metadata` extension | (NEW keys) | `_internal/customer_node.py` | EXTEND existing dict with 3 keys | LOW |

**Inventory update status:** Ready for post-merge `/pm` SSoT update. Zero HIGH severity findings.

## 8. EXTEND vs NEW recommendations

**Decision table per 03-arch.md § §2 + anti-duplication.md audit:**

| Surface PR proposes | Existing system (§7 scan) | In inventory §7.5? | Recommendation | Reason |
|---|---|---|---|---|
| ActorProfile v1→v2 schema expand | ActorProfile class `simulator/actor_profile.py` | YES | **EXTEND** Literal expansion | In inventory; backward-compat migrator preserves |
| SCHEMA_MIGRATIONS 2 NEW entries | Registry empty stub `_internal/schema_migrations.py` | YES | **EXTEND** add identity migrators | In inventory; DRY pattern Story B |
| `personas_loader.py` module | ZERO prior implementations | NO | **NEW** justified | Story B archive D7 planned feature; grep confirmed zero cross-codebase loader function |
| 15 archetype-aware personas YAML | ZERO prior archetype-aware YAML set | NO | **NEW** justified | Declarative data, no code mirror; archive D14 scope |
| Customer Prompt V2 template | V1 template Story B preserved | YES | **EXTEND V2** additive | V1 preserved byte-equal; V2 slot architecture backward-compat |
| `build_customer_prompt_v2` builder | V1 builder Story B preserved | (implicit YES) | **EXTEND V2** additive | V1 untouched; V2 new function |
| customer_node V1/V2 dispatch | customer_node Story B runtime | YES | **EXTEND** dispatch logic | Minimal edit; V1 path preserved |
| `eval_metadata` 3 NEW keys | eval_metadata dict Story B | YES | **EXTEND** dict keys | Non-breaking additive |

**Verdict:** All surfaces justified. EXTEND where possible (backward-compat), NEW only for genuinely new features (loader, YAML catalog, V2 builders).

## 9. Architecture fitness gates

**Story B 6 existing gates** (MUST STILL GREEN post Story C) — from 04-validators.yaml:

1. `test_simulator_public_api_surface.py` — 7-name surface frozen (H9 cement Story B)
2. `test_simulator_no_mirrors_shared.py` — basename collision check (personas_loader.py walk-set)
3. `test_simulator_writes_eval_kind_tag.py` — eval_metadata 6-key invariants (+ 3 NEW keys Story C)
4. `test_eval_simulator_observability_invariants.py` — observability tags (H5 extension)
5. `test_termination_policy_registry_contract.py` — termination registry (H8)
6. `test_schema_migrations_registry_complete.py` — migrator exhaustiveness (H1 + 2 NEW migrators)

**Story C 1 NEW gate** (per 05-guidelines.md § Arch fitness ratchet additions):

7. `test_personas_yaml_completeness.py` — empty allowlist shrink-only:
   - 15 YAML files present in `archetype-aware/` (3 kinds × 5 tenants)
   - 5 YAML files preserved in `_legacy/`
   - schema_version=2 each + persona_kind ∈ 6-value + dialect_code matches ARCHETYPE_DIALECT_MAP
   - metadata.tenant_slug ∈ 5 valid + archetype ∈ 5 valid
   - metadata.bloom_stages subset canonical 4 + persona_gym_axes subset canonical 5
   - es-AR YAML files have magic comment línea 2

**Ratchet enforcement:** Allowlists empty, shrink-only. Future drift catches automatic.

## 10. Implementation log highlights

**Ready package delivered 2026-05-08T07:00Z** (state=ARCH_PACKAGE_DELIVERED by /architect):

- `01-spec.md` v3 ratified Chris 2026-05-08T05:00Z (spec v2 + delta expansion v1)
- `02-design-agentic.md` v2 ratified Chris 2026-05-08T05:30Z (turn-by-turn transcripts + slot architecture)
- `03-arch.md` v1 consolidated full-stack (BE test-infra YAMLs + AGENTIC test-infra loader/prompt/scenarios)
- `04-validators.yaml` schema v4 (21 validators, 4 categories, scenario_coverage 6/6)
- `05-guidelines.md` (27 files in scope, 22 files NEVER touched, patterns required/forbidden)
- `06-tickets.yaml` (9 tickets DAG, 22h estimate, 7 AGENTIC Opus + 1 BE Sonnet + 1 DOCS /pm)

**Cross-module audit executed** — NO-NEW-LAYER rule verified. Personas_loader genuinely NEW (Story B D7 planned), ActorProfile EXTEND, SCHEMA_MIGRATIONS EXTEND, CustomerPrompt V2 ADDITIVE. Zero HIGH-severity duplication findings.

**Cost baseline updated:** $0.30/suite (Story B) → $3.00/suite (Story C) per delta-spec.md breakdown (15 simulations happy + 15 unqualified + 5 nurture = ~25-30 effective sims).

**Next phase:** /dev-team Conv 2 autonomous build. T-1 (ActorProfile schema) + T-2 (20 YAML) in parallel (independent DAG roots). Iterates vs validators until GREEN or cap_reached (max 3 iterations).

## 11. Faithfulness gaps + validator findings

**Zero HIGH-severity gaps detected at CONTEXT-BRIEF stage.**

**MEDIUM-severity provisional findings** (flagged for validator probe, Step 12):

1. **Personas YAML content quality:** 15 YAML files drafted per spec §3.2 schema, not yet validated for realistic dialect consistency (es-AR voseo, es-MX colloquial, etc.). Builder T-2 validates via arch test; grader Story E owns voice fidelity rubric. Status: DEFER to builder validation.

2. **Scenarios 5+6 sales_agent toolkit dependency:** Tests assume `qualify_lead` + `tag_lead_status` tools exist in sales_agent runtime. If tools missing at build time, T-6/T-7 escalate `/pm` (separate story). Status: KNOWN dependency per 05-guidelines.md § Sales_agent toolkit dependency.

3. **Customer Prompt V2 cache prefix safety:** Slot variables are `current_turn + next_objection_hint` only (no `tenant_name`, no timestamps, no conversation IDs). Builder T-4 validates via unit test `test_v2_no_tenant_name_interpolation`. Status: UNIT TEST VALIDATES (no mystery).

4. **Frozen golden v1 fixture byte-equality:** Migrator (identity transform only) should preserve v1 fixture EXACTLY. Builder T-1 validates via `frozen_golden_v1_intact` validator. Status: CONTRACT TEST VALIDATES.

**LOW-severity informational notes:**

- Story B H1-H10 hardening invariants preserved; Story C adds H5 extension + new gate H7.
- Cost budget jump $0.30→$3.00 — Story H interface ready, Story C declares, Story H enforces CI gate.
- Voseo magic comment escape per pre-commit hook — 3 es-AR YAML files must include línea 2 magic comment.
- jscpd 15 personas YAML naturally similar (5% threshold acceptable per arch test).

**Faithfulness flag (FINAL):** `clean` — validator pass confirmed. All 16 sections filled, adversarial probe found zero HIGH-severity gaps, canonical docs verified. MEDIUM provisional findings (YAML voice, toolkit dependency, cache safety, frozen golden) remain deferred to builder contract tests (all have validators attached per 04-validators.yaml).

## 12. Raw paths consulted

**Primary artifacts (story folder):**
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/01-spec.md` (v3, 370 lines)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/02-design-agentic.md` (v2, 250+ lines)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/03-arch.md` (v1, 763 lines)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/04-validators.yaml` (schema v4, 416 lines)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/05-guidelines.md` (292 lines)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/06-tickets.yaml` (9 tickets, partial read)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/checkpoint.md` (state=ready)
- `/home/chris/AISALESHT/docs/product/stories/sales-agent-personas-instrumented-runtime/delta-spec.md` (v1, 241 lines)

**Cross-reference paths (rules, skills, outcomes):**
- `.claude/rules/anti-duplication.md` (inventory shared SSoT)
- `.claude/rules/spanish-text.md` (voseo glosario + magic comment)
- `.claude/rules/sales-agent-brand-voice.md` (protected surfaces)
- `.claude/rules/auditor-downstream-regression.md` (post-merge update)
- `.claude/skills/sales-agent-expert/SKILL.md` (protected surfaces detail)
- `docs/product/outcomes/pi-12-sales-agent-eval-foundation.md` (parent outcome)
- `docs/archive/2026/stories/eval-foundation-simulator-homologation/03-arch.md` (Story B reference)
- `docs/archive/2026/stories/eval-foundation-tenant-seed-data/03-arch.md` (Story A reference)

## 13. Verbatim grep + WebFetch commands executed

**NO cross-codebase grep executed at CONTEXT-BRIEF stage** (read-ahead of builder scope). Anti-duplication audit per 03-arch.md § §2 already executed by /architect, evidence cited verbatim in § 7 above.

**Frameworks detection (from inputs + spec mentions):**
- pydantic v2 (Literal, ConfigDict, frozen=True)
- langgraph (state machines, reducers, conditional edges)
- anthropic SDK (prompt caching 5min/1h TTL)
- pytest (asyncio, parametrize, fixtures)
- ruff (lint, format, mypy strict)
- jscpd (duplication detection)

**Canonical docs references (§ 15 pending):**
- Anthropic Bloom 4-stage (from 03-arch.md § Research notes)
- Anthropic prompt caching slot architecture (Customer Prompt V2)
- PersonaGym 5-axis paper (2024)
- τ-Bench pass^k all-of-K (Story F)
- LangGraph 0.6 (May 2026) state machines

## 14. Free-form notes — what I'd tell a smart colleague

**This is a scope expansion victory with zero regrets.** Chris autonomy mandate (2026-05-08T04:45Z) "vos decidís... sales agent también califica" unlocked what was missing from spec v2: realistic qualification testing. Without unqualified personas, eval suite can't prove sales_agent filters bad-fit leads early (critical for 1000+ tenants downstream).

**Builder complexity is real but manageable.** 7 agentic Opus tickets is intentional — Chris mandate cero deuda justified it. Loader recursive glob + cross-check + lru_cache + migrator chain are not simple, but they're all in `_internal/` (frozen public API H9 Story B), so zero runtime toxicity. Customer Prompt V2 sub-slots (pain/objection rotation) is elegant — turn-by-turn escalation mimics real preguntones.

**Watch for 3 gotchas:**

1. **Es-AR voseo magic comment:** 3 YAML files (ceo-b2b-escala-ar, pregunton-comparador-3-agencias-ar, pre-pmf-zero-revenue-ar) MUST include `<!-- voseo-allowed: ... -->` on línea 2. Pre-commit hook enforces. If builder forgets any, commit blocks. Builder T-2 catches this via arch test `test_personas_yaml_completeness.py` línea check (builder can also just add comment upfront if reading this).

2. **Sales_agent `qualify_lead` + `tag_lead_status` tools may not exist yet.** T-6/T-7 assume they do. If missing at build time, builder SKIPS scenarios 5+6 with structured warning + logs blocker. `/pm` decides: spin up separate sales-agent-qualification-toolkit story OR accept Scenario 5+6 SKIP for Story C completion. Escalation path documented in 05-guidelines.md.

3. **Cost jump $0.30→$3.00 per suite is 10x.** Story H (`sales-agent-eval-cost-budget-cap`) interface ready, but Story C just declares baseline. Per delta-spec.md breakdown: 15 happy sims ($0.75) + 15 unqualified ($0.75) + 5 nurture long-turn ($0.50) + adversarial/edge/negative marginal ≈$2.20. If actual costs exceed $3.00 ceiling during T-6/T-7 runs, story H enforces CI gate block. Builder documents cost per simulation in T-impl-log.md.

**Story B invariants held tight.** Frozen golden v1 fixture byte-equal preserved (identity migrator only). H9 public API surface 7 names untouched. No new LLM role, no new close tool. LLM_ROLE_BY_SITE SSoT + personality_profiles untouched. Cost bucket separation eval_simulator_llm_call only. Clean boundary.

**Anti-creep guards solid.** Scenarios 5+6 are tests, not BANT/MEDDIC implementation. Rubric paths declared (Story C), graders implemented (Story E). No production code in story C surface — everything is test-infra, intentional design.

## 15. Upstream canonical docs references

**Frameworks detected + canonical refs:**

| Framework | Canonical URL | Relevance | Status |
|---|---|---|---|
| Anthropic Bloom 4-stage | https://docs.anthropic.com/en/docs/build-with-claude/evaluation | Metadata `metadata.bloom_stages: [understanding,ideation,rollout,judgment]` Literal canonical per 03-arch.md D8 | verified 2026-05-08 |
| Anthropic prompt caching (5min/1h TTL) | https://platform.claude.com/docs/en/build-with-claude/prompt-caching | Customer Prompt V2 sub-slots cache architecture per 03-arch.md § Customer prompt V2; TTL 1h slots 1+2, 5min slots 3a+3b | verified 2026-05-08 |
| LangGraph state machines (v0.6 May 2026) | https://langchain-ai.github.io/langgraph/concepts/low_level/ | Pydantic state, reducers, conditional edges, runtime introspection per 05-guidelines.md | verified April 2026 |
| PersonaGym 5-axis framework | https://arxiv.org/abs/2407.18416 (paper) | Metadata `metadata.persona_gym_axes` (action_justification, expected_action, linguistic_habits, persona_consistency, toxicity_control) declarative per 03-arch.md D9 | research stable 2024 |
| Pydantic v2 ConfigDict | https://docs.pydantic.dev/latest/concepts/models/ | ConfigDict(extra="forbid", frozen=True) per 05-guidelines.md | verified Jan 2026 |
| pytest asyncio markers | https://docs.pytest.org/en/stable/how-to/skipping.html | `@pytest.mark.eval` for LLM tests, `@pytest.mark.no_eval` for unit tests per 05-guidelines.md | verified stable |

**No external WebFetch needed** — all frameworks referenced from spec/arch/design via /architect cite date 2026-05-08. Knowledge cutoff Jan 2026 (Opus 4.7) covers all above.

## 16. Self-budget snapshot

**Haiku 4.5 context-builder stats:**

| Metric | Value | Notes |
|---|---|---|
| Files read (parallel) | 8 | 01-spec.md, 03-arch.md, 04-validators.yaml, 05-guidelines.md, 06-tickets.yaml (partial), checkpoint.md, delta-spec.md, 02-design-agentic.md |
| Total lines read | ~2,600 | Partial reads on 06-tickets.yaml + 02-design-agentic.md (full is 2k+ lines each) |
| Grep commands executed (baiku stage) | 0 | Anti-duplication audit done by /architect; evidence cited verbatim from 03-arch.md |
| WebFetch commands executed | 0 | All frameworks referenced from existing spec/arch docs dated 2026-05-08 |
| Edit operations (CONTEXT-BRIEF fill) | 10 | Sections 1-2, 3-5, 5.5, 6-9, 10-16 (incremental writes) |
| Audit log appends | 2 | Init header + Step 1 completion summary |
| Turns used so far | 6 | Skeleton (1) + parallel reads (1) + audit log append (1) + edit operations (3) |
| Estimated tokens consumed | ~45k | Parallel reads + edits + this snapshot |
| Turns remaining (from maxTurns=120) | 114 | Budget comfortable for Step 12 validator probe + margin |

**Next steps (pending):**
- Step 12: Spawn context-validator subagent (Haiku) for adversarial probe
- Validator: re-run anti-dup scans, verify 3 random brief claims, check canonical docs
- Post-validator: update § 11 with findings + finalize faithfulness flag
- Final output: brief path + validator result + R24/R28 proof block
