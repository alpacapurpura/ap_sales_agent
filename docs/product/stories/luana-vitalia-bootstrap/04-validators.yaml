# 04-validators.yaml — Story 11 luana-vitalia-bootstrap
# Owner: /architect Phase 2 (2026-05-13)
#
# Schema v4 — 4 categories: non_functional / functional / visual / agentic_eval
#
# Working directory for build phase: /home/chris/luana-platform/vitalia/
# (per Phase 0 Q3=B — code lives in luana-platform subdir).
#
# /dev-team iterates against these validators ticket-by-ticket until GREEN.
# /auditor verifies each validator GREEN before APPROVED.
#
# CRITICAL: cada validator MUST tener `must_pass: true` y comando ejecutable shell.

---
story_id: luana-vitalia-bootstrap
schema_version: v4
generated_at: 2026-05-13T18:00Z
generated_by: /architect (Opus 4.7)
cite_arch: docs/product/stories/luana-vitalia-bootstrap/03-arch.md
cite_spec: docs/product/stories/luana-vitalia-bootstrap/01-spec.md
cite_agentic_design: docs/product/stories/luana-vitalia-bootstrap/02-design-agentic.md

# ──────────────────────────────────────────────────────────────────
# Working dir convention: builders run commands from /home/chris/luana-platform/
# unless validator command starts with `cd vitalia/...`
# ──────────────────────────────────────────────────────────────────

validators:

  # ════════════════════════════════════════════════════════════════
  # NON-FUNCTIONAL — code quality (lint, format, type-check, arch fitness, audit)
  # ════════════════════════════════════════════════════════════════

  - id: V-NF-1
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/ruff check src/modules/vitalia/ tests/ --no-cache"
    must_pass: true
    timeout_sec: 60
    description: "Ruff 70+ rules, 0 errors on vitalia BE module + tests"

  - id: V-NF-2
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/ruff format --check src/modules/vitalia/ tests/"
    must_pass: true
    timeout_sec: 30
    description: "Ruff format strict (double quotes, spaces) — line-length 120"

  - id: V-NF-3
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && npx tsc --noEmit"
    must_pass: true
    timeout_sec: 120
    description: "TypeScript strict, 0 errors"

  - id: V-NF-4
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && npx eslint src/ --cache --cache-location .eslintcache"
    must_pass: true
    timeout_sec: 90
    description: "ESLint 60+ rules, 0 errors (sonarjs, boundaries, react-perf, prettier)"

  - id: V-NF-5
    category: non_functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/architecture/ -x -q --override-ini='addopts='"
    must_pass: true
    timeout_sec: 120
    description: "BE arch fitness gates — DDD boundaries + tenant isolation + naming + SQLA 2.0 + migrations idempotent + vitalia-specific 10 gates"

  - id: V-NF-6
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && npx vitest run src/__tests__/architecture/ --reporter=default"
    must_pass: true
    timeout_sec: 90
    description: "FE arch fitness — FSD-Lite boundaries + Spanish neutro voseo gate + ratchet allowlists shrink-only"

  - id: V-NF-7
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pip-audit"
    must_pass: true
    timeout_sec: 90
    description: "Python dependency security audit (no known CVEs)"

  - id: V-NF-8
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && npm audit --audit-level=high"
    must_pass: true
    timeout_sec: 60
    description: "NPM dependency audit (high severity 0)"

  - id: V-NF-9
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/backend && docker exec luana_platform_dev alembic upgrade head && docker exec luana_platform_dev alembic upgrade head"
    must_pass: true
    timeout_sec: 180
    description: "Alembic migration idempotent — run upgrade head twice without error (Story 10 T-10 cement pattern)"

  - id: V-NF-10
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/backend && docker exec luana_platform_dev alembic downgrade -1 && docker exec luana_platform_dev alembic upgrade head"
    must_pass: true
    timeout_sec: 240
    description: "Alembic downgrade -1 then upgrade head — verify rollback safety"

  - id: V-NF-11
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform && pnpm install --frozen-lockfile"
    must_pass: true
    timeout_sec: 240
    description: "pnpm workspace install — vitalia/frontend registered as @luana/vitalia workspace member"

  - id: V-NF-12
    category: non_functional
    type: shell
    cmd: "cd /home/chris/luana-platform && uv sync"
    must_pass: true
    timeout_sec: 240
    description: "uv workspace sync — vitalia/backend registered as workspace member"

  - id: V-NF-13
    category: non_functional
    type: pytest
    cmd: "cd /home/chris/luana-platform && .venv/bin/pytest core/tests/architecture/test_docs_extension_points_completeness.py -v"
    must_pass: true
    timeout_sec: 30
    description: "Extension SDK docs completeness — vitalia register_all surface covered"

  # ════════════════════════════════════════════════════════════════
  # FUNCTIONAL — Gherkin scenarios (BE unit + integration + E2E)
  # ════════════════════════════════════════════════════════════════

  # ─── BE unit tests ───
  - id: V-F-1
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/unit/domain/ -v --tb=short"
    must_pass: true
    timeout_sec: 120
    description: "Domain entity tests — Booking + TreatmentFollowup + ConsentRecord + PaymentIntent + etc"

  - id: V-F-2
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/unit/application/ -v --tb=short"
    must_pass: true
    timeout_sec: 180
    description: "Application services unit tests with mocked repos"

  - id: V-F-3
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/unit/api/ -v --tb=short"
    must_pass: true
    timeout_sec: 90
    description: "API DTO validation tests"

  # ─── BE integration tests (real Postgres) ───
  - id: V-F-4
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/integration/ -v --tb=short"
    must_pass: true
    timeout_sec: 240
    description: "Repository integration tests — real Postgres + alembic upgrade head + tenant isolation"

  # ─── BE E2E tests (full stack API + DB + webhooks) ───
  - id: V-F-5
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/e2e/test_onboarding_dental_e2e.py -v"
    must_pass: true
    timeout_sec: 120
    scenario_id: "spec § 3.1.A happy onboarding dental"

  - id: V-F-6
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/e2e/test_booking_prepaid_dental_e2e.py -v"
    must_pass: true
    timeout_sec: 120
    scenario_id: "spec § 3.4.A happy booking prepaid"

  - id: V-F-7
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/e2e/test_booking_consent_required_e2e.py -v"
    must_pass: true
    timeout_sec: 120
    scenario_id: "spec § 3.4.D adversarial consent missing"

  - id: V-F-8
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/e2e/test_slot_race_double_booking_e2e.py -v"
    must_pass: true
    timeout_sec: 120
    scenario_id: "spec § 3.4.C edge double-booking race"

  - id: V-F-9
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/e2e/test_cross_tenant_isolation_e2e.py -v"
    must_pass: true
    timeout_sec: 120
    scenario_id: "spec § 3.1.D adversarial cross-tenant attempt"

  # ─── BE coverage ───
  - id: V-F-10
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest --cov=src/modules/vitalia --cov-report=term-missing --cov-fail-under=43 tests/unit/ tests/integration/"
    must_pass: true
    timeout_sec: 300
    description: "BE coverage minimum 43% (per backend-quality.md baseline; target 70% for new vitalia module)"

  # ─── FE Vitest unit + integration ───
  - id: V-F-11
    category: functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && npx vitest run --coverage --reporter=default --reporter=json --outputFile=/tmp/vitalia-vitest-coverage.json"
    must_pass: true
    timeout_sec: 240
    description: "FE Vitest unit + integration tests — coverage threshold 20% all categories"

  # ─── BE webhook integration ───
  - id: V-F-12
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/integration/test_stripe_webhook.py tests/integration/test_mercadopago_webhook.py tests/integration/test_clerk_webhook.py -v"
    must_pass: true
    timeout_sec: 120
    description: "Webhook handlers — HMAC signature verify + idempotency + audit_log"

  - id: V-F-13
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/integration/test_advisory_locks.py -v"
    must_pass: true
    timeout_sec: 60
    description: "Postgres advisory locks for slot race prevention"

  - id: V-F-14
    category: functional
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/integration/test_pii_scanner.py -v"
    must_pass: true
    timeout_sec: 60
    description: "PII scanner middleware — offer description + testimonial inputs (spec § 3.2.D + § 3.3.D)"

  # ────── Fixture seed ────────
  - id: V-F-15
    category: functional
    type: shell
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/python scripts/seed_fixture_clinics.py --check"
    must_pass: true
    timeout_sec: 60
    description: "Seed 3 LatAm fixture clinics (Aurora dental + Mindful Santiago + Sanaré LATAM) idempotent"

  # ════════════════════════════════════════════════════════════════
  # VISUAL — Playwright E2E smoke per spec § 13.3 (18 specs total)
  # ════════════════════════════════════════════════════════════════
  # NEVER make e2e* Docker — playwright-expert SSoT (crashes WSL2)
  # Always native: E2E_BASE_URL=http://localhost:3000 npx playwright test

  - id: V-V-1
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/onboarding-dental-aurora.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.1.A onboarding happy Aurora dental"

  - id: V-V-2
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/onboarding-psych-mindful.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.1.A onboarding Mindful CL psychology"

  - id: V-V-3
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/onboarding-psych-sanare.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.1.A onboarding Sanaré LATAM MX"

  - id: V-V-4
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/brand-studio-dental.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.2.A brand studio 4 sections dental"

  - id: V-V-5
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/brand-studio-psych.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.2.A brand studio Mindful psychology"

  - id: V-V-6
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/brand-studio-sanare.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.2.A brand studio Sanaré LATAM"

  - id: V-V-7
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/offer-wizard-implant.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.3.A offer wizard dental implant"

  - id: V-V-8
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/offer-wizard-individual-session.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.3.B offer wizard psychology individual"

  - id: V-V-9
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/offer-wizard-packages.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.3.C offer wizard packages tokenized recurring"

  - id: V-V-10
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/booking-prepaid-sanare.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.4.A booking prepaid Sanaré"

  - id: V-V-11
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/booking-deposit-aurora.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.4.C booking deposit Aurora"

  - id: V-V-12
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/booking-full-prepay-mindful.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.4.A booking full prepay Mindful"

  - id: V-V-13
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/treatment-followup-aurora.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.5.A treatment followup Aurora dental"

  - id: V-V-14
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/treatment-followup-mindful.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.5.A treatment followup Mindful (N/A no procedures)"

  - id: V-V-15
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/treatment-followup-sanare.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.5.A treatment followup Sanaré medication tracking"

  - id: V-V-16
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/compliance-audit-log.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.6.A compliance HIPAA-lite audit log"

  - id: V-V-17
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/cross-tenant-isolation.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.1.D cross-tenant isolation"

  - id: V-V-18
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && E2E_BASE_URL=http://localhost:3000 npx playwright test --project=smoke e2e/specs/vitalia/consent-flow-implant.spec.ts"
    must_pass: true
    timeout_sec: 240
    scenario_id: "spec § 3.4.D consent capture HIPAA-lite"

  # ─── Responsive + a11y ───
  - id: V-V-19
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && npx playwright test e2e/specs/vitalia/responsive/ --project=mobile,tablet,desktop"
    must_pass: true
    timeout_sec: 300
    description: "Responsive breakpoints — mobile/tablet/desktop coverage per spec § 9"

  - id: V-V-20
    category: visual
    type: playwright
    cmd: "cd /home/chris/luana-platform/vitalia/frontend && npx playwright test e2e/specs/vitalia/a11y/ --project=smoke"
    must_pass: true
    timeout_sec: 240
    description: "Accessibility (axe-core) — critical/serious violations = fail per spec § 10"

  # ════════════════════════════════════════════════════════════════
  # AGENTIC EVAL — vertical-medical fidelity + safety + cost evaluation
  # R23 production AGENTIC code — Opus mandatory
  # ════════════════════════════════════════════════════════════════

  # ─── Compliance smoke tests (4 — spec § 15) ───
  - id: V-AE-1
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/smoke/smoke_prompt_injection.py -v"
    must_pass: true
    timeout_sec: 600
    description: "5 injection patterns blocked + audit_log entries (spec § 15.1)"

  - id: V-AE-2
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/smoke/smoke_pii_detection.py -v"
    must_pass: true
    timeout_sec: 120
    description: "10 PII inputs (AR DNI / CL RUT / MX RFC / email / phone) detected (spec § 15.2)"

  - id: V-AE-3
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/smoke/smoke_cross_tenant.py -v"
    must_pass: true
    timeout_sec: 120
    description: "3 cross-tenant attack vectors blocked at middleware (spec § 15.3)"

  - id: V-AE-4
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/smoke/smoke_hipaa_disclaimer.py -v"
    must_pass: true
    timeout_sec: 600
    description: "5 conversation flows insert HIPAA disclaimer (spec § 15.4)"

  # ─── Tools tests ───
  - id: V-AE-5
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/tools/ -v --tb=short"
    must_pass: true
    timeout_sec: 300
    description: "4 tools tests — happy/edge/error per tool (02-design § 6)"

  # ─── Extractors tests ───
  - id: V-AE-6
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/extractors/ -v --tb=short"
    must_pass: true
    timeout_sec: 600
    description: "MedicalKBExtractor + DentalHistoryExtractor wave composition + confidence + merge"

  # ─── Workflow tests ───
  - id: V-AE-7
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/workflows/ -v --tb=short"
    must_pass: true
    timeout_sec: 600
    description: "TreatmentFollowupWorkflow D0→D90 happy + safety escalation + paused branches + resume from checkpoint"

  # ─── Guardrails tests ───
  - id: V-AE-8
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/guardrails/ -v --tb=short"
    must_pass: true
    timeout_sec: 300
    description: "4 guardrails fire conditions + audit_log + chain order"

  # ─── KB pack tests ───
  - id: V-AE-9
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/kb_packs/ -v --tb=short"
    must_pass: true
    timeout_sec: 240
    description: "3 KB packs — Qdrant retrieval + tenant_id filter + forced disclaimer chunk (psychiatry medication query)"

  # ─── Vertical-medical-fidelity rubric ───
  - id: V-AE-10
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/grader/test_vertical_medical_fidelity_happy.py -v --maxfail=1"
    must_pass: true
    timeout_sec: 900
    pass_k:
      trials: 3
      per_trial_threshold: 0.75
      pass_k_threshold: 0.75
    description: "Vertical-medical-fidelity grader — happy + nurture personas pass^3 ≥0.75 (02-design § 13.3)"

  - id: V-AE-11
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/grader/test_vertical_medical_fidelity_adversarial.py -v --maxfail=1"
    must_pass: true
    timeout_sec: 1200
    pass_k:
      trials: 5
      per_trial_threshold: 0.85
      pass_k_threshold: 0.95
    description: "Vertical-medical-fidelity adversarial — diagnosis/prescription/injection personas pass^5 ≥0.95 (PRODUCTION-CRITICAL SAFETY BAR)"

  # ─── Voice fidelity per fixture ───
  - id: V-AE-12
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/grader/test_voice_fidelity_per_fixture.py -v"
    must_pass: true
    timeout_sec: 600
    rubric: docs/specs/rubrics/voice-fidelity.md
    threshold: 0.80
    description: "Voice fidelity per tenant: Aurora AR voseo / Mindful CL tuteo / Sanaré neutro broad ≥0.80"

  # ─── No-hallucination citation grader ───
  - id: V-AE-13
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/grader/test_no_hallucination.py -v"
    must_pass: true
    timeout_sec: 600
    rubric: docs/specs/rubrics/no-hallucination.md
    threshold: 0.90
    description: "No-hallucination grader — RAG citation contract enforced (KB chunk_id cited in trace_event.context_used)"

  # ─── Cost budget assertions ───
  - id: V-AE-14
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/cost_budget/test_cost_budget_booking_conversation.py -v"
    must_pass: true
    timeout_sec: 600
    threshold:
      cost_usd_max_per_booking_conversation: 0.08
    description: "Booking conversation cost ≤$0.08 USD per 10-turn happy path (02-design § 14.2)"

  - id: V-AE-15
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/cost_budget/test_cost_budget_followup_turn.py -v"
    must_pass: true
    timeout_sec: 300
    threshold:
      cost_usd_max_per_followup_turn: 0.025
    description: "Followup turn cost ≤$0.025 USD per turn"

  - id: V-AE-16
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/cost_budget/test_cost_budget_pdf_extraction.py -v"
    must_pass: true
    timeout_sec: 600
    threshold:
      cost_usd_max_per_pdf_medical: 0.15
      cost_usd_max_per_pdf_dental: 0.18
    description: "PDF extraction cost ≤$0.15 medical / ≤$0.18 dental (vision-heavy)"

  # ─── Cost bucket separation invariant ───
  - id: V-AE-17
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/architecture/test_vitalia_cost_bucket_invariant.py -v"
    must_pass: true
    timeout_sec: 60
    description: "Production calls write copilot_llm_call ONLY; eval calls write eval_simulator_llm_call ONLY (Story B/E cement)"

  # ─── Observability trace invariants ───
  - id: V-AE-18
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/observability/test_trace_invariants.py -v"
    must_pass: true
    timeout_sec: 120
    description: "copilot_trace_event records present + cost_usd > 0 + tokens accounted + PII sanitized + medical_audit_log linked"

  # ─── Adversarial suite (full Story G CI gate parity) ───
  - id: V-AE-19
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/grader/test_vertical_medical_fidelity_adversarial.py tests/agentic_evals/smoke/smoke_prompt_injection.py -v"
    must_pass: true
    timeout_sec: 1500
    description: "Adversarial suite combined — prompt injection + jailbreak + diagnosis + prescription + PII leak + scope creep — production-critical"

  # ─── Personas YAML completeness ───
  - id: V-AE-20
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/architecture/test_vitalia_personas_yaml_completeness.py -v"
    must_pass: true
    timeout_sec: 30
    description: "6 NEW personas YAML + 1 reuse — schema valid + voseo magic comment AR personas"

  # ─── Rubric MD v1 schema ───
  - id: V-AE-21
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/architecture/test_vitalia_rubric_md_v1_schema.py -v"
    must_pass: true
    timeout_sec: 30
    description: "vertical-medical-fidelity.md v1 frontmatter valid + 5 assertions A1-A5 + scoring formula"

  # ─── Cache hit rate target ───
  - id: V-AE-22
    category: agentic_eval
    type: pytest
    cmd: "cd /home/chris/luana-platform/vitalia/backend && .venv/bin/pytest tests/agentic_evals/cache/test_cache_hit_rate.py -v"
    must_pass: true
    timeout_sec: 600
    threshold:
      cache_hit_rate_min: 0.85
    description: "Slots 1-6 cache hit rate ≥85% measured via copilot_llm_call.cache_read/cache_creation ratio"

# ════════════════════════════════════════════════════════════════
# Scenario coverage matrix — spec scenarios map to N validators
# ════════════════════════════════════════════════════════════════

scenario_coverage:
  - scenario_id: "spec § 3.1 onboarding"
    validators: [V-F-5, V-V-1, V-V-2, V-V-3]
  - scenario_id: "spec § 3.2 brand studio"
    validators: [V-V-4, V-V-5, V-V-6, V-F-14]
  - scenario_id: "spec § 3.3 offer wizard"
    validators: [V-V-7, V-V-8, V-V-9, V-F-14]
  - scenario_id: "spec § 3.4 booking prepaid"
    validators: [V-F-6, V-F-7, V-F-8, V-F-12, V-F-13, V-V-10, V-V-11, V-V-12, V-V-18]
  - scenario_id: "spec § 3.5 treatment followup"
    validators: [V-AE-7, V-V-13, V-V-14, V-V-15]
  - scenario_id: "spec § 3.6 compliance audit log"
    validators: [V-V-16]
  - scenario_id: "spec § 3.1.D cross-tenant isolation"
    validators: [V-F-9, V-V-17, V-AE-3]
  - scenario_id: "spec § 3.5.D prompt injection diagnosis"
    validators: [V-AE-1, V-AE-11, V-AE-19]
  - scenario_id: "spec § 15 compliance gates"
    validators: [V-AE-1, V-AE-2, V-AE-3, V-AE-4]
  - scenario_id: "spec § 17 voice constraints"
    validators: [V-AE-10, V-AE-11, V-AE-12, V-NF-6]
  - scenario_id: "02-design § 4 workflow state machine"
    validators: [V-AE-7]
  - scenario_id: "02-design § 6 tools spec"
    validators: [V-AE-5]
  - scenario_id: "02-design § 7 extractors"
    validators: [V-AE-6]
  - scenario_id: "02-design § 14 cost budgets"
    validators: [V-AE-14, V-AE-15, V-AE-16]

# ════════════════════════════════════════════════════════════════
# Iteration policy — autonomous build behavior
# ════════════════════════════════════════════════════════════════

iteration:
  max_iterations: 10
  on_fail: "fix targeted file based on test output, re-run failing validator only"
  on_all_pass: "set state=developing→developed, append iteration_log to T-{n}-impl-log.md"
  on_cap_reached: "set state=developing→blocked, escalate to Chris with last error trace"

# ════════════════════════════════════════════════════════════════
# Halt triggers (per spec § 16 Q6 + Story 10 H1-H13 adapted)
# ════════════════════════════════════════════════════════════════

halt_triggers:
  H1: "cost variance >100% vs budget (V-AE-14/15/16 fail)"
  H2: "validators blocked >cap iter (max_iterations=10)"
  H3: "arch fitness violation introduced (V-NF-5/6 fail)"
  H4: "spec drift detected (impl != spec ratified)"
  H5: "tenant isolation regression (V-F-9 fail)"
  H6: "PII leak detected (V-AE-2 fail)"
  H7: "Spanish neutro violation user-facing (V-NF-6 fail)"
  H8: "alembic consolidation conflict (V-NF-9/10 fail)"
  H9: "cross-module import boundary violation"
  H10: "anti-duplication detection (shared abstraction needed)"
  H11: "anti-default-flip-audit violation (dormant Story 11 — no flag flips planned)"
  H12: "hotfix repro_verified false (N/A Story 11 — greenfield)"
  H13: "builder spawn refusal (Sonnet picked Opus-only ticket per R23)"
