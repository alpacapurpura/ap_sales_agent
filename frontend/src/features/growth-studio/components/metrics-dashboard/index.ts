// Layout and context are now consumed directly by the marketing-studio route layout.
// Stage wrappers are consumed by their individual page files.
export { GrowthStudioProvider, useGrowthStudioContext } from './context/GrowthStudioContext';
