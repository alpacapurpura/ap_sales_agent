import { AuthorityLandingPage } from "@/features/brand-studio/pages/AuthorityLandingPage";

/**
 * Authority landing — InstancePicker (col 2) + welcome pane.
 */
export default function BrandStudioAuthorityPage() {
  return <AuthorityLandingPage />;
}
